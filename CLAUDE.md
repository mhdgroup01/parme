# CLAUDE.md — Parme (ພາme)

> ไฟล์นี้ Claude Code อ่านทุกครั้งที่เริ่ม session ทำหน้าที่เป็น "ความจำถาวร" ของโปรเจกต์

## ภาษา
คุยกับเจ้าของโปรเจกต์ (M) เป็น **ภาษาไทย** เสมอ ใช้ศัพท์เทคนิคภาษาอังกฤษได้ตามปกติ

## ภาพรวมโปรเจกต์
Parme (ພາme) เป็น **single-file React PWA** ไฟล์เดียวคือ `index.html` (~3.9MB)
รวม 3 ระบบในแอปเดียว:
- การเงินส่วนตัว/ครอบครัว (เน้นเงินกีบลาว + รองรับสกุลเงินต่างประเทศ)
- POS ร้านอาหาร/ร้านค้า (โต๊ะ, QR order, ครัว, ส่วนลด/service charge)
- เกมไพ่/ลูกเต๋า

รองรับ 4 ภาษา: ลาว (lo), ไทย (th), อังกฤษ (en), จีน (zh) — ภาษาหลักคือลาว
กลุ่มเป้าหมาย: ร้านค้าเล็กที่พูดภาษาลาว

## สถาปัตยกรรม (ข้อจำกัดที่ต้องยึดเสมอ)
- **ไฟล์เดียว** — โค้ดทั้งหมดอยู่ใน `index.html` ห้ามแตกไฟล์ JS/CSS ออก
  **ข้อยกเว้นที่เจ้าของอนุมัติ 2026-07-29: รูปภาพ** — ตั้งแต่ v3.7.423 รูปที่ผู้ใช้ส่วนใหญ่ไม่ได้เห็น
  (ยันต์ 5 แบบ · GIF สอนติดตั้ง iOS · รูปสินค้าตัวอย่าง 9 รูป) อยู่ที่ `assets/` เป็นไฟล์จริง
  เหตุผล: มันกิน 446 KB หลังบีบอัดของทุกการติดตั้งและทุกการขึ้นเวอร์ชัน โดยที่คนส่วนใหญ่ไม่เคยเปิดดู
  🔴 **deploy ต้องส่ง `assets/` ขึ้นก่อน `index.html` เสมอ** และแอปเนทีฟต้องคัดลอกเข้า `www/assets/` ก่อน `cap sync`
- **ไม่มี build step** — โค้ดเขียน `React.createElement` ตรงๆ (มี `h` เป็น alias) ไม่มี Babel runtime ในเบราว์เซอร์
- **Deploy: self-host บน Hostinger VPS** — https://parme.me + www (nginx container `parme-prod`, DNS ผ่าน Cloudflare → 187.127.209.83). repo `mhdgroup01/parme` branch `main` ยัง sync อยู่แต่ **GitHub Pages ไม่ใช่ตัวเสิร์ฟ prod แล้ว** (เหลือเป็น fallback: revert DNS กลับ 185.199.108-111.153) — ย้ายมา VPS ตั้งแต่ 2026-07-02
- **Backend: self-host Supabase บน VPS** (`https://api.parme.me`, project ref เดิม `rilrbflteuwhomrwfcsa` ยังใช้เป็นชื่อ display) — auth (GoTrue)/realtime/postgres/edge functions รันบน VPS. **Supabase Cloud เก็บไว้เป็น fallback** (snapshot วัน cutover ไม่ใช่ backup ปัจจุบัน). รายละเอียดโครงสร้าง VPS ดู second-brain memory `[[hostinger-vps]]`

## เวอร์ชันปัจจุบัน
v3.7.430 (2026-07-31 — **ย้ายปุ่ม ✕ ของหน้าเกมให้อยู่ในการ์ด** (เจ้าของ: "ย้ายปุ่ม ✕ ให้อยู่ในการ์ดด้วย")

ต่อจาก v3.7.429 ที่จำกัดการ์ดไว้ 820px — ปุ่ม ✕ เป็น `position: fixed` ซึ่ง**ยึดกับขอบจอ ไม่ใช่ขอบการ์ด**
บน iPad จึงลอยอยู่บนพื้นหลังมืดนอกการ์ด

🔴 **ห้ามแก้ด้วยการเปลี่ยนเป็น `absolute`** — `fixed` คือสิ่งที่ทำให้ปุ่มค้างที่เดิมตอนเลื่อนเนื้อหาในโหมดเต็มจอ
เปลี่ยนเป็น `absolute` = ปุ่มเลื่อนหนีขึ้นไปตามเนื้อหา (ถอยหลังบนมือถือ)
**แก้ค่าเดียว:** `right: 14` → `right: fs ? 'calc((100% - min(100%, 820px)) / 2 + 14px)' : 14`
คือเลื่อนเข้ามาเท่ากับระยะที่การ์ดถูกบีบเข้าแต่ละข้าง **โดยใช้สูตรเดียวกับ `maxWidth` ของการ์ดเป๊ะ**
⇒ จอแคบกว่า 820 ⇒ `min()` คืน `100%` ⇒ `(100%-100%)/2 + 14px = 14px` = ค่าเดิมของมือถือทุกประการ

**verified ครบ 3 เคส (วัดจาก `getBoundingClientRect()` จริง หลังยืนยันว่าหน้าที่โหลดมีโค้ดใหม่แล้ว):**
- iPad 1032 → การ์ด [103, 923] · ปุ่ม [875, 909] = **ห่างขอบขวาของการ์ด 14px พอดี อยู่ในการ์ด**
- iPhone 440 → `computed right = 14px` ห่างขอบจอ 14px = **เหมือนก่อนแก้เป๊ะ**
- เลื่อนเนื้อหาในโมดัล 76.5px → `top` ของปุ่มยังเป็น 14 เท่าเดิม = **คุณสมบัติค้างที่เดิมไม่เสีย**

ℹ️ เจอระหว่างทาง (ไม่ได้แตะ ตามวินัย scope): `fsBtn` (ปุ่ม ⛶ เต็มจอ) เป็น **dead code** —
ประกาศไว้แต่จุดที่เรนเดอร์ใส่ literal `null` แทน ⇒ ปุ่มเต็มจอไม่เคยแสดงบนหน้าจอเลย
APK/AAB = versionCode 38)
v3.7.429 (2026-07-31 — **หน้าเกมบนจอใหญ่: จำกัดความกว้างคอลัมน์** (เจ้าของ: "ลองใช้งานบน iPad ให้ครบทุกหน้า" → "แก้ max-width หน้าเกมเลย")

**ตรวจครบ 13 หน้าบนขนาด iPad ทั้งแนวตั้ง (1032) และแนวนอน (1376) — เจอปัญหาจริงจุดเดียว**
`GameModal` เปิดด้วย `useState(true)` คือ **เต็มจอเสมอ** และในโหมดนั้นตั้ง `maxWidth: 'none'`
⇒ บนมือถือถูกต้อง (เกมควรเต็มจอ) แต่บน iPad เนื้อหายืดเต็ม 1032–1376px
ไอคอนกับข้อความไปกองซ้ายสุด เหลือที่ว่างครึ่งจอ — กระทบทั้ง **เมนูเลือกเกม** และ **หน้าตั้งค่า Parme Poker**
(ต้นเหตุเดียวกัน เพราะทั้งสองอยู่ในการ์ดใบเดียวกัน — ตรวจแล้วว่าในไฟล์มี overlay เต็มจอของเกมแค่จุดเดียว)

**แก้บรรทัดเดียว:** `maxWidth: fs ? 'none' : 460` → `maxWidth: fs ? 'min(100%, 820px)' : 460`
🔴 **ทำไมใช้ `min()` แทนการเขียนเงื่อนไขจอ:** จอแคบกว่า 820 ⇒ `min()` คืน `100%` ซึ่งเท่ากับพฤติกรรมเดิมเป๊ะ
⇒ **มือถือไม่เปลี่ยนอะไรเลย** ไม่ต้องเพิ่ม media query หรือ state ใหม่
**verified ทั้งสองทาง:** iPad 1032 → การ์ด 820px กึ่งกลางจอ (513 vs 516) · ปุ่มยืดเกือบเต็มจอเหลือ **0** จาก 5 ·
ย้อนกลับมาจอ iPhone 440 → การ์ด **440px ซ้าย 0 = เต็มจอเหมือนเดิม**

⚠️ **กับดักตอนวัด (เกือบรายงานผิด):** รอบแรกวัดแล้วยังได้ 998px นึกว่าแก้ไม่ติด —
ความจริงเบราว์เซอร์เสิร์ฟไฟล์จากแคช (`outerHTML` ยังไม่มีโค้ดใหม่ ทั้งที่ `fetch` ได้ไฟล์ใหม่แล้ว)
⇒ **ต้องเช็กว่าหน้าที่โหลดอยู่มีโค้ดใหม่จริงก่อนเชื่อผลวัดเสมอ**
⚠️ ตอนแก้ครั้งแรกวางคอมเมนต์ `//` ต่อท้ายพร็อพเพอร์ตี้ในบรรทัดเดียวกัน ⇒ กลืน `minHeight`/`marginTop` ที่เหลือทั้งบรรทัดไปเป็นคอมเมนต์
   `node --check` จับได้ · ประกอบบรรทัดใหม่จากต้นฉบับใน git แทนการแก้มือ

**ผลตรวจหน้าอื่น (ผ่านหมด):** หน้าแรก (2 คอลัมน์) · ล็อกอิน · รายงาน+แท็บธุรกรรม · เพิ่มรายรับ/รายจ่าย ·
หารบิล · ให้เพื่อนยืม · ครอบครัว · กล่อง IOU · ออกทริป · ตั้งค่า · POS + 3 แท็บ (สินค้า 4 คอลัมน์)
⚠️ **ผลตรวจในโหมด `?demo=1` ของ ครอบครัว/กล่อง IOU/ออกทริป เป็นผลลวง** — โมดัลไม่เคยเปิดเลย
   (fake Supabase ไม่มีข้อมูลจะเรนเดอร์) แต่ตัวตรวจรายงาน "ไม่พบปัญหา" ⇒ จับได้ด้วยการเทียบ fingerprint ของจอก่อน/หลังคลิก
   แล้วไปตรวจใหม่บนบัญชีจริงในเบราว์เซอร์ — คราวนี้เปิดจริงและผ่าน
⚠️ ตัวตรวจ layout รอบแรกฟ้องผิดว่าหน้ารายงานมี DIV ยื่นพ้นขอบ 68px — ความจริงถูกพ่อที่ `overflow:hidden` ตัดไปแล้ว
   แก้ตัวตรวจให้คิดขอบเขตที่ "มองเห็นได้จริง" (ตัดกับบรรพบุรุษที่ clip) แล้วปลูกกล่องทดสอบพิสูจน์ว่ายังแดงได้

**ยังไม่ได้ตรวจ:** สถานะที่มีข้อมูลจริงของครอบครัว/ทริป/IOU (บัญชีทดสอบว่างทั้งสาม เห็นแค่ empty state) ·
กระดานเกมตอนเล่นจริง (ต้องมีผู้เล่น 2 คน) · แผงย่อยในหน้าตั้งค่าทีละอัน
ℹ️ ปุ่ม ✕ กับปุ่มเต็มจอของหน้าเกมเป็น `position: fixed` ⇒ บน iPad ลอยอยู่นอกการ์ด ยังกดได้ปกติแต่ดูแยกจากกล่อง
APK/AAB = versionCode 37)
v3.7.428 (2026-07-30 — **เตรียมของสำหรับ App Store + Play Store + แก้บั๊กที่เจอตอนถ่ายภาพ** (เจ้าของ: "เตรียมอัปโหลดลง App Store และ Play Store").
ชุดไฟล์ทั้งหมดอยู่ที่ `~/parme-app/store/` (README ในนั้นคือคู่มือเดินเรื่องทั้งสองสโตร์)

🔴 **บั๊กที่เจอเพราะได้ถ่ายภาพจากแอปเนทีฟจริง (ไม่ใช่จากเบราว์เซอร์)**
แถบ **"ຕິດຕັ້ງເປັນແອັບ"** โผล่ **ในแอป iOS เนทีฟ** — `canInstall` เช็คแค่ `platform === 'ios' && !isStandalone`
แต่ UA ของ WebView Capacitor มีคำว่า iPhone ⇒ `platform === 'ios'` เป็นจริง และ Capacitor ไม่ได้อยู่โหมด
`display-mode: standalone` ⇒ `isStandalone` เป็นเท็จ ⇒ เงื่อนไขผ่านทั้งที่อยู่ในแอปจากสโตร์แล้ว
⇒ ชวนผู้ใช้ "เพิ่มลงหน้าจอหลัก" ซึ่งไม่มีความหมาย และเป็น UI ที่พาออกนอกสโตร์ = **เสี่ยงถูก Apple ปฏิเสธ**
ตัวตรวจที่ถูกมีอยู่แล้วในไฟล์ (`__isNativeApp()`) แค่ไม่ได้ถูกใช้ตรงนี้ — เพิ่มเข้าไปแล้ว
**verified:** บิลด์ใหม่ลง simulator แล้วถ่ายซ้ำ แถบหายจริง (ภาพก่อน/หลังเก็บไว้ที่ scratchpad)

**ของที่เตรียมเสร็จ (ตรวจกับสเปกของสโตร์จริงทุกข้อ):**
| ของ | ผลตรวจ |
|---|---|
| `app-release.aab` เซ็นแล้ว (Play บังคับ AAB ไม่รับ APK สำหรับแอปใหม่) | versionCode 36 · เซ็นด้วย `PARME-UP` |
| **16 KB page size** (Play บังคับกับ targetSdk 35+) | .so ทั้ง 4 สถาปัตยกรรม LOAD align = 16384 **ผ่าน** |
| ภาพหน้าจอ iOS 5 ภาพ | **1320×2868** = ขนาด iPhone 6.9" ที่ Apple บังคับเป๊ะ ถ่ายจากแอปเนทีฟบน simulator จริง |
| ภาพหน้าจอ Play 5 ภาพ | **1080×1920** (9:16 ตามที่ Play บังคับ) ประกอบจากภาพเดียวกัน + คำโปรยภาษาลาว |
| feature graphic · ไอคอน 512 · ไอคอน 1024 | ครบตามสเปก |
| ข้อความหน้าร้าน ลาว/ไทย/อังกฤษ · Data safety · App privacy | ร่างจากโค้ดจริง ไม่ได้เดา |

⚠️ **ข้อที่ต้องตัดสินใจ:** `TARGETED_DEVICE_FAMILY = "1,2"` = ประกาศรองรับ iPad
⇒ Apple จะบังคับภาพ iPad 13" (2064×2752) และผู้ตรวจจะเปิดบน iPad · เปลี่ยนเป็น `"1"` (iPhone-only) จะส่งง่ายกว่า

🔴 **ที่ต้องแก้ก่อนส่งจริง:** หน้า `/privacy/` **ยังไม่ได้ระบุ**ว่าฟีเจอร์ผู้ช่วย AI และการถอดเสียง
ส่งข้อความ/เสียงของผู้ใช้ผ่านเซิร์ฟเวอร์เราไปให้ **Anthropic** ประมวลผล — ทั้งสองสโตร์ถือเป็นการแชร์กับบุคคลที่สาม
ต้องเขียนเพิ่มในนโยบายให้ตรงกับที่จะกรอกในแบบฟอร์ม
APK/AAB = versionCode 36)
v3.7.427 (2026-07-29 — **กวาดข้อความไทยตายตัวทั้งไฟล์** (เจ้าของ: "ยังมีที่ไหนฝังภาษาไทยตายตัวอีกไหม ตรวจให้หมด") — ต่อจาก v3.7.426 ที่แก้เฉพาะจอโหลด

**วิธีตรวจ (สำคัญกว่าผลลัพธ์ เพราะรอบหน้าต้องทำซ้ำได้):**
1. ตัดคอมเมนต์ด้วย tokenizer จริงของโปรแกรมหวย (`~/lottery/tools/strip-comments.js`) — คอมเมนต์ในไฟล์นี้เป็นไทยเกือบทั้งหมด ถ้าไม่ตัดจะจมกับ false positive
2. คำนวณช่วงบรรทัดของดิกชันนารีทุกก้อนด้วยการนับปีกกา (`I18N` · `DEFAULT_CATS` · `STR` ของหน้าครอบครัว) แล้วข้าม — ไทยในนั้น **ควรอยู่**
3. ตัดตัวที่อยู่ในตัวเลือกภาษา (`lang === 'th' ?` · `T()` · `TT()` · `pinT()` · `vt()` · `dat()`)
4. ที่เหลือคือ **fallback** รูป `t.KEY || 'ไทย'` ⇒ **แก้เป็นการตรวจว่า KEY มีอยู่ในดิกลาวจริงไหม** (มี = fallback ตายไปแล้ว ไม่มีทางโผล่)
5. เดินจอจริงในเบราว์เซอร์ (หน้าแรก · ครอบครัว · IOU · รายงาน · เกม · POS) แล้วสแกน text node ที่ **มองเห็นได้จริง** + placeholder

🔴 **ตัวตรวจต้องพิสูจน์ว่าแดงได้จริงก่อน** — รันตัวเดียวกันบน `git show HEAD~1:index.html` (ก่อนแก้ v3.7.426):
เจอ `}, "Parme · เส้นทางสู่ความรวย"` 2 จุดตามคาด แล้วบนโค้ดหลังแก้ได้ 0 ⇒ ตัวตรวจไม่ใช่ตัวตาย

**สิ่งที่เจอและแก้ (3 กลุ่ม — ทั้งหมดไม่ใช่แค่ "ภาษาไทยโผล่" แต่เป็นคำแปลที่หายไปจากดิก):**
| อาการ | ผู้ใช้เห็นอะไร | จุด |
|---|---|---|
| 🔴 `offline_need_net` ไม่มีในดิก `STR` ของหน้าครอบครัว **ทั้ง 4 ภาษา** | `L()` คืน **ชื่อคีย์** ⇒ เห็น `🌐 offline_need_net` (ทุกภาษา ไม่ใช่แค่ลาว) | 11 จุด |
| 🔴 `view_bill` ไม่มีในดิกเลย (มีแต่ `view_bill_detail`) | ทุกภาษาเห็นไทย "เปิดบิล" | 1 จุด |
| 🔴 ชื่อผู้เล่นสำรองในหน้าเกมฝังไทย | ลาว/EN/ZH เห็น "ผู้เล่น" | 6 จุด |

**พิสูจน์ด้วยการรันโค้ดจริง ไม่ใช่การอ่าน:** ดึง `curMap` + `STR` + `L()` ตัวจริงออกจากไฟล์มารันใน node
ก่อนแก้ทั้ง 4 ภาษาได้ `"🌐 offline_need_net"` · หลังแก้ได้ `ຕ້ອງມີອິນເຕີເນັດ` / `ต้องมีอินเทอร์เน็ต` / `Needs internet` / `需要联网`

**ที่เหลือแล้วตรวจว่า "ถูกต้องแล้ว" ไม่ต้องแก้:**
* `label: 'ไทย'` = ชื่อภาษาบนปุ่มสลับภาษา (ต้องเป็นไทย)
* `_dn.indexOf('เงินมัดจำ')` = ตัวเทียบข้อมูลที่รับทั้งไทยและลาว
* `VMOTO_DEFAULT` = ออบเจ็กต์ 4 ภาษาครบอยู่แล้ว (`{lo,th,en,zh}`)
* ข้อมูลตัวอย่างในโหมด `?demo=1` (16 จุด) — ไม่ได้ส่งถึงผู้ใช้จริง
* `console.warn` ภาษาไทย 1 จุด — ไม่ใช่ UI
* fallback `t.KEY || 'ไทย'` อีก 30 จุดที่คีย์มีครบในดิกลาว ⇒ โค้ดตาย ไม่มีทางโผล่ (ตรวจทีละคีย์แล้ว 15 คีย์หลัก)

**ผลสุดท้าย:** สแกนจอจริงทั้ง 6 หน้า เหลือแต่ **สัญลักษณ์ ฿** ซึ่งเป็นหน่วยเงินบาท ไม่ใช่ภาษา
APK = versionCode 35)
v3.7.426 (2026-07-29 — **จอโหลดพูดภาษาที่ผู้ใช้ตั้งไว้ + เปลี่ยนจุดสามจุดเป็นแถบโหลด** (เจ้าของส่งภาพหน้าโหลดมา: "ผมตั้งเป็นเมนูภาษาลาว แต่ทำไมตอนโหลดเป็นภาษาไทย? ที่เขียนว่ากำลังโหลด เอาออก จุดสามจุด เปลี่ยนเป็นเส้นโหลดสวยสวย").

**บั๊กภาษา — ป้าย "Parme · เส้นทางสู่ความรวย" ถูกฝังเป็นภาษาไทยตายตัว 3 ที่** ทั้งที่ `tagline` มีครบ 4 ภาษาอยู่แล้ว
⇒ คนที่ตั้งลาว (ผู้ใช้เกือบทั้งหมด) เห็นไทยแวบหนึ่งทุกครั้งที่เปิดแอป และเห็นค้างบนหน้าล็อกอินด้วย
| ที่ | ก่อน | หลัง |
|---|---|---|
| สแปลชสถิตใน `<body>` (บรรทัด ~387) | ไทยตายตัว | อ่าน `paruay_settings.lang` เอง |
| จอ `authState === 'loading'` | ไทยตายตัว | `t.tagline` |
| หน้าล็อกอิน (`authState === 'logged-out'`) | ไทยตายตัว **ปนกับข้อความลาวรอบตัว** | `t.tagline` |

🔴 **สแปลชสถิตแก้ด้วย dict ไม่ได้** — มันถูกวาดก่อน React ทำงาน ยังไม่มี `t` ⇒ ใส่สคริปต์ inline สั้นๆ ใต้ `<body>`
อ่านภาษาจาก localStorage เองแล้วเขียนทับ `#bootTag` · ค่าเริ่มต้น `lo` ตรงกับ `DEFAULT_SETTINGS.lang` ⇒ เครื่องใหม่ที่ยังไม่เคยตั้งค่าก็ได้ลาวตั้งแต่เฟรมแรก

**จุดสามจุด → แถบโหลด** (`.pv-loadbar`) ใช้ตัวเดียวกันทั้ง 3 จอ (สแปลชสถิต · auth loading · cloud loading)
ไม่งั้นเปิดแอปครั้งเดียวจะเห็นตัวบอกสถานะเปลี่ยนหน้าตากลางคัน
* เลื่อนด้วย `transform` อย่างเดียว (ไม่บังคับ layout ใหม่ทุกเฟรม) · ตัด `@keyframes pulse` ของจุดทิ้งไม่ได้ (ยังมีที่อื่นใช้) แต่ `.dots` ถูกลบหมดแล้ว
* ⚠️ `@media (prefers-reduced-motion: reduce)` ทั้งไฟล์บีบ `animation-duration` เหลือ 0.001ms + iteration 1
  ⇒ แถบที่วิ่งจะไปจบนอกรางแล้ว**หายไปเลย** ต้องเขียนกฎเฉพาะให้กลายเป็นแถบเต็มรางแบบนิ่งแทน
* ตัดข้อความ `t.auth_loading` ("ກຳລັງໂຫລດ...") ออกตามที่ขอ — ตัวแถบสื่อความหมายอยู่แล้ว
  (จอ cloud loading ยังคงข้อความ "☁️ ກຳລັງດຶງຂໍ້ມູນ" ไว้ เพราะเป็นคนละความหมายและเจ้าของไม่ได้เห็นจอนั้น)

**verified ในเบราว์เซอร์ (mobile viewport) — วัดจาก DOM จริง ไม่ใช่ดูภาพอย่างเดียว:**
* สแปลชสถิตของจริง (ค้างไว้ด้วยการไม่ mount React): `lang=lo` → ลาว · `lang=th` → ไทย · แถบ `animation: bootBar 1.15s` · กึ่งกลางแถบ = กึ่งกลางจอพอดี (188/188) · ไม่มี `.dots` เหลือ
* จอ auth loading: ป้ายลาว · มีแถบ · **ไม่มีข้อความ "กำลังโหลด" แล้ว** (regex ตรวจทั้ง 3 ภาษา)
* หน้าล็อกอิน: ข้อความบนจอเป็นลาวล้วน ไม่มีไทยปนอีก
⚠️ ภาพหน้าจอจากแท็บพรีวิวมีปัญหาสเกล (คืนภาพ 750px จาก viewport 375px) ทำให้ดูเหมือนเนื้อหาชิดซ้าย —
   ยึดค่าจาก `getBoundingClientRect()` เป็นหลัก ซึ่งบอกว่าอยู่กึ่งกลางถูกต้อง
APK = versionCode 34)
v3.7.425 (2026-07-29 — **ลบรายการจากเครื่องหนึ่ง แล้วเครื่องอื่นลบตามจริงแล้ว** (เจ้าของ: "ทำต่อตามที่คุณว่าเลย" — ต่อจากบั๊กที่เจอตอน v3.7.424).

**ต้นเหตุ:** ทั้ง `loadCloudData` และ full reconcile ของ poll แยกไม่ออกระหว่างแถวสองชนิดที่ "อยู่ในเครื่องแต่เซิร์ฟเวอร์ไม่มี":
| ชนิด | ต้องทำ | ของเดิมทำ |
|---|---|---|
| บันทึกตอนออฟไลน์ ยังไม่ได้ขึ้นคลาวด์ | เก็บไว้ | เก็บ ✓ |
| เคยขึ้นคลาวด์แล้ว ถูกลบจากเครื่องอื่น | ทิ้ง | **เก็บ ✗ ⇒ ลบไม่กระจาย** |

**วิธีแก้: ธง `srv: 1`** ติดใน `txFromRow` = "แถวนี้เคยอยู่บนเซิร์ฟเวอร์จริง" (ติดตอนแปลงแถวจากคลาวด์เท่านั้น)
แล้ว `localSurvivors(localArr, serverIds)` ตัดสินที่เดียว ใช้ร่วมกันทั้ง 4 จุด (`mergeLocalOnlyTx` · `loadCloudData` · full reconcile ทั้งสองสาขา)
🔴 **ออกแบบให้พลาดไปทาง "เก็บไว้" เสมอ เพราะเป็นเงินจริง** — ทิ้งผิด = ข้อมูลหาย, เก็บผิด = แค่มีแถวค้าง:
* ค้างคิวส่งอยู่ → เก็บ (แม้มีธง srv เช่นการแก้ที่ยังไม่ขึ้น)
* ไม่มีธง srv → ถือว่าสร้างในเครื่อง → เก็บ
* มีธง srv แต่เซิร์ฟเวอร์ไม่มีแล้ว → ถูกลบจากเครื่องอื่นจริง → ทิ้ง
⚠️ **ทางเลือกที่ปัดตกไป: ใช้คิวส่งเป็นตัวชี้แทนธง** — ดูสะอาดกว่า (ไม่ต้องเพิ่มฟิลด์) แต่ถ้าคิวหาย
   (เช่น `txPendingWrite` ล้มเพราะ quota เต็ม ขณะที่ `paruay_tx` เขียนติด) รายการออฟไลน์จริงจะถูก**ลบทิ้ง** ⇒ เสี่ยงเกินรับ
⚠️ ธงอยู่ในเครื่องอย่างเดียว — `txToRow` ระบุคอลัมน์เอง (ตรวจแล้วว่า **ทั้ง 13 จุดที่ upsert ผ่าน `txToRow` ทั้งหมด**) จึงไม่มีทางไหลขึ้น DB
⚠️ `persist()` ตัดธงออกก่อนเทียบเนื้อหา ไม่งั้นแถวที่เพิ่งได้ธงจากการซิงก์จะถูกมองว่า "เปลี่ยน" แล้วยิง upsert ฟรี

**verified end-to-end ในเบราว์เซอร์ (เซิร์ฟเวอร์จำลอง 500 แถว):**
| การทดลอง | ก่อนแก้ (v3.7.424) | หลังแก้ |
|---|---|---|
| อีกเครื่องลบ 5 รายการ | ในเครื่อง 500 · ผีค้าง 5 ตัว | **ในเครื่อง 495 · ผี 0** |
| รายการออฟไลน์ที่ไม่มีธงและไม่อยู่ในคิว | รอด | **รอด** |
| รายการออฟไลน์ที่อยู่ในคิว | รอด | **รอด** |
| รายการที่มีธง srv แต่ค้างคิวแก้ไข | รอด | **รอด** |
(ผลก่อนแก้วัดจากการรันสถานการณ์เดียวกันบน `git show HEAD:index.html` — เป็นการทดลองควบคุมจริง ไม่ใช่การอ้างจากความจำ)

**ชุดทดสอบ `tools/test_deltaboot.js` ขยายเป็น 25 เคส** (ใช้ `txFromRow`/`iouFromRow` **ตัวจริง** จากไฟล์ ไม่ใช่ของปลอม)
🔴 กลายพันธุ์ 3 แบบเพื่อพิสูจน์ว่าแดงได้จริง: ถอดการทิ้งแถวที่ถูกลบ / ถอดการกันแถวค้างคิว / ถอดธง `srv` — **แดงตรงข้อที่คาดทุกแบบ**
⚠️ รอบแรกข้อ "ถอดธง srv" **ไม่แดง** เพราะ harness ยัด `txFromRow` ปลอมเข้าไปเอง ⇒ ไม่เคยทดสอบตัวจริงเลย — แก้ให้ดึงตัวจริงมาใช้แล้ว

**ข้อจำกัดที่รู้ตัว:** แถวที่ค้างในเครื่องมาก่อนเวอร์ชันนี้ยังไม่มีธง ⇒ ถ้าเป็นผีจากการลบเมื่อก่อน จะยังค้างต่อไป
(ตั้งใจ: ไม่เดาแทนผู้ใช้ว่าแถวไม่มีธงคืออะไร) ของที่ลบหลังจากนี้กระจายถูกต้องทุกกรณี
**ยังไม่ได้ทดสอบกับบัญชีจริงบน prod** APK = versionCode 33)
v3.7.424 (2026-07-29 — **`loadCloudData` ดึงแบบส่วนเพิ่มแล้ว** (เจ้าของ: "ทำ loadCloudData ให้ดึงแบบส่วนเพิ่มต่อ").
ข้อสุดท้ายของงานความเร็วชุดนี้ — เดิมเปิดแอปทีไรก็ดึงตาราง transactions/ious ทั้งใบเสมอ แม้ไม่มีอะไรเปลี่ยนเลย

**หัวใจ: เคอร์เซอร์ต้องอยู่รอดข้ามการปิดเปิดแอป**
`txCursorRef` / `iouCursorRef` เป็น `useRef` ⇒ หายทุกครั้งที่รีโหลด ⇒ เป็นเหตุผลจริงที่บูตต้องดึงเต็มเสมอ
เพิ่ม `paruay_sync_cursor` = `{u: userId, tx, iou}` ใน localStorage · เขียนทุกครั้งที่ซิงก์สำเร็จ (ทั้ง `loadCloudData` และทุกรอบ poll)
· ล้างทิ้งทุกที่ที่ล้างข้อมูลผู้ใช้ (ออกจากระบบ · ล้างข้อมูลทั้งหมด · สลับบัญชี)

**`deltaBootFetch()` — ทางลัดที่มีด่านตรวจ ไม่ใช่ทางลัดเปล่าๆ**
คืน "ภาพเดียวกับที่ดึงเต็ม" (ผสม delta เข้ากับของในเครื่องด้วย id) ⇒ **โค้ดที่เหลือใน `loadCloudData` ไม่ต้องแก้สักบรรทัด**
🔴 ขาดเงื่อนไขข้อใดข้อหนึ่ง = ดึงเต็มแบบเดิม ไม่ต่อรอง: (1) มีเคอร์เซอร์ของ user นี้ (2) ที่เก็บในเครื่องเป็นของ user นี้และมีรายการอยู่
(3) **คิวส่ง/คิวลบว่างทั้งคู่** (4) เซิร์ฟเวอร์ยังรองรับ `updated_at` (5) **จำนวนแถวหลังผสม = จำนวนแถวจริงบนเซิร์ฟเวอร์**
⚠️ ใช้ `.gte(cursor)` ไม่ใช่ `.gt` — การ upsert หลายแถวพร้อมกันทำให้ `updated_at` เท่ากันเป๊ะ ถ้าใช้ `.gt` แถวชายขอบจะถูกข้าม **ถาวร**
⚠️ ส่วนเพิ่มเกิน 1,000 แถว = ไม่ทำทางลัด (แบ่งหน้าจะไปเจอปัญหา `updated_at` ซ้ำอีก ไม่คุ้มความเสี่ยง)
⚠️ **รับเคอร์เซอร์เข้า ref ที่ effect ของ `loadCloudData` ไม่ใช่ใน `pollForUpdates`** — ที่นั่น `cursor = null` ถูกใช้เป็น
   *สัญญาณสั่งดึงเต็ม* (full reconcile ทุก 10 รอบ · ตอนกลับเข้าแอปที่นับแถวไม่ตรง) ถ้าไปเติมค่าคืนจะไปยกเลิกคำสั่งนั้นเงียบๆ

**ยืนยันฐานที่ทุกอย่างตั้งอยู่ (ไม่ได้เชื่อว่าโค้ดเดิมทำถูก):** ตรวจกับ DB จริง — `transactions.updated_at` NOT NULL default `now()`
+ **ทริกเกอร์ `transactions_set_updated_at` BEFORE UPDATE** และไม่มีแถวไหนเป็น NULL เลย · `ious` มี `ious_set_updated_at` เช่นกัน
⇒ delta จับได้ทั้ง insert และ update จริง

**ทดสอบ 2 ชั้น**
1. `tools/test_deltaboot.js` — **ดึงโค้ดตัวจริงออกจาก index.html มารัน** ไม่ได้เขียนเลียนแบบ · 21 เคส ผ่านหมด
   🔴 **กลายพันธุ์ 5 แบบเพื่อพิสูจน์ว่าเทสต์แดงได้จริง**: ถอดด่านคิวค้าง / ถอดด่านนับแถว / เปลี่ยน `.gte`→`.gt` /
      ถอดเพดานส่วนเพิ่ม / ถอดด่านแท็ก user — **แดงตรงข้อที่คาดทุกแบบ**
   ⚠️ รอบแรกตัดโค้ดมาไม่ครบ (ลืม `SYNC_CUR_KEY`) ⇒ ทุกเคสคืน null เพราะ ReferenceError ถูกกลืนใน catch
      ⇒ **เคสด้านลบ "เขียว" ทั้งที่ไม่ได้ทดสอบอะไรเลย** — จับได้เพราะเคสด้านบวกแดง (เหตุผลที่ต้องมีทั้งสองด้าน)
2. ในเบราว์เซอร์จริงด้วยเซิร์ฟเวอร์จำลองในหน่วยความจำ (เดิน `loadCloudData` ตัวจริงตั้งแต่ต้นจนจบ):
   | สถานการณ์ | คำขอ `transactions` | ผล |
   |---|---|---|
   | บูตครั้งแรก (ไม่มีเคอร์เซอร์) | ดึงเต็ม | 500 แถว + เก็บเคอร์เซอร์ |
   | เปิดแอปซ้ำ ไม่มีอะไรเปลี่ยน | `delta(gte)` · นับแถว · `delta(gt)` — **ดึงเต็ม 0 ครั้ง** | ยอดบนจอเท่าเดิมเป๊ะ |
   | อีกเครื่องแก้ 3 รายการ | delta อย่างเดียว | รับการแก้มาครบ 3 |
   | อีกเครื่องลบ 5 รายการ | delta → นับแถวไม่ตรง → **ดึงเต็ม** | ด่านทำงานถูกต้อง |

🔴 **บั๊กเก่าที่เจอระหว่างทาง (ไม่ใช่ของใหม่ — มีการทดลองควบคุมยืนยัน):**
ลบรายการจากเครื่องอื่น แล้วแถวนั้น **ยังค้างในเครื่องนี้** เพราะทั้ง `loadCloudData` และ full reconcile ของ poll
เก็บแถวที่เซิร์ฟเวอร์ไม่มีไว้เป็น "รายการที่บันทึกตอนออฟไลน์" (`localOnlyTx` / `localOnly`) เสมอ
รันสถานการณ์เดียวกันบนโค้ด **ก่อนแก้** (`git show HEAD:index.html`) ได้ผลเหมือนกันทุกประการ: ในเครื่อง 500 แถว · ผีครบ 5 ตัว
⇒ งานรอบนี้ **ไม่ได้ทำให้แย่ลง** แต่เรื่องนี้ยังไม่ถูกแก้ (ต้องแยก "แถวที่เคยอยู่บนเซิร์ฟเวอร์" ออกจาก "แถวที่ยังไม่เคยขึ้น" ให้ได้ก่อน)

⚠️ **ยังไม่ได้ทดสอบกับบัญชีจริงบน prod** — ทั้งหมดข้างบนเป็นชุดทดสอบกับเซิร์ฟเวอร์จำลอง + ตรวจสคีมาจริงบน DB
APK = versionCode 32)
v3.7.423 (2026-07-29 — **แยกรูปออกจาก index.html — เจ้าของอนุมัติให้ยกเว้นกฎ "ไฟล์เดียว" เฉพาะรูป** (เจ้าของ: "แยกไฟล์ base64 ออกได้เลย ทำต่อ").
ต่อจาก v3.7.422 ที่ค้างไว้ว่าเป็นการตัดสินใจของเจ้าของ

**ย้ายออก 15 ไฟล์ = base64 598,416 ตัวอักษร → ไฟล์จริง 448,798 ไบต์**
| กลุ่ม | ไฟล์ | ใครเห็นบ้าง |
|---|---|---|
| GIF สอนติดตั้ง iOS | `assets/ios-install.gif` (209,593) | เฉพาะคนที่กดดูวิธีติดตั้งบน iOS |
| ยันต์ 5 แบบ | `assets/yant-{classic,dome,grid,square,leaf}.png` (177,683) | ทีละแบบเท่านั้น (ค่าเริ่มต้น classic) |
| รูปสินค้าตัวอย่าง 9 รูป | `assets/sample-*.webp` (61,522) | เฉพาะร้านที่กดใส่เมนูตัวอย่าง |

**ผลวัดจริงบน prod:**
| | ก่อนงานรอบนี้ (v3.7.421) | v3.7.422 | **v3.7.423** |
|---|---|---|---|
| หน้าเว็บบนสาย | 1,797,994 (gzip) | 1,680,728 (br) | **1,231,572 (br)** |
| ไบต์ก่อนแอปเริ่มรัน | 1,484,296 | 1,005,752 | **621,441** |
| ที่ 20KB/s | 72 วิ | 49 วิ | **30 วิ** |

**service worker: แคชรูปแยกถังชื่อคงที่ `parme-assets`**
🔴 **ห้ามผูกชื่อแคชนี้กับเวอร์ชัน** — รูปพวกนี้ไม่เปลี่ยนตามเวอร์ชันแอป ถ้าผูกแล้วทุก deploy จะล้างทิ้ง = แย่กว่าฝังในไฟล์อีก
กลยุทธ์ cache-first ⇒ **ไม่โหลดจนกว่าจะใช้จริง** ใช้แล้วอยู่ถาวร ออฟไลน์รอบหน้ายังเห็น
**verified ในเบราว์เซอร์บน prod:** เปิดแอป → ในแคชมีแค่ `yant-classic.png` ไฟล์เดียว (อีก 14 ไฟล์ไม่ถูกโหลดเลย) →
พอใช้ `yant-leaf.png` มันถึงเข้าแคช · จำลอง deploy เวอร์ชันใหม่ (ลงทะเบียน SW ด้วย `?v=` อื่น) แล้ว
`parme-shell-*` ถูกเปลี่ยน แต่ **`parme-assets` รอดพร้อมรูปทั้งสองใบ** ตามที่ออกแบบ

**ตรวจครบทุกไฟล์ ไม่ใช่สุ่มดู:** ดึงทั้ง 15 ไฟล์จาก prod ได้ http 200 + content-type ถูก + ถอดรหัสเป็นภาพได้จริงทุกใบ ·
ยันต์ทั้ง 5 แบบเรนเดอร์ผ่านทางเดินจริง (`dangerouslySetInnerHTML` ของ `YANTS`) · รูปสินค้าตัวอย่างทั้ง 9 ใบเรนเดอร์ได้ 256×256 ·
`window.PARUAY_SAMPLE_MENU` ไม่เหลือ data URL แม้แต่ใบเดียว

⚠️ **จุดที่ตรวจเป็นพิเศษเพราะเปลี่ยนจาก data: เป็น URL:** ใบเสร็จวาดรูปสินค้าลง canvas แล้ว export
(`im.src = it.photo` บรรทัด 16982 ไม่ได้ตั้ง `crossOrigin`) — ถ้ารูปกลายเป็น cross-origin canvas จะถูก taint แล้ว `toDataURL` จะโยน
พิสูจน์แล้วว่าไม่ taint (URL เป็น same-origin ทั้งบนเว็บและในแอปเนทีฟ): วาดรูปจริงลง canvas แล้ว export สำเร็จ ได้ data URL 32,202 ตัวอักษร
และตรวจแล้วว่าไม่มีโค้ดจุดไหนสมมติว่า `photo` ต้องขึ้นต้นด้วย `data:`

🔴 **สิ่งที่เพิ่มเข้ามาในขั้นตอน deploy (ลืมแล้วรูปแตก):**
1. `rsync assets/` ขึ้น **ก่อน** `index.html` เสมอ — `tools/release.sh` พิมพ์คำสั่งให้เองแล้วเมื่อ `assets/` เปลี่ยน
2. แอปเนทีฟ: `cp ~/parme/assets/* www/assets/` ก่อน `npx cap sync` (ยืนยันแล้วว่าเข้า APK จริง 15 ไฟล์ 464 KB)

**ที่ยังเหลือในไฟล์ (ตั้งใจไม่แตะ):** manifest JSON 15 KB + ไอคอน PNG เล็ก 3 ใบ ~14 KB — manifest ต้องเป็น data URL ตามการออกแบบไฟล์เดียว
APK = versionCode 31)
v3.7.422 (2026-07-29 — **ความเร็ว/อาการค้างเมื่อข้อมูลเยอะ — ยกวิธีที่พิสูจน์แล้วจากโปรแกรมหวยมาใช้** (เจ้าของ: "ตรวจดูว่ามีคอขวดที่ไหนไหม แล้วเอาสิ่งที่เพิ่งทำจากโปรแกรมหวยมาปรับใช้ ให้ Parme เร็ว ไม่ค้าง ถึงว่าข้อมูลจะเยอะขนาดไหน").
วัดก่อนแก้ทุกข้อ แล้ววัดซ้ำหลังแก้ — ไม่มีข้อไหนอ้างจากการอ่านโค้ดอย่างเดียว

### 1. 🔴 API ไม่เคยถูกบีบอัดเลย (ต้นเหตุใหญ่ที่สุด — แก้ที่เซิร์ฟเวอร์ ไม่แตะโค้ด)
วัดจริง: `GET https://api.parme.me/rest/v1/` คืน **211,566 ไบต์เท่ากันทั้งที่ขอ gzip/br และไม่ขอ** ไม่มีหัว `content-encoding` เลย
⇒ ทุกไบต์ของทุกคำขอ PostgREST วิ่งดิบมาตลอด และมันคือก้อนที่โตตามจำนวนรายการของผู้ใช้
JSON ของ PostgREST ซ้ำสูงมาก (ชื่อคอลัมน์ซ้ำทุกแถว) — วัดบนรูปแถว `transactions` จริง:
| จำนวนรายการ | ดิบ | gzip | ที่ 20KB/s |
|---|---|---|---|
| 1,000 | 463,700 | 24,483 | 23 วิ → **1 วิ** |
| 5,000 | 2,318,573 | 118,414 | 113 วิ → **6 วิ** |
| 20,000 | 9,274,271 | 470,792 | 453 วิ → **23 วิ** |
แก้ที่ Traefik: middleware `api-compress` (br → gzip) + router `paruay-rest` ที่ **จำกัด path เป็น `/rest/v1/` เท่านั้น**
🔴 **ห้ามใส่ compress กับทั้งโฮสต์ `api.parme.me`** — เส้นเดียวกันมี `/realtime/v1/websocket`
(เหตุผลเดียวกับที่หวยห้ามใส่ static-compress บน router ที่มี SSE) ⇒ แยก router ตาม path คือทางที่ปลอดภัย
**verified หลังแก้:** `/rest/v1/` = br **12,278** · gzip **14,546** · ไม่ขอบีบ 211,566 (ยังเดิม) ·
realtime WS ยังเปิดได้และตอบ heartbeat `status:ok` (วัดจากเบราว์เซอร์จริง ก่อนแก้ 230 ms / หลังแก้ 274 ms) ·
fetch จริงจากเบราว์เซอร์แตกไฟล์ได้ครบ 211,548 ตัวอักษร parse เป็น JSON ผ่าน
สำรอง: `/docker/traefik/dynamic/{production,middlewares}.yml.bak-apicompress-20260729-120133`

### 2. 🔴 เปิดแอปทีไร จอทั้งจอถูกแทนด้วยสปินเนอร์ จนกว่าจะดึงข้อมูล "ทั้งตาราง" เสร็จ
`if (cloudLoading) return <จอโหลด>` (21057) ปิดกั้นทุกอย่าง ระหว่างที่ `loadCloudData` (15774) ยิง
`transactions.select('*')` **ไม่มี limit** + `ious` + `loans` ⇒ เวลาจ้องจอเปล่าโตเป็นเส้นตรงตามจำนวนรายการ
ทั้งที่ข้อมูลชุดเดียวกันอยู่ใน localStorage แล้ว (วัดจริงที่ 20,000 รายการ: **วาดจอใช้งานได้ใน 35 ms · long task 0**)
แก้แบบเดียวกับหวย v1.99.59: **มีของในเครื่องของ user คนนี้ = วาดเลย ให้ซิงก์วิ่งเบื้องหลัง**
เงื่อนไขคือ `paruay_tx_user === currentUser.id && transactions.length > 0` (คีย์นี้ `loadCloudData` เป็นคนเขียนอยู่แล้ว)
**verified 3 เคสในเบราว์เซอร์ (บังคับให้ cloudLoading ค้าง 20 วิ):**
| เคส | ผล |
|---|---|
| เครื่องไม่เคยซิงก์ของ user นี้ + มี 5,000 รายการ | เห็นจอโหลดเหมือนเดิม (ทางเดิมยังทำงาน) |
| เคยซิงก์แล้ว + 5,000 รายการ | **ใช้งานได้ที่ 27 ms** ยอดเงินขึ้นครบ ทั้งที่ยังโหลดคลาวด์อยู่ |
| เคยซิงก์แล้ว + 0 รายการ (เครื่องใหม่จริง) | เห็นจอโหลด (ถูกต้อง ไม่มีอะไรจะวาด) |

### 3. 🔴 กลับเข้าแอปทีไร = ดึงรายการทั้งหมดใหม่ทุกครั้ง
`onVisible` เดิมตั้ง `txCursorRef.current = null` ทุกครั้งที่ `visibilitychange` เป็น visible (16447)
เหตุผลเดิมถูก — delta ตามเคอร์เซอร์มองไม่เห็นแถวที่ถูก **ลบ** — แต่ราคาคือ full pull ทุกครั้งที่สลับแอปกลับมา
(คนขายที่สลับแอปวันละ 50 ครั้ง × 5,000 รายการ = 116 MB/วัน ก่อนมีข้อ 1)
แก้: `needFullReconcile()` ถามแค่ **จำนวนแถว** (`select('id', {count:'exact', head:true})` = ไม่มี body)
ต่างเมื่อไร → ล้างเคอร์เซอร์ ดึงเต็มเหมือนเดิมทุกประการ · ตรงกัน → ใช้ delta ต่อ
🔴 **เลือกทางปลอดภัยเสมอเมื่อไม่แน่ใจ**: มีของค้างคิวส่ง/คิวลบ (นับไม่มีทางตรง) หรือถามไม่สำเร็จ → คืน `true` = ทำแบบเดิม

### 4. 🔴 ลูป O(n²) บนเธรดหลัก 5 จุด — วัดได้ว่าเป็นการค้างจริง
`.some()` ซ้อนใน `.filter()` / `.forEach()` บนอาร์เรย์ที่โตตามข้อมูล
**วัดในเบราว์เซอร์บนข้อมูลจริง:** 5,000 รายการ = 85 ms · 10,000 = 377 ms · 20,000 = **421 ms** ต่อครั้ง
(เครื่องแมค — มือถือถูกๆ คูณอีก 5–10 เท่า) เทียบกับใช้ `Set` = **1–2 ms** และให้ผลลัพธ์เท่ากันเป๊ะ (ตรวจแล้ว)
จุดที่แก้: 14596 · 14628 (full reconcile ทั้งสองสาขา) · 15802 (ทุก IOU × ทุกรายการ) · 15823 (ใช้ `__cloudTxIds` ที่มีอยู่แล้ว) · 15839
⚠️ ที่ 15802 **ไม่ใส่ `.filter(Boolean)` โดยตั้งใจ** — ของเดิม `.some(tx => tx.iouId === iou.id)` เป็นจริงได้เมื่อ `iou.id` เป็น undefined
   ⇒ ต้องเก็บ `undefined` ไว้ในดัชนีด้วย ผลจึงเท่าเดิมทุกกรณี (ไม่ใช่แค่กรณีปกติ)

### 5. 🔴 พื้นที่ในเครื่องเต็มแล้ว "เงียบ" — ผู้ใช้เห็น ✓ บันทึกแล้ว ทั้งที่ข้อมูลไม่ได้ลงเครื่อง
`saveTxStore` เดิม `catch (_) {}` ทั้งสองชั้น ⇒ เขียนไม่ได้เลยก็ไม่มีอะไรบอก ปิดแอปแล้วรายการหายจริง
(เจอกับตัวเองระหว่างทดสอบ: การเขียนชุดข้อมูลทดสอบล้มเพราะ quota แล้วเงียบสนิท — ไล่ผิดทางอยู่พักหนึ่ง)
แก้: คืนค่า `true` / `'slim'` / `false` และตั้งธง `window.__parmeStorageFull` + ยิงอีเวนต์ `paruay-storage-full`
→ `OfflineBanner` ขึ้นแถบแดงเข้ม 💾 บนสุด (มาก่อนแถบออฟไลน์ เพราะร้ายแรงกว่า) ครบ 4 ภาษา
**verified ด้วยการฉีดความผิดพลาด** (ทำให้ setItem ของคีย์ `paruay_tx` โยน QuotaExceededError) แล้วกดบันทึกจากหน้าจอจริง
⇒ แถบขึ้นจริง; และรอบที่เขียนสำเร็จแถบไม่ขึ้น (ไม่มี false positive)
**ยังเหลือ:** `savePosSalesStore` และเพื่อนๆ ยังกลืนเงียบเหมือนเดิม — ยกมาเป็นงานถัดไป

### 6. 🔴 สไปรต์ไพ่ 1.27 MB ขวางทางเปิดแอปของทุกคน
`<svg id="pcards">` ที่บรรทัด 392 (หัว `<body>` ก่อนสคริปต์ทุกก้อน) = **1,268,893 ตัวอักษร = 25% ของทั้งไฟล์**
(gzip 529,025 · brotli 477,120) และถูกใช้ **ที่เดียวในไฟล์**: มินิเกมไพ่ (`#vpc_<ดอก>_<แต้ม>` บรรทัด ~32444)
เบราว์เซอร์ต้องโหลดจนจบก้อนนี้ก่อนจึงจะเริ่ม parse React/แอปได้ ⇒ ย้ายไปท้าย `<body>` (ไม่ลดขนาดไฟล์ แต่ย้ายออกจากทางวิกฤต)
**วัดจริง: ไบต์ที่ต้องโหลดก่อนแอปเริ่มรัน 1,484,296 → 1,006,512 (brotli) = 72 วิ → 49 วิ ที่ 20KB/s**
**verified ว่าเกมไม่พัง:** วาด `<use xlink:href="#vpc_spade_ace">` แบบเดียวกับที่เกมทำ → getBBox 222×311 และเห็นรูปโพดำจริงในภาพหน้าจอ

### 7. brotli สำหรับหน้าเว็บ (nginx:alpine ไม่มีโมดูล brotli)
ย้ายการบีบอัดไปชั้น Traefik (`static-compress@file`) + **ปิด `gzip` ที่ nginx** (ไม่ปิด Traefik จะไม่แตะเลย)
**วัดจริงบน prod: 1,797,994 → 1,680,728 ไบต์ (−6.5%)**
⚠️ แลกมาด้วย: client ที่รับได้แค่ gzip ได้ 2,014,547 ไบต์ (แย่กว่า nginx เดิม 12%) — gzip ของ Traefik อ่อนกว่า nginx
   ยอมรับได้เพราะเบราว์เซอร์ทุกตัวตั้งแต่ปี 2016 รองรับ br บน https (แอปเนทีฟใช้ WebView = Chrome/WebKit)
สำรอง: `/docker/parme-prod/{docker-compose.yml,default.conf}.bak-brotli-<วันเวลา>`

### สิ่งที่วัดแล้วพบว่า **ไม่ใช่** ปัญหา (จะได้ไม่ไปแก้ผิดที่)
* localStorage ที่ 20,000 รายการ: เขียนทั้งก้อน **6 ms** · อ่านกลับ **8.5 ms** — ขนาดไม่ใช่ปัญหาความเร็ว แต่เป็นปัญหา *เพดาน* (ข้อ 5)
* หน้าแรก/กราฟ/รายงาน/POS ที่ 20,000 รายการ + 20,000 บิล: **long task 0 ms** ทุกหน้า DOM 1,075–2,352 โหนด — มีขอบเขตอยู่แล้ว
* 304 revalidation ของ index.html ทำงานอยู่ ⇒ เปิดซ้ำไม่ได้โหลด 1.7 MB ใหม่ (จ่ายเฉพาะตอนติดตั้งครั้งแรกและตอนขึ้นเวอร์ชันใหม่)

### 🔴 ที่ยังไม่ได้แก้ (มีหลักฐานแล้ว รอตัดสินใจ/รอบหน้า)
1. **base64 ฝังในไฟล์ 630,988 ไบต์** = gzip 473,701 — GIF สอนติดตั้ง iOS (211,509) · PNG ยันต์ 5 แบบ (181,156) · webp สินค้าตัวอย่าง (62,731)
   แยกเป็นไฟล์ต่างหาก + โหลดตอนใช้ = ลดได้อีก ~26% **แต่ชนกฎเหล็ก "ไฟล์เดียว"** ⇒ เป็นการตัดสินใจของเจ้าของ
2. `loadCloudData` ยัง full pull ทุกครั้งที่เปิดแอป (ตอนนี้ไม่ปิดกั้นจอแล้ว + บีบอัดแล้ว = 5,000 รายการ ≈ 6 วิเบื้องหลัง) — ทำเป็น incremental ได้อีก
3. `select('*')` ลากคอลัมน์ `photos` (รูป base64 ในแถว, เพดาน 180,000 ตัวอักษร/รูป) มาทุกครั้ง — ควรแยกรูปออกจาก query ปกติ
4. poll ทุก 9 วิ ยิง ~11 คำขอ · handler realtime หลายตัวดึงทั้งตารางใหม่ต่อ 1 อีเวนต์ · `friendships.select('*')` ไม่มี where
5. POS: ปิดบิล 1 ใบ = เขียน localStorage ~12 ครั้ง · อัปโหลดแคตตาล็อกทั้งร้านพร้อมรูปทุกการขาย · canvas 768×4800 สองใบต่อบิล
6. `supabase-js` จาก jsdelivr เป็นสคริปต์ **บล็อก parser** (ไม่มี defer/async) — CDN ช้า = จอขาว
7. ไม่มีการตัดข้อมูลเก่าเลยสักคอลเลกชัน (ยกเว้น stock moves 300) · ไม่มี IndexedDB (เพดาน localStorage คือ ~5 MB บน iOS)
8. `AbortSignal.timeout(90000)` = เน็ตอืดแต่ไม่ขาด ผู้ใช้รอได้ถึง 90 วินาที

APK = versionCode 30)
v3.7.421 (2026-07-27 — **FCM เปิดใช้จริงแล้ว — verified ว่าเด้งตอนแอปถูกฆ่าโปรเซส** (เจ้าของ: "คุณเข้าไป Browser ผมทำเองเลยได้ไหม?" → เลือก "ทำให้ทั้งหมดเลย" ⇒ ผมตั้ง Firebase ให้ทั้งหมดผ่านเบราว์เซอร์)
ต่อจาก v3.7.420 ที่โค้ดครบแต่ยังไม่มีคอนฟิก. งานรอบนี้เป็นงานตั้งค่า+พิสูจน์ ไม่ใช่งานฟีเจอร์ — `index.html` แทบไม่แตะ:
* **Firebase** โปรเจกต์ `parme` (id `parme-afd8f`, number `613605190518`) แผน Spark ฟรี, ปิด Google Analytics + Gemini in Firebase (ไม่จำเป็นกับ FCM และเก็บข้อมูลน้อยลง). แอป Android package `me.parme.pos` — `google-services.json` วางที่ `android/app/` แล้ว, service account key อยู่ที่ `~/fcm-service-account.json` (chmod 600, **ไม่เคยพิมพ์ค่าออกมาในแชท**)
* **VPS** ตั้ง `FCM_SERVICE_ACCOUNT` ใน `/docker/supabase/docker/.env` (สำรอง `.env.bak-fcm-20260727`) แล้ว `docker compose up -d --wait functions`
* **edge function** แก้ข้อความ error ให้แยก "ไม่ได้ตั้ง secret" ออกจาก "ตั้งแล้วแต่ parse ไม่ได้" (`fcmSaErr`) — เพราะตอนติดตั้งจริงค่าถูก shell กินจนพัง แล้วข้อความเดิมบอกว่า "no FCM_SERVICE_ACCOUNT" ทำให้ไล่ผิดทางอยู่พักหนึ่ง. เก็บซอร์สเข้า repo แล้วที่ `supabase/functions/send-pos-push/index.ts` (เดิมมีแต่บน VPS)
* Gradle ไม่ต้องแก้อะไรจริงตามที่คาดไว้ — มีไฟล์ = plugin apply เอง (ยืนยันด้วยการหา `parme-afd8f` + `613605190518` ใน `resources.arsc` ของ APK ที่บิลด์ออกมา = เจอทั้งคู่)

**verified ปลายทางจริง (ไม่ใช่แค่ "โค้ดถูก"):** ฆ่าโปรเซสแอปจนเหลือ 0 process แล้วยิง edge function ⇒ ตอบ `{"fcm":{"sent":1,"pruned":0,"total":1}}` และ Android โพสต์ notification จริง `Parme • now / 🛎 ออเดอร์ใหม่ / โต๊ะ 7 · 3 รายการ` (`tag=pos-order-7`, channel `fcm_fallback_notification_channel`) — ตรวจด้วย `dumpsys notification --noredact` ไม่ใช่ดูหน้าจอเฉยๆ. อ่าน token ออกจากเครื่องโดยให้แอปวาดเป็น QR แล้ว decode จากภาพหน้าจอด้วย `BarcodeDetector` (กันพิมพ์ผิด). ลบแถวทดสอบใน `fcm_tokens` ทิ้งแล้ว
**⚠️ กับดักการทดสอบที่เจอ:** `adb shell am force-stop` ใช้ทดสอบ FCM **ไม่ได้** — Android จะใส่แอปในสถานะ *stopped* แล้วบล็อกข้อความทั้งหมด (รอบแรกได้ `sent:1` แต่ไม่มีอะไรเด้ง แล้วผมเกือบสรุปว่าพัง). ต้องใช้ `am kill` ซึ่งเลียนแบบระบบฆ่าเพราะแรมไม่พอ
**ยังไม่ได้:** iOS ต้องมีบัญชี Apple Developer ($99/ปี) + APNs key อัปโหลดเข้า Firebase ก่อน. APK = versionCode 29)
v3.7.420 (2026-07-27 — **FCM push: แจ้งเตือนได้แม้ปิดแอป — โค้ดครบทุกชั้นแล้ว รอไฟล์จาก Firebase ของเจ้าของ** (เจ้าของ: "ให้ทำ FCM ต่อ").
ต่อจาก v3.7.419 ที่ LocalNotifications แก้ได้แค่ตอนโปรเซสแอปยังมีชีวิต. ทำครบทั้ง 4 ชั้น:
* **client** `@capacitor/push-notifications@8.1.2` + `__pushPlugin()` / `getFcmToken()` / `enableFcm()` / `disableFcm()`. `getFcmToken` ห่อ event `registration` / `registrationError` เป็น promise พร้อมเพดานเวลา 15 วิ (ไม่มี google-services.json = ยิง registrationError กลับมา). สวิตช์ "🔔 แจ้งเตือนเข้ามือถือ" ในหน้าตั้งค่าเปลี่ยนเงื่อนไขจาก `pushSupported` เป็น `pushSupported || fcmSupported` แล้วส่งต่อไปกลไกที่ถูกตัว (แอปเนทีฟ → FCM, เบราว์เซอร์ → Web Push เดิม) ⇒ **แถวนี้ไม่เคยโผล่ในแอปเลยมาก่อน** เพราะ `pushSupported` ต้องมี `PushManager` ซึ่งแอปไม่มี
* **DB** ตารางใหม่ `public.fcm_tokens` (token UNIQUE, shop_id, platform) + RLS ลอกแบบ `push_subscriptions` เป๊ะ — ไม่ยัดลงตารางเดิมเพราะรูปนั้นเป็นของ Web Push (`p256dh`/`auth` NOT NULL) ยัดต้องใส่ค่าปลอม. SQL: `tools/migrations/2026-07-27-fcm-tokens.sql` (apply บน VPS แล้ว)
* **edge function** `send-pos-push` ส่งสองช่อง: Web Push (เดิม) แล้วต่อด้วย FCM HTTP v1. legacy FCM (server key) ปิดไปกลางปี 2024 ⇒ ต้องแลก JWT ของ service account เป็น OAuth token ก่อน (เซ็น RS256 ด้วย WebCrypto `importKey(pkcs8)` → `oauth2.googleapis.com/token`, cache token 55 นาที). ส่งเป็น **notification message** ไม่ใช่ data-only ⇒ Android โชว์เองตอนพื้นหลัง/ปิดแอป และ**ไม่**โชว์ตอนแอปเปิด (ตอนเปิดมี realtime + LocalNotifications เตือนอยู่แล้ว) ⇒ แยกหน้าที่ ไม่เตือนซ้ำ. ลบ token ที่ตอบ 404/403 ทิ้งกันสะสม
* **compose** เพิ่มช่อง `FCM_SERVICE_ACCOUNT: ${FCM_SERVICE_ACCOUNT:-}` ให้ service `functions` แล้ว (สำรอง `docker-compose.yml.bak-fcm-20260727`)

**ออกแบบให้ "พร้อมแต่ยังไม่เปิด" ทุกชั้น — verified ทุกข้อ:**
* บิลด์ Android **ผ่าน** ทั้งที่ยังไม่มี `google-services.json` (exit=0) เพราะปลั๊กอินแค่เพิ่ม dependency `firebase-messaging` ไม่ได้ apply google-services plugin เอง — ตัว apply อยู่ใน template ของ Capacitor แบบมีเงื่อนไข (`if (servicesJSON.text)`) และ classpath `com.google.gms:google-services:4.4.4` มีอยู่แล้วใน root build.gradle ⇒ **Gradle ไม่ต้องแก้อะไรเลย**
* แอปเปิดปกติไม่แครช logcat มีแค่ warning `Default FirebaseApp failed to initialize because no default options were found` (ตามคาด)
* edge function ที่ deploy แล้วตอบ `{"webpush":"no subs","fcm":{"skipped":"no FCM_SERVICE_ACCOUNT"}}` = ช่องแรกยังเดิน ช่องสองข้ามเงียบ
* **พิสูจน์ว่าช่อง Web Push ไม่ถดถอย** ด้วยการ diff บล็อกส่งกับตัวสำรอง: เหมือนกันทุกตัวอักษร ต่างแค่ย่อหน้า
* เว็บไม่ถดถอย: ในเบราว์เซอร์ `Capacitor` undefined ⇒ `fcmSupported=false` แถวตั้งค่ายังโชว์และยังใช้ Web Push, console ไม่มี error

**⚠️ พฤติกรรมที่เปลี่ยนของ edge function (เปิดเผยตามจริง):** (1) เดิม `if (!subs.length) return 'no subs'` **ตัด early return ออก** ไม่งั้น FCM ไม่มีโอกาสได้รันเมื่อไม่มี web sub; (2) response เปลี่ยนจาก text (`ok`/`no subs`) เป็น **JSON สรุปผลสองช่อง** — ตรวจแล้วว่าไม่มีใครอ่าน body (trigger ใช้ `net.http_post` แล้วทิ้งผล); (3) บล็อก web push ถูกห่อ try/catch ของตัวเอง ⇒ ถ้าช่องนั้นพัง FCM ยังทำงานต่อ (เดิม error จะเด้งออกทั้งฟังก์ชัน)

**🔴 ที่เหลือต้องเป็นเจ้าของ (ผมทำแทนไม่ได้ — ต้องล็อกอินบัญชี Google):** สร้างโปรเจกต์ Firebase (ฟรี แผน Spark พอ) → เพิ่มแอป Android package `me.parme.pos` (**ไม่ต้องใส่ SHA-1** นั่นของ Google Sign-In) → โหลด `google-services.json` วางที่ `~/parme-app/android/app/` → Service accounts → Generate new private key → เซฟไว้ที่ `~/fcm-service-account.json` **(ห้ามวางเนื้อไฟล์ในแชท — มี private key; บอกแค่ว่าวางแล้ว)**. ขั้นตอนละเอียดอยู่ใน `~/parme-app/README.md` หัวข้อ "🔔 แจ้งเตือนออเดอร์ — สองชั้น". **iOS ยังทำไม่ได้** ต้องมีบัญชี Apple Developer + APNs key. **ยังไม่ได้ทดสอบการส่ง FCM จริง** (ไม่มี service account) — ที่ทดสอบแล้วคือเส้นทาง "ไม่มี secret ⇒ ข้ามอย่างปลอดภัย" และช่อง Web Push ไม่ถดถอย. APK = versionCode 28)
v3.7.419 (2026-07-26 — **แจ้งเตือนแบบเนทีฟ + อุดต้นตอ "realtime ในแอปไม่ทำงาน"** (เจ้าของ: "ระบบ real time ในแอปมือถือไม่ทำงาน ไม่มีการแจ้งเตือน ให้แจ้งเตือนแบบ Notification").
**สืบสวนก่อนแก้ — สมมติฐานแรกของผมผิด:** ผมเดาว่า origin `https://localhost` ทำให้ WS ถูกบล็อก. ทดสอบ handshake จริงไป `wss://api.parme.me/realtime/v1/websocket` ด้วย `Origin: https://localhost` และ `capacitor://localhost` ⇒ ได้ **101 Switching Protocols** ทั้งคู่เหมือน parme.me ⇒ **transport ไม่ใช่ปัญหา** ตัดทิ้ง. โพรบในแอป Android จริงยืนยันอีกชั้น: broadcast channel และ postgres_changes channel ได้ **SUBSCRIBED** ทั้งคู่, `isConnected=true`.

**(1) 🔴 ต้นตอจริงของ "realtime ไม่ทำงาน": ออเดอร์ QR ไม่มีอะไรตามเก็บเมื่อ socket หลุด.** `pollForUpdates` ไม่แตะ `pos_qr_orders` เลย (poll มีแต่ ious/loans/friends/transactions/trips) และ effect ของ channel `qro_<shopId>` ดึงออเดอร์ **ครั้งเดียวตอน mount** — callback `.subscribe()` ทำแค่ `setAuth` ไม่ refetch, และไม่มี `visibilitychange`/`online` ⇒ ทุกครั้งที่ socket หลุด (workflow อ่าน Kong log ของเครื่องเจ้าของ: ต่อใหม่ทุก **30–60 วิ** ตอนแอปอยู่ background) ออเดอร์ที่เข้ามาช่วงนั้น **หายถาวร** ไม่มีเสียง ไม่มี toast ไม่มี badge จนกว่าจะ remount. **แก้ตามแบบแผนที่ไฟล์นี้ทำถูกอยู่แล้วที่ channel `pob_` (pos_open_bills)**: แยกเป็น `pullQrOrders()` แล้วเรียกซ้ำตอน `SUBSCRIBED` / `visibilitychange`→visible / `online` (คอมเมนต์เดิมของ pob_ เขียนไว้ตรงๆ ว่า "realtime ไม่การันตีว่าจะได้ event ครบหลังหลุด/ตื่นจาก background → ดึงซ้ำเมื่อกลับมา").
**กันเตือนซ้ำ (จำเป็นเพราะการดึงซ้ำ):** เพิ่ม `qrAlertedRef` (Set ของ order id ต่อเซสชัน, ล้างเมื่อเกิน 500) — state ยังอัปเดตทุกครั้ง แต่ **เสียง/toast/notification ยิงครั้งเดียวต่อออเดอร์**.

**(2) 🔴 ทำไม "ไม่มีการแจ้งเตือน" — WebView ไม่มี API แจ้งเตือนเลย (วัดในแอปจริง):** `Notification = undefined`, `PushManager = undefined`, service worker ลงทะเบียนได้ (`scope=https://localhost/`) แต่ `registration.pushManager` **ไม่มี** ⇒ ทั้ง Web Notification และ Web Push ที่ทำไว้ตั้งแต่ v3.7.74 **ยิงในแอปเนทีฟไม่ได้เลย** และ `pushSupported` (index.html) ก็ปิดตัวเองเงียบๆ. เพิ่ม `@capacitor/local-notifications@8.2.1` + helper `notifyNative(title, body)` / `ensureNotifyPerm()` ต่อเข้า 3 จุดที่แอปเตือนออเดอร์อยู่แล้ว (เรียกพนักงาน/ขอเช็คบิล · ออเดอร์ทางไกล · ออเดอร์โต๊ะ) โดย **ใช้สตริง i18n เดิมทั้งหมด ไม่คิดคำใหม่**. `POST_NOTIFICATIONS` มีใน manifest อยู่แล้วตั้งแต่ก่อน. ขอสิทธิ์ตอน effect ของ QR เริ่มทำงาน (`qrEnabled`) ไม่ใช่ตอนเปิดแอปที่ยังอยู่หน้าล็อกอิน.

**verified rung-3 บนเครื่องจริงทั้งหมด:**
* กล่องขอสิทธิ์ Android เด้งจริง ("Allow Parme to send you notifications?") · `display=prompt` → หลังกด Allow `POST_NOTIFICATIONS: granted=true`
* แจ้งเตือนโพสต์เข้าระบบจริง: `dumpsys notification` ⇒ `pkg=me.parme.pos id=1 channel=default` `android.title=(🛎 ອໍເດີໃໝ່)` `android.text=(ໂຕະ 1 (+2))` และ **เห็นบนแถบแจ้งเตือนจริง** (ภาพหน้าจอ: Parme • now)
* การดึงซ้ำ: นับการเรียก `pullQrOrders` แบบแยกที่มา ⇒ `mount` ×1, `SUBSCRIBED` ×1, `visible` ×1, `online` ×1 (เดิม 2 event หลังไม่มีผลเลย)
* **guard กันเตือนซ้ำพิสูจน์ว่าล้มเหลวได้**: เปิด guard ส่งออเดอร์เดิม 3 ครั้ง ⇒ เตือน **1** ครั้ง (ออเดอร์ใหม่ ⇒ 2); ปิด guard ⇒ เตือน **3** ครั้ง = เทสต์จับได้จริง ไม่ใช่เทสต์ที่เขียวตลอด

**⚠️ ข้อจำกัดที่ยังอยู่ (ไม่ได้แก้ และแก้ด้วยวิธีนี้ไม่ได้):** LocalNotifications เด้งได้เฉพาะตอน **โปรเซสแอปยังมีชีวิต** (เปิดอยู่ หรือเพิ่งสลับไปแอปอื่น) — ถ้าผู้ใช้ปิดแอปทิ้ง/ระบบฆ่าโปรเซส จะไม่มีอะไรเด้งเลย. เคสนั้นต้อง push จริง (FCM) = ต้องมีโปรเจกต์ Firebase + `google-services.json` + `@capacitor/push-notifications` + แก้ edge function ให้ยิง FCM. ฝั่งเซิร์ฟเวอร์พร้อมแล้ว (trigger `trg_notify_new_pos_order` + edge function `send-pos-push` ยิงจริงและได้ HTTP 200 กลับมา แต่ตอบ "no subs" เพราะ `push_subscriptions` มีแถวเดียวเป็น endpoint `web.push.apple.com` จาก PWA เมื่อ 2026-06-16 — แอป Capacitor สมัคร subscription ไม่ได้เพราะไม่มี PushManager). APK = versionCode 27)
v3.7.418 (2026-07-26 — **ป้ายแท็บโต๊ะเป็น "ໂຕະ / โต๊ะ" เสมอ** (เจ้าของสั่ง "อยากให้เปลี่ยนจาก รายชื่อ เป็นคำว่า โต๊ะ เสมอ").
เดิมป้ายสลับอัตโนมัติ: `label: hasSeatNames ? 'ລາຍຊື່' : t.pos_tab_tables` ⇒ **พอมีการตั้งชื่อช่องแม้ช่องเดียว** (`hasSeatNames` = มี key ใดใน `seatNames` ที่ไม่ว่าง) ป้ายจะกลายเป็น "ລາຍຊື່" ทั้งร้านและทุกเครื่อง เพราะชื่อช่องเก็บใน `pos_shops.table_names` บนคลาวด์ด้วย (มาตั้งแต่ commit `2f790d4` 2026-06-28 "POS: name tables as people"). แก้เป็น `label: t.pos_tab_tables || 'Tables'` ตายตัว — คำแปลมีครบ 4 ภาษาอยู่แล้ว (ໂຕະ/โต๊ะ/Tables/餐桌).
**ไม่แตะชื่อช่อง**: `seatLabel(n)` ยังคืนชื่อที่ตั้งไว้ตามเดิม ⇒ การ์ดยังโชว์ชื่อ, หัวโมดัล QR ยังเป็น "QR · <ชื่อ>", บนบิล/การ์ดที่พิมพ์ก็ยังใช้ชื่อ — เปลี่ยนแค่ป้ายแท็บอย่างเดียว. `k: 'tables'` (คีย์แท็บ) ไม่เปลี่ยน ⇒ ตรรกะ `tab === 'tables'` ที่อื่นไม่กระทบ.
ลบ `hasSeatNames` ออกด้วยเพราะหลังแก้เหลือ 0 ที่ใช้ (grep ยืนยันว่ามี 2 จุดคือนิยาม + ป้ายนี้เท่านั้น) — ไม่ปล่อยโค้ดตายที่ตัวเองทำให้เกิด.
**verified fail→pass บนเบราว์เซอร์จริงด้วยเงื่อนไขเดิมที่ทำให้สลับ** (ตั้ง `paruay_pos_seatnames_local = {"1":"ກິ"}` เหมือนข้อมูลจริงของเจ้าของ, viewport 375×812):
* **ก่อนแก้** (v3.7.417 จาก `git show HEAD:index.html`): เจอแท็บ `ລາຍຊື່` และ **ไม่เจอ** `ໂຕະ` = รีโปรได้
* **หลังแก้**: เจอ `ໂຕະ` และ **ไม่เจอ** `ລາຍຊື່` · ชื่อ "ກິ" ยังแสดงบนการ์ดช่อง 1 (ยืนยันด้วยภาพหน้าจอ) ✓
(รายงาน `innerWidth` มาในผลวัดทุกครั้งตามกติกาที่จดไว้ตอน v3.7.416 — รอบแรกเจอ 0×0 อีก แต่การเช็คนี้เป็นการหาข้อความ ไม่ใช่เรขาคณิต จึงยังใช้ได้ แล้วตั้ง viewport ใหม่ก่อนถ่ายภาพ). APK = versionCode 26)
v3.7.417 (2026-07-26 — **🔴 QR โต๊ะที่สร้างจากแอปมือถือสแกนไม่ได้เลย เพราะเป็น `https://localhost/?qro=...`** (เจ้าของส่งภาพจากเครื่องจริง). **บั๊กจริงที่กระทบผู้ใช้ทุกคนที่ใช้แอปฝังไฟล์ตั้งแต่ v3.7.395** และการ์ดที่พิมพ์ตั้งแต่ v3.7.413 ก็ตายด้วย.
**เหตุ:** `qrUrlFor` สร้าง URL จาก `window.location.origin + pathname` — บนเว็บได้ `https://parme.me/` ถูกต้อง แต่ในแอปแบบฝังไฟล์ origin คือ `https://localhost` (Android) / `capacitor://localhost` (iOS) ⇒ QR ชี้กลับมาที่ตัวเครื่องพนักงานเอง ลูกค้าสแกนแล้วเปิดไม่ได้.
**แก้:** เพิ่ม `publicBaseUrl()` ระดับโมดูล (วางข้าง `__isNativeApp`) — ตัดสินจาก **origin จริง** ไม่ใช่จาก Capacitor (APK รุ่นโหลดสดก่อน v3.7.395 มี origin เป็น parme.me อยู่แล้ว ไม่ต้องแทน; self-host โดเมนอื่น/GitHub Pages fallback ที่ pathname มีความหมายก็ยังใช้ของตัวเอง): ถ้า protocol เป็น `capacitor:`/`file:` หรือ hostname เป็น localhost/127.0.0.1/::1 ⇒ คืน `https://parme.me/`; นอกนั้นคืน `location.origin + pathname` **ซึ่งเป็นนิพจน์เดิมเป๊ะ ⇒ ฝั่งเว็บไม่มีทางถดถอย**. ใช้กับ 2 จุดที่ส่ง URL ออกไปให้เครื่องอื่นเปิด: `qrUrlFor` (QR โต๊ะ — ครอบทั้ง QR บนจอ, ข้อความ URL ใต้ QR, และการ์ดที่พิมพ์ เพราะ `printTableQr` เรียกตัวนี้) และ `shopOrderUrl` (ลิงก์/QR สั่งทางไกล `?shop=`).
**ตรวจแล้วว่าไม่ต้องแตะ auth:** `redirectTo` ของลืมรหัส/ยืนยันอีเมล/เปลี่ยนอีเมล (3 จุด) ก็ส่ง `https://localhost/` เหมือนกัน แต่ VPS `.env` มี `ADDITIONAL_REDIRECT_URLS=https://parme.me,https://www.parme.me,me.parme.pos://**` — **ไม่มี localhost** ⇒ GoTrue ปฏิเสธแล้ว fallback ไป `SITE_URL=https://parme.me` เอง ลิงก์ในอีเมลจึงใช้งานได้อยู่แล้ว (กลไกเดียวกับที่ทำให้ OAuth พังตอน v3.7.395). ไม่แก้ = ไม่เพิ่ม blast radius.
**verified บนแอป Android จริง** (บิลด์ probe แล้วอ่านค่าจากในแอป): `origin=https://localhost/` แต่ `publicBaseUrl()=https://parme.me/` และตัวอย่าง QR = `https://parme.me/?qro=SHOP.1.tok12345` ✓ + ในเบราว์เซอร์ที่ localhost:8081 (origin ที่เครื่องอื่นเข้าไม่ถึงเหมือนกัน) `qrUrlFor(1)` คืน `https://parme.me/?qro=.1.9ojjq7iq` ✓
**🔴 บทเรียนที่ต้องจำ — fidelity ไม่ใช่ correctness:** ตอน v3.7.413 ผมทดสอบการ์ดที่พิมพ์ด้วยการ **ถอดรหัส QR จริง** แล้วรายงานว่า `decodeMatchesUrl: true` ผ่าน — ค่าที่ถอดได้คือ `http://localhost:8081/__qrtest.html?qro=...` **ผมเห็นคำว่า localhost อยู่ในผลทดสอบของตัวเองแล้วไม่สะดุด** เพราะตั้งเกณฑ์ผิด: ไปตรวจว่า "QR ตรงกับ URL ที่แอปแสดง" ซึ่งเป็นการเทียบกับ implementation ของตัวเอง ไม่ใช่ตรวจว่า "ลูกค้าสแกนแล้วเปิดได้ไหม" ซึ่งคือเป้าหมายจริง. ⇒ เกณฑ์ผ่านของ QR/ลิงก์ที่ส่งออกไปข้างนอก ต้องเป็น **"URL นี้เปิดได้จากเครื่องอื่น"** (โฮสต์เป็นโดเมนสาธารณะ) ไม่ใช่ "ตรงกับที่แอปคิด". APK = versionCode 25)
v3.7.416 (2026-07-26 — **ปุ่ม "สร้าง QR ใหม่" ถามยืนยันก่อน** (เจ้าของสั่ง "กันกดพลาด") — เก็บข้อ 2 ที่รายงานค้างไว้.
ปุ่มนี้เดิมยิงทันทีที่แตะ ทั้งที่ผลคือ QR บนจอ **และการ์ดที่พิมพ์แจกลูกค้าไปแล้ว** ตายทันที ต้องพิมพ์ใบใหม่ — ขณะที่ปุ่มหมุน token ของ remote ordering (~37237) ถามยืนยันมาตั้งแต่แรก และ `askConfirm` ถูกใช้ครอบ action ที่ทำลายของทั่วทั้งไฟล์ (ปิดบิล/ย้าย-รวมโต๊ะ ฯลฯ) ⇒ ปุ่มนี้แหกบรรทัดฐานของไฟล์เอง. ใช้ `askConfirm(T(...4 ภาษา...), () => newQrCode(qrModal), { danger: true, confirmLabel: T('ສ້າງໃໝ່', ...) })` ตามสำนวนเดิมของไฟล์ — `danger: true` ให้ปุ่มยืนยันเป็นสีแดง (`--pv-danger`) เหมือน action ที่จบของที่กำลังใช้งานอยู่.
**ตรวจสมมติฐานที่แบกน้ำหนักก่อนเขียน:** `ConfirmDialog` ของ POS เรนเดอร์ที่ **zIndex 10090** สูงกว่าโมดัล QR (10040) จึงซ้อนขึ้นมาบนสุดได้จริง (ถ้าต่ำกว่า กล่องจะไปอยู่หลังโมดัล = กดไม่ได้ ปุ่มจะเหมือนเสีย).
**verified rung-3 บน UI จริง** (ทำนายผลก่อนรัน): แตะปุ่มเฉยๆ → กล่องขึ้น **token ไม่เปลี่ยน rpc=0** · กด "ຍົກເລີກ" → token ยังเท่าเดิม rpc=0 และกล่องปิด · กด "ສ້າງໃໝ່" → token เปลี่ยนเป็นตัวใหม่ rpc=1 · ปุ่มยืนยันสี `rgb(170,60,40)` = `--pv-danger` ✓ · ที่ viewport 375×812 ปุ่มในกล่องกว้าง 135/138px สูง 48px และ `elementFromPoint` กลางปุ่มคืนตัวปุ่มเอง = **ผู้ใช้แตะได้จริง** ส่วนปุ่มของโมดัล QR ที่อยู่ข้างหลังถูกบังถูกต้อง.
**กับดักเครื่องมือวัดที่เจอซ้ำรอบนี้ (จดไว้กันพลาดอีก):** (1) ตัววัดแรกใช้ `getComputedStyle(d).zIndex === '10080'` หาไม่เจอเลยแม้ตอนกล่องเปิดอยู่ = **checker ที่ไม่เคยยิงบวก** (ค่าจริงคือ 10090 คนละ ConfirmDialog) — แก้โดยหา element จากข้อความ + ตรวจว่า `position: fixed` แล้วยืนยันด้วย control ว่างก่อนเปิด/ไม่ว่างหลังเปิด. (2) เช็ค "กล่องปิดแล้วหรือยัง" ด้วยการค้นสตริงทั้งหน้า ให้ผลบวกลวง (สตริงไปโผล่ที่อื่น). (3) วัดขนาดปุ่มแล้วได้ 34×140px + `elementFromPoint` คืน null — **พรีวิวถูกย่อจนเหลือ 0×0** (กับดักเดียวกับที่จดไว้ตอน v3.7.406) เกือบสรุปว่าข้อความยืนยันที่ยาวทำเลย์เอาต์พัง; ตั้ง viewport เป็น 375×812 แล้ววัดใหม่จึงได้ค่าที่ถูก ⇒ **เช็ค `innerWidth` ก่อนเชื่อตัวเลขทุกครั้ง**. APK = versionCode 24)
v3.7.415 (2026-07-26 — **เปิดโมดัล QR ต้อง "อ่าน DB ก่อนเขียน" — เลิกเขียนทับ token ที่เครื่องอื่นเพิ่งหมุน** (เจ้าของสั่งเก็บข้อ 1 ที่ผมรายงานค้างไว้ตอน v3.7.414).
**บั๊ก:** `openQrModal` เขียน token ที่อยู่ในเครื่องลง DB **ทุกครั้งที่เปิดโมดัล** โดยไม่ดูว่า DB มีค่าใหม่กว่าไหม และ RPC `set_table_token` เป็น `jsonb ||` ทับตรงๆ ไม่มี compare-and-swap ⇒ เครื่องที่ถือ token ค้าง (effect ดึง `table_tokens` มี deps `[qrEnabled, shopId]` = ดึงครั้งเดียวตอนเปิดแอป และไม่มี realtime บน `pos_shops`) เขียนทับ token ที่เครื่องอื่นเพิ่งหมุน ⇒ **QR/การ์ดที่เพิ่งแจกลูกค้าไปตายเงียบๆ** และเครื่องนั้นก็โชว์ QR ที่ลูกค้าใช้ไม่ได้อยู่ดี.
**ทางแก้ — read-before-write ที่ไม่ทำให้ของเราหายเอง:** ปัญหาคือ "local เก่าหรือใหม่กว่า DB" แยกจากค่า token เฉยๆ ไม่ได้ (ไม่มี version/timestamp) — ถ้ารับค่า DB มาดื้อๆ จะเจอเคสกลับด้าน: เช็คบิลตอนเน็ตล่ม (หมุนแล้วเขียนไม่ติด) แล้วเปิดโมดัลตอนเน็ตกลับมา จะไป**ปลุก token เก่าที่ตั้งใจฆ่าให้ฟื้น**. จึงเพิ่ม `pendingTokRef` — `persistTokens` ทำเครื่องหมายโต๊ะที่ "หมุนแล้วเขียน DB ไม่ติด" ไว้ (และลบทิ้งเมื่อเขียนสำเร็จ) ⇒ ใช้เป็นหลักฐานว่า local ใหม่กว่าจริง. ตรรกะใหม่ของ `openQrModal`: อ่าน `table_tokens` ของโต๊ะนั้นจาก DB ก่อน → **ถ้า DB มีอยู่แล้วและเราไม่มี pending = รับของ DB มาโชว์ ไม่เขียนทับ** (อัปเดตเฉพาะในเครื่องด้วย `persistTokens(next, null)` ซึ่งเป็นสำนวนเดิมของไฟล์ที่ข้าม RPC); ถ้ามี pending = ของเราใหม่กว่า ดันขึ้น DB; ถ้า DB ยังไม่มี = mint แล้วเขียน; **อ่าน DB ไม่ได้ (เน็ตล่ม) = ถอยไปพฤติกรรมเดิม** ไม่ให้ร้านออฟไลน์แย่ลงกว่าก่อน.
`pendingTokRef` เก็บในหน่วยความจำอย่างเดียว **ไม่เก็บข้ามการเปิดแอป** โดยตั้งใจ — เพราะ effect merge ที่ ~35467 เป็น "DB ชนะต่อ key" อยู่แล้ว ถ้าเก็บข้ามเซสชันสองกลไกจะขัดกันเอง (ผลข้างเคียงที่ยังอยู่: หมุนตอนออฟไลน์แล้วปิดแอปก่อนเน็ตกลับ = การหมุนนั้นหาย ซึ่งเป็นพฤติกรรมเดิมไม่ใช่ของใหม่).
**verified rung-3 fail→pass ด้วย DB ปลอมที่คุมค่าได้ + นับจำนวนครั้งที่เรียก RPC จริง** (ทำนายผลก่อนรันทุกเคส):
* **ก่อนแก้ (v3.7.414)**: DB=`TTTT2222` local=`aaaa1111` → เปิดโมดัลแล้ว **เขียนทับด้วย `aaaa1111` (rpc=1)** = รีโปรบั๊กได้
* **หลังแก้ A**: เคสเดียวกัน → local กลายเป็น `TTTT2222` และ **rpc=0** (ไม่เขียนเลย) sync=ok
* **B** local=`LLLL3333` + pending → เขียน `LLLL3333` ขึ้น DB (rpc=1) และล้าง pending ✓
* **C** อ่าน DB ไม่ได้ → ถอยไปเขียนตามเดิม (rpc=1) ✓  · **D** DB ยังไม่มี token ของโต๊ะนั้น → mint 8 ตัวแล้วเขียน (rpc=1) ✓
**บทเรียนเรื่องเครื่องมือวัด:** รอบแรกผมเช็ค `qrSync` ด้วยการค้นสตริง "ยังไม่พร้อม" ทั้งหน้า แล้วได้ผลว่า fail ทั้งที่ควรเป็น ok — สตริงไปแมตช์ที่อื่นในหน้า. เปลี่ยนมาวัดจาก `disabled` ของปุ่มพิมพ์ซึ่งผูกกับ `qrSync !== 'ok'` ตรงๆ จึงได้ค่าที่ถูก. APK = versionCode 23)
v3.7.414 (2026-07-26 — **ยกเลิกโต๊ะ/ปิดบิลโดยไม่มีการขาย ก็หมุน token ของโต๊ะด้วย** (เจ้าของสั่งหลังผมตรวจแล้วรายงานว่าเป็นช่องว่างจริง).
**ที่มา:** เจ้าของถามซ้ำว่าปุ่ม "สร้าง QR ใหม่" ยังจำเป็นไหม เพราะเชื่อว่า "เปิดปิดโต๊ะก็สร้าง QR ใหม่อยู่แล้ว". ตรวจโค้ดจริง + workflow 3 เลนส์ × 26 เอเจนต์ (ยืนยัน 15 / ตกไป 11) แล้วพบว่า **ครึ่งเดียวถูก**: `rotateToken` ถูกเรียกแค่ 3 จุด (เช็คบิลสำเร็จ ~35146 · ย้ายโต๊ะ · รวมโต๊ะ) — `openTable` ไม่แตะ token เลย และ `openQrModal` ใช้ `cur[n] || genTok()` = ใช้ตัวเดิม (**verified บน UI จริง**: เปิดโต๊ะ 1 แล้ว token ยังเป็น `aaaa1111` เท่าเดิม). ที่สำคัญกว่า: `closeBill` ซึ่งเป็นทาง "จบเซสชันโต๊ะโดยไม่มีการขาย" **ไม่หมุน token** ⇒ ยกเลิกโต๊ะแล้วลูกค้าคนเก่ายังสั่งเข้าโต๊ะของลูกค้าคนถัดไปได้ (RLS เทียบ token ตรงตัวเท่านั้น และคำสั่งบริการ `__CALL__`/`__BILL__` ถูกรับอัตโนมัติที่ ~35425 โดยไม่ผ่านการยืนยันของพนักงาน).
**หมายเหตุน่าสนใจ:** ไฟล์ migration `tools/migrations/2026-07-09-qr-session-token.sql` บรรทัดที่ 2 บันทึกสเปกเดิมของเจ้าของว่า "เช็คบิลสำเร็จ → QR ตายทันที; **เปิดโต๊ะใหม่**/กดสร้าง QR ใหม่ = token ใหม่" — ฝั่ง "เปิดโต๊ะใหม่" ไม่เคยถูกเขียนลงโค้ด ความเข้าใจของเจ้าของจึงตรงกับสเปก แต่โค้ดทำไม่ครบ. รอบนี้ปิดช่องด้วยการหมุนที่ "ปิด" แทนที่ "เปิด" (ได้ผลเท่ากันแต่ไม่ทำให้ QR ที่โชว์ค้างอยู่ตายกลางทาง).
**แก้จุดเดียวคุมทั้งคลาส:** `closeBill` (~34740) อ่านบิลจาก `billsLiveRef` (เหตุผลเดียวกับคอมเมนต์ v3.7.364 — callback ค้างใน state) แล้วเรียก `rotateToken(bl.table)` **นอก** updater ของ `setBills` (กัน side effect รันซ้ำถ้า React เรียก updater มากกว่าหนึ่งครั้ง) ⇒ ครอบทั้ง 2 call site คือปุ่ม ✕ "ยกเลิกโต๊ะ" (~36386) และ "ปิดบิลนี้?" (~38193) โดยไม่ต้องแตะทั้งสองที่. ไล่เส้นทางลบบิลอื่นครบแล้ว: `finishActiveBill` ถูกเรียกจาก `completeSale` เท่านั้นซึ่งหมุนไปก่อนแล้ว (ไม่หมุนซ้ำ), `setBills(filter)` ที่เหลือคือ sync จากเครื่องอื่น (เครื่องนั้นหมุนเอง — ถ้าหมุนซ้ำจะฆ่า QR ที่เครื่องนั้นเพิ่งออก) และบิล `__edit__` ที่ `table: null`.
**verified rung-3 fail→pass ด้วยสคริปต์เดียวกันบน UI เดียวกัน** (ขับปุ่มจริง: แตะการ์ดโต๊ะ → กด ຕົກລົງ → แตะ ✕ → กด ປິດ):
* ไฟล์ **ก่อนแก้** (v3.7.413 จาก `git show HEAD:index.html`): ยกเลิกโต๊ะแล้ว token ค้างที่ `aaaa1111` = **รีโปรบั๊กได้**
* ไฟล์ **หลังแก้**: `aaaa1111` → `k0wilmf4` (สุ่มใหม่ 8 ตัว) และเขียนลง localStorage แล้ว · **โต๊ะ 2 ไม่ถูกแตะ** (`bbbb2222` เท่าเดิม) · โต๊ะกลับเป็น "ວ່າງ"
(ทดสอบในสำเนาชั่วคราวที่ปลด guard `qrEnabled` เฉพาะบรรทัดใหม่ เพราะโหมด demo ไม่มีร้าน — ตัว guard เป็นสำเนาแบบแผนเดิมจาก ~35146; call site ที่สองยืนยันด้วยการอ่านโค้ดว่าเรียกฟังก์ชันเดียวกัน ไม่ได้ขับ UI)
**แก้ข้อความให้ตรงกับพฤติกรรมใหม่ 2 จุด ครบ 4 ภาษา** (ของเดิมกลายเป็นข้อมูลผิดทันทีที่แก้โค้ด): คำอธิบายใต้ QR ในโมดัล "...ทุกครั้งที่เช็คบิล" → "...ทุกครั้งที่เช็คบิล**หรือยกเลิกโต๊ะ**" และบรรทัดอายุบนการ์ดที่พิมพ์ "ใช้ได้ถึงเช็คบิลของโต๊ะนี้" → "ใช้ได้ถึงเช็คบิล**หรือยกเลิกโต๊ะ**นี้".
**ปุ่ม "สร้าง QR ใหม่" ยังเก็บไว้** — หน้าที่เหลือคือสวิตช์ฉุกเฉิน 2 เคสที่การหมุนอัตโนมัติไม่ครอบ: (1) QR รั่วกลางบิลที่ยังเปิดอยู่ (ลูกค้าถ่ายรูป/เอาการ์ดกลับบ้าน) (2) เครื่องอื่นถือ token ค้าง — effect ดึง `table_tokens` มี deps `[qrEnabled, shopId]` = ดึงครั้งเดียวตอนเปิดแอป ไม่มี realtime บน `pos_shops` และ `openQrModal` จะเขียน token ค้างทับของใหม่ได้ เพราะ RPC `set_table_token` เป็น `jsonb ||` ทับตรงๆ ไม่มี compare-and-swap.
**ค้างไว้ (ยังไม่แก้ — ของเดิม):** `openQrModal` ควรมี guard ไม่ให้เขียน token เก่าทับค่าที่ใหม่กว่าใน DB; ปุ่ม "สร้าง QR ใหม่" ไม่มี `askConfirm` ต่างจากปุ่มหมุน token ของ remote ordering ที่มี; ลด/เพิ่มจำนวนโต๊ะไม่ล้าง token ของโต๊ะที่หายไป (`savePosTableCount` เขียนแค่ `table_count`). APK = versionCode 22)
v3.7.413 (2026-07-26 — **ปุ่ม "🖨️ พิมพ์ QR" ในโมดัล QR ของโต๊ะ** (เจ้าของสั่ง "ทำปุ่ม Print QR ให้ด้วย เพื่อปริ้นไปให้ลูกค้าสแกน").
`printTableQr(n)` ใช้แบบแผนเดิมของการพิมพ์บิล (iframe ซ่อน + `doc.write` + `@page size:80mm auto`) ⇒ ใช้เครื่องพิมพ์บิล 80mm ที่ร้านมีอยู่แล้ว ไม่ต้องมีเครื่องพิมพ์เพิ่ม. การ์ดมี: ชื่อร้าน (ถ้ามี) · ชื่อ/เลขโต๊ะตัวใหญ่ · "สแกนเพื่อสั่งอาหาร" · QR 62mm · URL ตัวเล็กเผื่อ QR เสีย · บรรทัดบอกอายุการใช้งาน · เส้นประสำหรับตัด — ครบ 4 ภาษา และ escape ชื่อร้าน/ชื่อโต๊ะก่อนต่อเป็น HTML.
**เม็ด QR ใหญ่กว่าบนจอ** (`createDataURL(10, 8)` เทียบกับจอที่ใช้ 6) เพราะกระดาษความร้อน 203dpi ต้องการเม็ดหยาบจึงสแกนติด — ได้ภาพ 346×346px บนพื้นที่ 62mm.
**ปุ่มถูกปิดเมื่อ `qrSync !== 'ok'`** — RLS เช็ค token กับ DB ⇒ ถ้า token ยังไม่ถึง DB การ์ดที่พิมพ์ออกไปสแกนสั่งไม่ได้ (เสียกระดาษ + ลูกค้าสับสน).
**verified rung-3 (ไม่ใช่ดูด้วยตา):** เปิดโมดัลจริงในเบราว์เซอร์ (hook `setQrModal` ในสำเนาทดสอบชั่วคราวที่ลบทิ้งแล้ว) → กดปุ่มพิมพ์ → ดัก HTML ที่เขียนลง iframe → **ถอดรหัส QR ที่พิมพ์ด้วย `BarcodeDetector` แล้วได้ URL ตรงกับ `qrUrlFor(n)` เป๊ะ** (`decodeMatchesUrl: true`) + เรนเดอร์การ์ดที่ 303px (=80mm @96dpi) ดูเลย์เอาต์ + แถวปุ่มไม่ล้น (137/140/284px สูง 44–47px ที่ maxWidth 320) + ทดสอบ gate: `qrSync='fail'` → `disabled=true` และกดแล้ว **ไม่พิมพ์**, `'ok'` → พิมพ์.
**แก้ข้อความอธิบายใต้ QR** จาก "อย่าพิมพ์ติดโต๊ะ" → "โชว์จากในแอป หรือพิมพ์เป็นการ์ดวางโต๊ะต่อรอบลูกค้า (ไม่ใช่สติกเกอร์ติดถาวร)" ครบ 4 ภาษา เพราะตอนนี้พิมพ์ได้แล้ว แต่ token ยังหมุนตอนเช็คบิล/ย้ายโต๊ะ/รวมโต๊ะ (v3.7.263) ⇒ **การ์ดที่พิมพ์ตายเมื่อเช็คบิล** จึงต้องบอกให้ถูก ไม่ใช่ปล่อยให้เข้าใจผิด.

**คำตอบเรื่อง "ปุ่มสร้าง QR ใหม่ยังจำเป็นไหม" (ตรวจจากโค้ดจริง — ยังจำเป็น อย่าลบ):** การหมุนอัตโนมัติครอบแค่ 3 จังหวะ — เช็คบิล (บรรทัด ~35138), ย้ายโต๊ะ (~35261), รวมโต๊ะ (~35280) — ปุ่มนี้ครอบอีก 2 เคสที่การหมุนอัตโนมัติไม่แตะ: (1) **ทางกู้เดียวใน UI เมื่อ token เขียนลง DB ไม่ติด** — `qrSync='fail'` และ UI พิมพ์บอกตรงๆ ว่า "ຍັງບໍ່ພ້ອມ — ກົດສ້າງ QR ໃໝ່"; `persistTokens` retry 3 ครั้งแล้วยังล้มได้ (เน็ตร้านอ่อน) ถ้าลบปุ่มนี้ = โต๊ะนั้นสั่ง QR ไม่ได้เลยจนกว่าจะเช็คบิล (2) **ฆ่า QR ที่รั่วกลางเซสชัน** — ลูกค้าถ่ายรูป QR ไป/การ์ดถูกเอากลับบ้าน แล้วสั่งเข้าบิลโต๊ะที่ยังเปิดอยู่ ⇒ หมุนได้ทันทีโดยไม่ต้องปิดบิล. ยิ่งมีปุ่มพิมพ์แล้วเคส (2) ยิ่งเกิดง่ายขึ้น ⇒ ปุ่มนี้ **สำคัญขึ้น** ไม่ใช่ลดลง. APK = versionCode 21)
v3.7.412 (2026-07-26 — **เก็บ 3 งานค้างจาก v3.7.411 (เจ้าของสั่ง "เอาตามที่คุณว่าเลย") + แก้ regression ที่ตัวเองสร้างในรอบเดียวกัน**.

**(1) ปุ่มดาวน์โหลด APK ในหน้า admin ใช้ path สัมพัทธ์** (มาตั้งแต่ v3.7.285) — `'/parme.apk'` ในแอปเนทีฟ resolve เป็น `https://localhost/parme.apk` (Android) / `capacitor://localhost/parme.apk` (iOS) ซึ่งไม่มีไฟล์ ⇒ กดแล้วเงียบ. แก้เป็น URL เต็ม `https://parme.me/...` ทั้งสองปุ่ม — Android: `Bridge.launchIntent` ตัดสินจาก **host** ไม่ใช่ `target` (อ่านซอร์ส: ถ้าโฮสต์/สคีมไม่ตรงกับ appUrl และไม่อยู่ใน `allowNavigation` → `Intent.ACTION_VIEW` = เปิดเบราว์เซอร์ระบบ) จึงไม่ต้องเติม `target=_blank` และ **บนเว็บยังเป็น same-origin ⇒ `download` ทำงานเหมือนเดิมเป๊ะ ไม่มีการเปลี่ยนพฤติกรรมฝั่งเว็บ**. Capacitor Android ไม่ได้ลงทะเบียน `DownloadListener` และไม่เปิด `setSupportMultipleWindows` ⇒ คลิกกลายเป็น navigation ปกติที่เข้า `shouldOverrideUrlLoading` แน่นอน.
**🔴 regression ที่ตัวเองสร้างและจับได้จาก review**: บน **iOS** การเปลี่ยนเป็น URL เต็มทำให้พฤติกรรมเปลี่ยนจาก "กดแล้วเงียบ" เป็น "ถูก Capacitor เตะออก Safari แล้วโหลด APK 5 MB ที่ iPhone ติดตั้งไม่ได้" (`WebViewDelegationHandler`: `!isApplicationNavigation && toplevelNavigation` → `UIApplication.shared.open` + `.cancel`; iOS ไม่มี `WKDownloadDelegate` เลย) — และรอบนี้เองเป็นรอบที่เริ่มปล่อย iOS จริง (ข้อ 3). แก้ด้วย `__isIosNative()` gate ทั้งสองปุ่ม ใช้มาตรฐานเดียวกับ `UpdateBanner` ที่ gate `getPlatform() !== 'android'` ไว้แล้ว. ปุ่มนี้เห็นเฉพาะ `profiles.is_admin` จึงวงจำกัด แต่เป็นผลเสียจริงและเกิดจากบรรทัดที่รอบนี้แก้เอง.

**(2) `tools/release.sh` เขียนใหม่ทั้งไฟล์** — ของเดิมบรรทัดสุดท้ายบอกให้ push แล้วรอ GitHub Pages ซึ่ง **ไม่ใช่ prod ตั้งแต่ v3.7.249** และไม่รู้จัก `parme.apk` / `app-version.json` ที่แอปเนทีฟต้องใช้ตั้งแต่ v3.7.411 ⇒ ใครเชื่อมันจะคิดว่า deploy แล้วทั้งที่เว็บยังเวอร์ชันเก่า. ตัวใหม่: bump → check → **พิมพ์ทั้ง 5 ขั้นพร้อมค่าที่กรอกไว้แล้ว** (อ่าน `versionCode`/`versionName` จาก `build.gradle` มาเทียบให้, เตือนถ้ายังไม่ bump และบอกเลขถัดไป รวมทั้ง `MARKETING_VERSION`/`CURRENT_PROJECT_VERSION` ของ iOS), คงเจตนาเดิมคือ **ไม่ deploy/ไม่ push ให้เอง**. 2 จุดที่ review จับได้แล้วแก้: (ก) ขั้นที่ 3 เดิมพิมพ์ `scp` APK แล้วพิมพ์ `printf` app-version.json แยกกันโดยไม่มีอะไรผูกเลขที่ประกาศกับไฟล์ที่อัปจริง ⇒ ถ้า build/scp ล้มแต่รันคำสั่งถัดไป จะประกาศเวอร์ชันที่ไม่มีอยู่บนเซิร์ฟเวอร์ (แอปทุกเครื่องเด้งแบนเนอร์ไปโหลด APK เก่า) — แก้เป็น **ร้อยด้วย `&&` ทั้งเส้น + อ่าน versionCode/versionName จาก `output-metadata.json` ของ APK ที่ build ออกมาจริง** ไม่พิมพ์มือ + เพิ่มขั้นตรวจ `curl` app-version.json และเทียบ sha256 ของ APK ในเครื่องกับบนเซิร์ฟเวอร์. (ข) ธงตรวจ `sw.js` ใช้ `git diff --name-only` ซึ่ง **ตาบอดถ้าเจ้าของ `git add` ไปแล้ว** (ซึ่งเป็นสถานะที่สคริปต์เองแนะนำในขั้นที่ 2) ⇒ เปลี่ยนเป็น `git status --porcelain -- sw.js`.

**(3) เวอร์ชัน iOS** — `MARKETING_VERSION` 1.0 → 3.7.412 และ `CURRENT_PROJECT_VERSION` 1 → 20 ครบทั้ง Debug และ Release configuration (`Info.plist` อ้างผ่านตัวแปรอยู่แล้ว จึงไม่ต้องแตะ). **verified ด้วยการ build จริง** ไม่ใช่แค่แก้ไฟล์: `xcodebuild -configuration Release` ผ่าน และ `Info.plist` ในไบนารีที่ออกมาให้ `CFBundleShortVersionString = 3.7.412`, `CFBundleVersion = 20`.

**กระบวนการ**: review 3 เลนส์ × 16 เอเจนต์ (ยืนยัน 4 / ตกไป 12) — ที่ตกไปเช่น "www/index.html ค้างเวอร์ชันเก่า" (เป็น build artifact ที่ gitignore) และ "ปุ่ม APK ควรซ่อนบน iOS อยู่แล้วก่อนรอบนี้" (เดลต้าบนเว็บ/PWA = ศูนย์). APK = versionCode 20)
v3.7.411 (2026-07-26 — **แก้ของ v3.7.410 เองหลัง adversarial review 4 เลนส์ × 25 เอเจนต์ (ยืนยัน 10 / ตกไป 11) — 410 ยัง *ไม่ได้* แก้บั๊กที่เจ้าของรายงานบนเครื่องจริง และแถมทำให้ POS ขายของไม่ได้**.

**(1) 🔴 แถบสถานะ: 410 เดาจากเวอร์ชัน Android ซึ่งเป็นแกนที่ผิด** — ตัวชี้ขาดว่า "ใครเป็นเจ้าของพื้นแถบ" ไม่ใช่เวอร์ชัน OS แต่เป็น **เวอร์ชัน WebView**: `@capacitor/android` `SystemBars.java:41` `WEBVIEW_VERSION_WITH_SAFE_AREA_FIX = 140` และ `:200` `shouldPassthroughInsets = getWebViewMajorVersion() >= 140 && hasViewportCover` (เรามี `viewport-fit=cover` ที่บรรทัด 5) ⇒ **WebView ≥140 = Capacitor ส่ง inset ทะลุ ไม่ใส่ padding ให้เว็บวิว** ⇒ หน้าเพจของเราวาดใต้แถบสถานะ พื้นแถบคือสีของเราเอง. ที่วัดได้ `inset = 0` ตอน 410 เป็นเพราะ **อีมูเลเตอร์ตัวที่ใช้มี WebView 124** (ตรวจด้วย `dumpsys package com.google.android.webview`) — ผลวัดนั้นจึง generalize ไม่ได้ และบนเครื่องจริงปี 2026 (WebView ≥140) + ดาร์กโหมด **บั๊กเดิมยังอยู่ครบ**: 410 สั่ง `DEFAULT` → ระบบเลือก DARK ตามธีมเครื่อง → ไอคอนขาวบนพื้นครีม #FAF6EE ≈ 1.08:1. อีก 2 รูรั่วของ 410: userAgent แบบ reduced รายงาน "Android 10" (หลุดเข้าสาขา ≤14 แล้วบังคับสไตล์ทั้งที่ setBackgroundColor เงียบ) และ `__pageColor()` อ่านแค่ `--pv-page` ทั้งที่ **หัว POS (`linear-gradient` เขียวเข้ม z-index 200) วาดทับเขตแถบ** ⇒ บน iOS ได้ไอคอนดำบนเขียวเข้ม 2.0–2.7:1 ตลอดเวลาที่อยู่หน้า POS.
**ทางแก้ 411 — เลิกเดา วัดเอาจากของจริง**: `__barSurface()` ใช้ `elementsFromPoint(กลางจอ, 2)` ไล่จากชั้นบนสุด เอาตัวแรกที่ **อยู่เหนือแถบคลุม** (`__aboveScrim`: มี ancestor fixed/sticky ที่ z-index > 40 — แถบคลุม `body::before` คือ 40) และดึงสีพื้นทึบ (`__bgColorOf` รองรับ gradient ด้วย เพราะ gradient ทำให้ `backgroundColor` โปร่งใส); ไม่เจอ = เห็นแถบคลุม ⇒ `--pv-page`. จากนั้น `applyStatusBar()` วัด `env(safe-area-inset-top)` จริงจาก `getComputedStyle(body,'::before').height`: **inset > 0 หรืออยู่บน iOS ⇒ พื้นเป็นของเรา สั่งสไตล์ตามความสว่างเอง; inset = 0 บน Android ⇒ ระบบวาดแถบเอง ปล่อย `DEFAULT` ห้ามแตะ**. ตัด `setBackgroundColor` + การ parse เวอร์ชันจาก userAgent ทิ้งทั้งหมด (no-op บนเครื่องสมัยใหม่อยู่แล้ว) และวัดซ้ำทุก 1.2 วิ ตอนหน้าจอไม่ได้ถูกซ่อน เพราะสิ่งที่วาดตรงแถบเปลี่ยนได้ตลอด (เปิด POS / แถบเตือนโผล่ / สลับธีม) — เรียกปลั๊กอินเฉพาะตอนค่าเปลี่ยนจริง.

**(2) 🔴 แบนเนอร์อัปเดตของ 410 ทับปุ่ม "ບັນທຶກການຂາຍ" ของ POS 100% แล้วกินคลิก** — การ์ดลอยล่างจอ z-index 99998 อยู่เหนือ overlay แทบทุกตัว (POS 200, modal 7000–10090, หน้าล็อก PIN 99998) ⇒ แคชเชียร์กดบันทึกบิลแล้วเด้งไปโหลด APK แทน = ขายไม่ได้ทั้งร้าน จนกว่าจะหาปุ่ม ✕ 30px เจอ; หน้าล็อก PIN ก็โดนแบบเดียวกัน (กด "ลืม PIN?" ไม่ได้ = ออกจากหน้าล็อกไม่ได้เลย). **รีโปรแล้วในเบราว์เซอร์จริง**: `elementFromPoint` กลางปุ่มบันทึกคืนตัวแบนเนอร์ `reachable: false`. **ทางแก้: ย้ายไปใช้แบบเดียวกับแถบออฟไลน์** — ตรึงบนสุดและ **จองพื้นที่ผ่าน `--paruay-banner-h`** ซึ่ง overlay 15+ จุดทั้งแอปเลื่อน `top` ตามอยู่แล้วตั้งแต่ v3.7.244 ⇒ ไม่ทับอะไรเลยโดยไม่ต้องไล่แก้ทีละ overlay. เพิ่มตัวรวมความสูง `__bannerH`/`__syncBannerH()` (เดิม OfflineBanner เขียน var นั้นคนเดียว ถ้ามีสองแบนเนอร์จะเขียนทับกัน) + `--paruay-off-h` ให้แถบอัปเดตเกาะใต้แถบออฟไลน์ด้วย CSS ล้วน. **verified fail→pass**: จำลองการ์ดแบบ 410 → ปุ่มบันทึก `blockedByBanner: true, reachable: false`; แบบ 411 → `reachable: true` และหัว POS เริ่มที่ y=51 = ความสูงแบนเนอร์พอดี ไม่ทับกัน.

**(3) เช็กอัปเดตครั้งเดียวตอน mount** — ร้านที่เปิดแอปตอนเน็ตยังไม่มา จะไม่เช็กอีกเลยจนกว่าจะปิดแอป. เพิ่ม timeout 8 วิ (`AbortSignal.timeout`) + ลองใหม่ตอน `resume` ของแอปและ event `online`, และกัน `versionCode` ที่ไม่ใช่ตัวเลข (`isFinite`) ไม่งั้น `NaN <= NaN` เป็น false = เตือนทุกคนรวมทั้งคนที่อัปเดตแล้ว. + `role/tabIndex/onKeyDown/aria-label` ให้แบนเนอร์.

**(4) เอกสาร**: CLAUDE.md §4 Deploy เพิ่มขั้นที่ 4–5 (อัป `parme.apk` + เขียน `app-version.json`) พร้อมคำเตือนว่า `tools/release.sh` ยังไม่รู้จักขั้นเหล่านี้และยังพิมพ์ URL ของ GitHub Pages ที่ไม่ใช่ prod แล้ว.

**ยังไม่แก้ (นอกขอบเขตรอบนี้ — ของเดิมก่อนหน้า):** ปุ่ม "ดาวน์โหลด APK" ในหน้า admin (บรรทัด ~9701, v3.7.285) ใช้ path สัมพัทธ์ `/parme.apk` ⇒ ในแอปเนทีฟชี้ไป `https://localhost/parme.apk` = ไม่มีไฟล์ (บนเว็บยังปกติ); `tools/release.sh` เอง; iOS `MARKETING_VERSION` ยังเป็น 1.0. APK = versionCode 19)
v3.7.410 (2026-07-26 — **แถบสถานะอัตโนมัติ (ไม่ให้สีทับซ้อนกัน) + แจ้งเตือนอัปเดตในแอป Android** (เจ้าของสั่งสองข้อพร้อมกัน: "ที่มองไม่เห็น เป็นเฉพาะ Android บางรุ่น หาวิธีทำแบบ Automatic ได้ไหม?" และ "ถ้ามีอัปเดตใหม่ ให้แจ้งเตือนบนแอปและกดที่แอปอัปเดตได้เลย").

**(1) แถบสถานะ — กลไกต่างกัน 3 แบบ ต้องปฏิบัติคนละอย่าง** (อ่านซอร์ส `@capacitor/status-bar` 8.0.3 + วัดบนเครื่องจริงทั้งสองแพลตฟอร์ม):
* **iOS** — เว็บวิววาดทะลุใต้แถบสถานะ (`env(safe-area-inset-top)` > 0) ⇒ **พื้นหลังแถบคือสีหน้าเพจของเรา** แต่ระบบเลือกสไตล์ตามดาร์กโหมด*ของเครื่อง* ⇒ เครื่องเปิดดาร์กโหมด + หน้าเพจสว่างเสมอ (เขียว #FAF6EE / ฟ้า #F0F2F5) = **ตัวหนังสือขาวบนขาว** → ต้องสั่งสไตล์เองตามความสว่างของ `--pv-page`
* **Android ≤14** — `setBackgroundColor` ยังทำงาน ⇒ ตั้งพื้นแถบ = สีหน้าเพจ (กลืนกันไม่มีรอยต่อ) แล้วสั่งสไตล์ตามสีนั้น
* **Android 15+** — บังคับ edge-to-edge: `setBackgroundColor` เป็น **no-op** (`StatusBar.java` → `shouldSetStatusBarColor`: API 35 ต้องมี `windowOptOutEdgeToEdgeEnforcement`, targetSdk 36 = ไม่ได้เลย) และวัดได้ว่า `env(safe-area-inset-top) = 0px` ⇒ **พื้นแถบเป็นของระบบ** ไล่ตามธีมเครื่องเอง ⇒ ต้องปล่อย `DEFAULT` **ห้ามสั่งเอง** — นี่คือจุดที่ v3.7.409 ทำผิดจนไอคอนหาย
โค้ด: `__pageColor()` อ่าน `--pv-page` (สีที่ตาเห็นจริงหลังแถบ ตัวเดียวกับ `body::before` v3.7.405), `__lum()` รับทั้ง hex และ `rgb()`, `applyStatusBar()` ไม่รับพารามิเตอร์อีกต่อไป (เดิมรับสีแบรนด์ = ต้นเหตุ) และย้ายการเรียกตอนบูตมาไว้ *หลัง* จุดประกาศ (แก้ TDZ ของ 409). `meta theme-color` เปลี่ยนจาก `#1B4332`/`#FFFFFF` เป็นสีหน้าเพจ `#FAF6EE`/`#F0F2F5` ⇒ PWA ไม่มีขาวล้วนให้เบราว์เซอร์เดาผิดอีก.
**verified rung-3 วัดเป็นตัวเลข (ไม่ใช่ดูด้วยตา)** — สุ่มสีเด่นของแถบเทียบกับพิกเซลที่ต่างจากพื้นมากที่สุด:
* Android 15 (API 35) โหมดสว่าง: พื้น (250,250,250) / ไอคอน (100,100,100) ต่าง 150 ✓ · โหมดมืด: พื้น (48,48,48) / ไอคอน (255,255,255) ต่าง 207 ✓
* **พิสูจน์ว่า `setStyle` มีผลจริงด้วยการ intervene**: บิลด์ทดสอบสั่ง `setStyle('DARK')` ตรงๆ บนโหมดสว่าง → ไอคอนกลายเป็นขาวจางบนพื้นสว่าง = **รีโปรบั๊กของ 409 ได้**
* iOS 26.5 (iPhone 17 Pro) **ตั้งเครื่องเป็นดาร์กโหมด**: ตัวหนังสือแถบสถานะเป็น (0,0,0) บนพื้น (255,255,255) ต่าง 255 ✓ (ก่อนแก้จะเป็นขาวบนขาว)
**⚠️ พบระหว่างทาง: `ios/App/CapApp-SPM/Package.swift` ไม่มี CapacitorStatusBar เลย** เพราะเคย `npx cap sync android` อย่างเดียวหลังเพิ่มปลั๊กอิน — ต้อง `npx cap sync ios` ด้วยทุกครั้งที่เพิ่ม/ถอดปลั๊กอิน ไม่งั้นฝั่ง iOS เงียบสนิทโดยไม่มี error

**(2) แจ้งเตือนอัปเดตในแอป (`UpdateBanner`)** — แอปแบบฝังไฟล์ไม่ได้อัปเดตเองเหมือน PWA. คอมโพเนนต์ใหม่วางคู่ `OfflineBanner`: ทำงาน**เฉพาะ Android เนทีฟ**, ดึง `https://parme.me/app-version.json` แล้วเทียบ `versionCode` กับ `App.getInfo().build`, แถบล่างจอ `bottom: calc(12px + env(safe-area-inset-bottom))` z-index 99998 — แตะ = เปิด URL ของ APK ในเบราว์เซอร์นอกแอป, ✕ = จำ versionCode ที่ข้ามไว้ใน `paruay_upd_skip`.
**🔴 ต้นเหตุที่มันเงียบตอนแรก — CORS ไม่ใช่ cleartext**: แอปฝังไฟล์มี origin `https://localhost` ⇒ อ่าน `parme.me/app-version.json` คือ **ข้ามโดเมน** แต่ nginx ไม่ได้ส่ง `Access-Control-Allow-Origin` → `TypeError: Failed to fetch` เงียบๆ (หลงคิดว่าเป็น cleartext เพราะเทสต์ครั้งแรกใช้ http). แก้ที่ VPS `/docker/parme-prod/default.conf` เพิ่ม `location = /app-version.json { add_header Cache-Control "no-cache"; add_header Access-Control-Allow-Origin "*"; }` (backup `default.conf.bak-cors-20260726`) แล้ว `nginx -t && nginx -s reload`.
**verified end-to-end บนอีมูเลเตอร์จริง**: ตั้งเซิร์ฟเวอร์เป็น 18 ขณะแอปเป็น 17 → แบนเนอร์ "ມີເວີຊັນໃໝ່ 3.7.410" ขึ้นจริง → แตะ → Chrome เปิด `parme.me/parme.apk` และขึ้นกล่องยืนยันดาวน์โหลด APK ตามปกติของ Chrome. **เคสลบ**: เซิร์ฟเวอร์ 18 = แอป 18 → ไม่มีแบนเนอร์ (วัดสีโซนแบนเนอร์ ไม่พบเขียว #1B4332).
**ขั้นตอนปล่อยเวอร์ชันใหม่เพิ่มมาอีกอย่าง**: นอกจาก rsync `index.html` ต้องอัป `parme.apk` + แก้ `app-version.json` ให้ตรงกับ `versionCode` ของ APK ด้วย ไม่งั้นผู้ใช้แอปเนทีฟไม่รู้ว่ามีของใหม่. APK = versionCode 18)
v3.7.409 (2026-07-25 — **กวาด safe-area รอบท้าย + งานที่ v3.7.410 มาแก้ต่อ**. เนื้อหาที่ยังอยู่: หน้าเวทีโป๊กเกอร์ `pkStage` เผื่อขอบนิรภัยจริงตอนหมุน 90°, และ **แจ้งเตือนอัปเดตในแอป Android** (`UpdateBanner` — ดูรายละเอียดที่ v3.7.410 ซึ่งเป็นตัวที่ทำให้ใช้ได้จริง). **ส่วนแถบสถานะของ 409 ผิด** — คำนวณความสว่างจาก *สีแบรนด์* (#1B4332 / #FFFFFF) ทั้งที่สิ่งที่อยู่หลังแถบจริงคือ *สีหน้าเพจ* ⇒ ธีมเขียวได้ `setStyle('DARK')` = ไอคอนขาวบนพื้นสว่าง = มองไม่เห็น (รีโปรได้บนอีมูเลเตอร์ API 35). อีกจุด: IIFE บูตเรียก `applyStatusBar` ก่อนบรรทัดที่ประกาศ (TDZ) เลยรอด setTimeout 800ms ตัวเดียว. แก้ทั้งสองที่ v3.7.410. APK = versionCode 17 ซึ่งถูกแทนที่แล้ว)
v3.7.408 (2026-07-25 — **แถบสถานะบน PWA มองไม่เห็น WiFi/แบตเตอรี่** (เจ้าของส่งภาพ: ขาวโล่ง). เหตุ: head ตั้ง `apple-mobile-web-app-status-bar-style="black-translucent"` ⇒ **iOS วาดไอคอน/ตัวหนังสือแถบสถานะเป็นสีขาว** ทับเนื้อหา แต่พื้นแอปเป็นสีอ่อนทั้งสองธีม (เขียว #FAF6EE / ฟ้า #F0F2F5) → ขาวบนขาว. แก้เป็น `content="default"` = ให้ระบบวาดแถบสถานะเองด้วยตัวหนังสือสีเข้ม และเนื้อหาไม่ไหลขึ้นไปใต้แถบ. **ขอบเขต: มีผลเฉพาะ PWA (เพิ่มลงหน้าโฮม) เท่านั้น** — แอปเนทีฟ Capacitor ไม่ใช้ meta นี้ (ไอคอนเข้มอยู่แล้ว ยืนยันจากภาพหน้าจอ simulator ก่อนหน้าทุกใบ) และ Android/เดสก์ท็อปไม่เกี่ยว. **verified rung-3 บน PWA จริง**: ติดตั้ง parme.me ลงหน้าโฮมของ iPhone 17 Pro simulator ผ่าน Safari → เปิดจากหน้าโฮม → **นาฬิกา/WiFi/แบตเตอรี่ เห็นชัดเจนเป็นสีเข้มบนพื้นครีม** เนื้อหาเริ่มใต้แถบสถานะ ไม่ทับกัน. **หมายเหตุ: `env(safe-area-inset-top)` ในโหมด default จะเป็น 0 ⇒ งาน safe-area v3.7.399-405 กลายเป็น no-op บน PWA แต่ยังจำเป็นสำหรับแอปเนทีฟซึ่งวาดใต้ Dynamic Island จริง** (พิสูจน์แล้วตอน v3.7.404). **กับดักตอนทดสอบ: เปิด PWA ครั้งแรกจอขาวนาน >2 นาที เพราะโหลด 4.9MB โดยยังไม่มีแคช SW — ไม่ใช่บั๊ก เปิดซ้ำขึ้นทันที อย่าด่วนสรุปว่าพัง**. APK = versionCode 16)
v3.7.407 (2026-07-25 — **ช่องวันที่บน iOS สูงเกินเพื่อน 2px — บั๊กที่ v3.7.406 ปล่อยไปเอง**. **iOS/WebKit ปฏิเสธ `box-sizing: border-box` กับ `input[type=date/time]` ที่ยังเป็นหน้าตา native** — `getComputedStyle` คืน `content-box` ทั้งที่เขียน `border-box !important` ⇒ เส้นขอบไปบวก *นอก* กรอบ ความสูงจริง = 38 + 2×ความหนาขอบ. **วัดเองบน iOS Simulator 26.5 (หน้าเทสต์แยก เปิดใน Safari ของ simulator):** `date` ขอบ 1px = **40.00** · ขอบ 1.5px = **40.66** · `text` = 38.00 · `select` = 38.00 · `date` + `appearance:none` = **38.00** (box-sizing กลับเป็น border-box). แก้ด้วยกฎเดียวคุมทุกจุด: `input[type=date/time/datetime-local/month/week] { -webkit-appearance: none; appearance: none; color: var(--pv-text) }` — ครอบ 8 จุดที่ยังไม่ได้กัน (12813, 12819, 27615, 29495, 30410, 30849, 31877, 37723; อีก 3 จุดกันไว้แล้วด้วย inline ตั้งแต่ก่อนหน้า) และกันจุดใหม่ในอนาคตด้วย. **ต้องตั้ง `color` คู่เสมอ** ไม่งั้น WebKit ใช้สีฟ้าระบบ; **ห้ามใส่ `!important` ที่ color** เพื่อให้ inline color ของแต่ละจุดยังชนะ (เช่นช่องนัดทดลองขับที่ใช้ `var(--pv-warn)`). **verified บน iPhone 17 Pro จริง**: ช่องวันที่สูงเท่าช่องอื่นแล้ว ตัวอักษรสีปกติ. **บทเรียน: การตรวจด้วยตาบนแอปจริงจับส่วนต่าง 2px ไม่ได้ — workflow ที่วัดเป็นตัวเลขจับได้ ส่วนผมยืนยันซ้ำเองด้วยหน้าเทสต์ที่คุมตัวแปร (ขอบ 1/1.5px + ชุดคุม) ก่อนเชื่อ**. APK = versionCode 15)
v3.7.406 (2026-07-25 — **ช่องกรอกทั้งแอปสูงเท่ากันหมด ลดลงราว 28% + ปุ่มฟอร์มสมดุลกับช่อง** (เจ้าของ). **วัดของเดิมก่อน** (เบราว์เซอร์ 390×844, demo): ความสูงจริงกระจาย **37/38/40/42/47/49/51/53px** เพราะแต่ละที่ตั้ง inline style เอง. **ทำเป็นกฎ CSS ตัวเดียวระดับทั้งแอป** (แบบแผนเดียวกับ v3.7.378 ที่เคยทำ POS): `input:not(checkbox/radio/file/color/range/hidden), select { height: 38px !important; padding-top/bottom: 0 !important; box-sizing: border-box !important }` — **38px = ลดจากช่องสูงสุด 53px ราว 28%** ตามที่ขอ และพอดีกับช่องกะทัดรัดที่มีอยู่เดิม. `textarea` คงหลายบรรทัด ลดแค่ padding (ไม่งั้นช่องบันทึกเหลือบรรทัดเดียว). **ปุ่ม:** `button[style*="width: 100%"] { min-height: 44px !important; padding-top/bottom: 0 }` — ปุ่มหลักของฟอร์ม 57→44px สูงกว่าช่องเล็กน้อยตามหลักที่ปุ่มหลักควรหนักกว่า และยังคงระยะแตะ 44px (WCAG 2.5.5); การ์ดนำทาง/tile สินค้า/ชิปหมวด **ไม่โดน** เพราะไม่ได้ตั้ง `width: 100%`. + **ปรับ POS จอใหญ่ 36→38px** ให้ตรงกับทั้งแอป (7 จุดในบล็อก @media ≥700px). **workflow 4 โซนช่วยจับสิ่งที่ทดสอบบนจอมือถือมองไม่เห็น:** (ก) `select { }` เปล่ามี specificity (0,0,1) แพ้ `.pv-pos-body select` (0,1,1) ⇒ ถ้าไม่แก้บล็อก POS ด้วย select ใน POS จะค้าง 36 ขณะที่ input เป็น 38; (ข) ช่องอีโมจิ POS ถูกบังคับจัตุรัส 36×36 จะกลายเป็น 36×38 ไม่เป็นจัตุรัส; (ค) ปุ่มข้างช่องใน POS ตรึง 36 จะไม่เท่าช่อง — ทั้งสามข้อหายไปด้วยการปรับ POS เป็น 38. **verified:** เบราว์เซอร์ 390 (เขียว+ฟ้า) และ 820 (POS: input/select/อีโมจิ/ปุ่มข้างช่อง = 38 หมด, อีโมจิ 38×38 จัตุรัส) + **iOS/WebKit จริงบน iPhone 17 Pro** (เอนจินคนละตัวกับที่ทดสอบ) + ไม่มีช่องไหนตัวหนังสือล้นกล่อง (เช็ค scrollHeight > clientHeight ทุกช่อง รวมช่องฟอนต์ 28px). **บทเรียนวัดผล: พรีวิวถูกย่อจนกว้าง 0 ทำให้ตัวเลขเพี้ยน (ปุ่มขึ้น 147px) — ต้องเช็ค innerWidth ก่อนเชื่อค่าที่วัดได้**. APK = versionCode 14)
v3.7.405 (2026-07-25 — **เลื่อนหน้าจอแล้ว UI บนสุดทับแถบสถานะ — แก้ให้ครบทุกธีม** (เจ้าของส่งภาพธีมฟ้า: โลโก้/ชิป/ปุ่ม ซ้อนกับนาฬิกา). **2 สาเหตุคนละแบบ ต้องแก้คนละทาง:** (1) **ธีมฟ้า** — กฎ v3.7.315 ทำให้ `header.fade-up` เป็น `position: sticky; top: 0` พอเลื่อนขึ้นแถบหัวจึงไปเกาะขอบบนสุด = ใต้ Dynamic Island พอดี. แก้เป็น `top: max(env(safe-area-inset-top), var(--paruay-banner-h, 0px))` (เกาะใต้ขอบนิรภัย และใต้แถบออฟไลน์ถ้ามี) + `header.fade-up::before` วาดพื้นขาว `position: fixed` คลุมเขตแถบสถานะ เนื้อหาจะได้ไม่วิ่งผ่านให้เห็นเป็นแถบโล่ง. (2) **ธีมเขียว (ค่าเริ่มต้น)** — ไม่มี sticky header เนื้อหาจึงเลื่อนผ่านใต้นาฬิกาตรงๆ (เห็นชัดตอนเส้นกราฟวิ่งทับตัวเลขนาฬิกา). แก้ด้วย **`body::before` แถบคลุมเขตแถบสถานะสำหรับทุกธีม** — `position: fixed; height: env(safe-area-inset-top); background: var(--pv-page); z-index: 40` (ต่ำกว่า sticky header ฟ้า 45 / แถบออฟไลน์ 99999 / POS 200 / เกม 300 ซึ่งจัดขอบเองอยู่แล้ว). **verified บน iPhone 17 Pro จริงทั้ง 2 ธีม** (สลับธีมผ่าน UI ในแอปแล้วเลื่อนจริง): ฟ้า → นาฬิกาอยู่บนพื้นขาว แถบหัวเกาะใต้ลงมา ไม่ทับ; เขียว → เนื้อหาวิ่งหลังแถบครีม ไม่ไปอยู่ใต้นาฬิกา. **Android ไม่เปลี่ยน** (inset = 0 → แถบสูง 0, ภาพเทียบเหมือนเดิมทุกจุด). APK = versionCode 13)
v3.7.404 (2026-07-25 — **หน้าล็อกอินถูก Dynamic Island บัง — เจอบน iPhone จริงครั้งแรก**. ติดตั้ง Xcode 26.6 เสร็จ → build โปรเจกต์ iOS (`~/parme-app/ios`, SPM ไม่ใช่ CocoaPods) → รันบน simulator iPhone 17 Pro **แล้วเห็นทันทีว่าโลโก้บนหน้าล็อกอินถูก Dynamic Island บังครึ่งใบ** (สิ่งที่ emulator Android มองไม่เห็นเพราะที่นั่น inset = 0 เสมอ). เหตุ: กล่องหน้าล็อกอิน + หน้ารอโหลด (บรรทัด ~20239/20300) เป็น `minHeight:100vh` + `justifyContent:center` + `padding:'32px 24px'` **ไม่มี env(safe-area-inset-*) เลย** — พอเนื้อหาสูงกว่าจอนิดเดียว ขอบบนจึงถูกดันไปใต้ status bar. แก้ทั้ง 2 จุดเป็น `calc(32px + env(safe-area-inset-top)) 24px calc(32px + env(safe-area-inset-bottom))`. **verified บน iPhone 17 Pro จริง**: ก่อนแก้โลโก้ถูกบัง หลังแก้เห็นเต็มใบพ้น Dynamic Island (ภาพก่อน/หลัง). **บทเรียน: การกวาด safe-area รอบ v3.7.400 ใช้เครื่องจำลองที่ผมทำเอง จับได้เฉพาะของที่ position fixed/sticky — หน้าล็อกอินเป็น flow ปกติจึงหลุด; พอมี Xcode แล้วต้องไล่ซ้ำบน simulator จริง**. APK = versionCode 12)

### สภาพแวดล้อม iOS (พร้อมแล้ว 2026-07-25)
Xcode 26.6 (`/Applications/Xcode.app`) · license ผ่าน · `xcode-select` ตั้งแล้ว · iOS 26.5 SDK + platform + simulator runtime ครบ · **โปรเจกต์ใช้ SPM ไม่ต้อง CocoaPods**
build พิสูจน์แล้ว: `xcodebuild -project App.xcodeproj -scheme App -configuration Release -sdk iphoneos -destination "generic/platform=iOS" CODE_SIGNING_ALLOWED=NO build` → **BUILD SUCCEEDED** (App.app 6.2MB)
**กับดักที่เจอตอนติดตั้ง:** `xcodebuild -downloadPlatform iOS` ล้มตอน Installing เพราะมีอิมเมจ simulator ซ้ำ (`Duplicate of ...`) — **ห้ามลบตัวซ้ำเฉยๆ** เพราะทั้งสองทะเบียนชี้ไฟล์ asset เดียวกัน ลบตัวหนึ่งแล้วอีกตัวกลายเป็นทะเบียนลอย (State: Ready แต่ mount path ไม่มีจริง → Xcode ฟ้อง "iOS 26.5 Platform Not Installed" ทั้งที่ SDK อยู่ครบ) วิธีที่ได้ผล: `xcrun simctl runtime delete <ทุกตัว>` ให้เหลือ 0 แล้ว `xcodebuild -downloadPlatform iOS` ใหม่
**ยังไม่ได้ตั้ง:** `CFBundleShortVersionString` ยังเป็น 1.0 / `CFBundleVersion` 1 (ต้องตั้งก่อนส่ง App Store)
v3.7.403 (2026-07-25 — **กดปุ่มลบรัวๆ กลายเป็นเพิ่มไม่รู้จบ** (เจ้าของ). **รีโปรได้จริงก่อนแก้** (เบราว์เซอร์, POS demo): เพิ่ม 1 ชิ้น แล้วกดปุ่มลบห่างกัน 90ms → `1→2→3→4→5→6→7`. **ต้นเหตุ:** `decGuarded` (v3.7.349) สาขาที่แปลง "แท็บลบ→เพิ่ม" เรียก `markAdd(p.id)` ซึ่ง **ต่ออายุหน้าต่าง 420ms ให้ตัวเอง** ⇒ ทุกแท็บถัดไปยังอยู่ในหน้าต่างเสมอ = แปลงเป็นเพิ่มไม่จบ. **แก้ 3 จุด ให้ guard ครอบเฉพาะจังหวะที่เป็นกับดักจริง (ปุ่มลบ "โผล่ขึ้นมาใต้นิ้ว" ตอน qty 0→1)**: (1) `decGuarded` แปลงได้ครั้งเดียวแล้ว **ปลดอาวุธ** (`lastAddRef.current[p.id] = 0`) ไม่ใช่ต่ออายุ; (2) tile onClick ติดอาวุธเฉพาะ `qty === 0` (ถ้า qty≥1 ปุ่มลบอยู่บนจอก่อนแท็บนี้แล้ว = กดโดนคือเจตนา); (3) แถวในแผงเลือกแบบ (ร้อน/เย็น/ปั่น) ใช้กติกาเดียวกัน `if (iq === 0)`. **verified 3 เคส:** A) เพิ่ม 5 พัก 600ms แล้วกดลบรัว → `5→4→3→2→1→0` ✓; B) regression v3.7.349 รัวแท็บมุมซ้ายบน (จุดที่ปุ่มลบโผล่ทับ) → `1→2→1→0` (แท็บที่ 2 ยังถูกกันไว้ตามเจตนาเดิม; แท็บ 3+ ถือเป็นเจตนาลบ เพราะปุ่มอยู่บนจอมาก่อนแล้ว); C) **ดีขึ้นเป็นของแถม** — มีของ 3 กดเพิ่มเป็น 4 แล้วรีบกดลบใน 80ms → ได้ 3 (เดิมได้ 5 เพราะ tile ติดอาวุธทุกแท็บ). APK = versionCode 11)
v3.7.402 (2026-07-25 — **ปุ่มลบบน tile POS: วงกลม → สี่เหลี่ยม "เข้ามุม"** (เจ้าของ). ทรง: มุมซ้ายบนโค้งเท่ามุม tile, อีกสามมุมเหลี่ยม ยกเว้นมุมด้านใน (ขวาล่าง) โค้งเล็กน้อย; ตัด `border: 2px solid var(--pv-modal)` ออก (ถ้าคงไว้จะเห็นเป็นเส้นซ้อนขอบ tile ตรงมุม) ใช้ `boxShadow: 2px 2px 6px` เฉียงเข้าด้านในแทน. **บทเรียนสำคัญ: กำหนดมุมแยก 4 ด้านผ่าน inline style ไม่ได้** — เบราว์เซอร์ยุบ longhand กลับเป็น shorthand ใน style attribute (`border-radius: 14px 0px 10px`) ทำให้กฎธีม fb ที่จับ substring `[style*="border-radius: 14px"]` มาทับจนกลายเป็น 8 ทั้งสี่มุม (จับได้ตอนวัด computed แล้วเทียบ 2 ธีม) ⇒ **ย้ายมุมไปไว้ใน CSS class `.pv-tile-del`** แล้ว inline ไม่มีคำ border-radius อีก กฎ fb จับไม่ได้: `.pv-tile-del { border-radius: 14px 0 10px 0 }` + `html[data-theme="fb"] .pv-tile-del { border-radius: 8px 0 8px 0 }` (fb ย่อ tile เป็น 8 ด้วยกฎ `button:has(img)` มุมปุ่มต้องตามให้ไม่ปลิ้นออกนอกโค้ง tile). **verified 2 ธีม:** เขียว tile 14 / ปุ่ม 14-0-10-0; fb tile 8 / ปุ่ม 8-0-8-0; ขนาดที่เห็น 34, พื้นที่กด 44, ชิดมุม tile ที่ (0,0) ทั้งคู่. + พฤติกรรมยังถูก (มุมนอกพื้นที่กด→ลด, กลาง tile→เพิ่ม, กลางปุ่ม→ลด). APK = versionCode 10)
v3.7.401 (2026-07-25 — **ปุ่มลบ (−) บน tile สินค้าใน POS เล็กเกินไป กดติดยาก** (เจ้าของ). วัดของเดิม: วงกลม 28×28 = 25% ของ tile (111×171 บนจอ 390) — ต่ำกว่าเกณฑ์ touch target 44px มาก. แก้เป็น **2 ชั้น**: ชั้นนอก = พื้นที่กดโปร่งใส 44×44 (WCAG 2.5.5 AAA) ที่ `top/left: -8`, ชั้นใน = วงกลมที่มองเห็น 34×34 `margin: 6` + **`flexShrink: 0`** (ไม่ใส่ flex จะบีบเหลือ 32 — เจอตอนวัด) → มุมวงกลมยังอยู่ตำแหน่งเดิมเป๊ะ (ชิดขอบ tile ตามที่ v3.7.354 ตั้งใจ) เพราะ -8 + margin 6 + border tile 2 = 0. **ยื่นออกนอก tile 8px ซึ่งพอดีช่องไฟ grid 10px → ไม่ทับ tile ข้างๆ (verified)**; กินเข้าใน tile 26→38px (guard 420ms ของ v3.7.349 ยังกันแท็บพลาดตอนรัวเพิ่ม). borderRadius 14→999 เพื่อให้ธีม fb คงความเป็นวงกลม (fb มีกฎ `[style*="border-radius: 999"]` คงวงกลมไว้) — เดิม fb remap 14→16 เป็นสี่เหลี่ยมมน. **verified พฤติกรรมจริงในเบราว์เซอร์:** จิ้มมุมนอกสุดของพื้นที่กดใหม่ → ลดจำนวน (เดิมจุดนั้นกดไม่ติด); จิ้มกลาง tile → **เพิ่ม** ไม่ใช่ลด (ไม่ถดถอยจาก v3.7.349); จิ้มกลางวงกลม → ลด. **ไม่แตะป้ายจำนวนฝั่งขวา** (28px) เพราะเป็นป้าย ไม่ใช่ปุ่ม — ปุ่มลบจึงใหญ่กว่าเล็กน้อยโดยตั้งใจ. APK = versionCode 9)
v3.7.400 (2026-07-25 — **กวาด safe-area ทั้งแอป (เจ้าของสั่ง "ตรวจเช็คทั้งหมด...โหมดเกมก็ยังทับซ้อน สูงเกินไป")** — 399 แก้แค่ 3 จุด รอบนี้ไล่ทั้งไฟล์. **เครื่องมือที่ใช้ (ทำซ้ำได้):** สร้างสำเนาไฟล์ที่แทน `env(safe-area-inset-*)` ด้วย `var(--SATT/--SATB/--SATL/--SATR)` แล้วตั้ง 47/34/0/0 → เสิร์ฟที่ port 8124 เปิดขนาด iPhone + วาดแถบสีทับเขตขอบนิรภัย + สคริปต์ไล่หา element ที่ **ตรึงอยู่กับที่** (fixed/sticky) และมีเนื้อหา/ปุ่ม ล้ำเข้าเขต — **สำคัญ: ต้องคิดการตัดขอบของกล่อง overflow ทุกชั้น** ไม่งั้นได้ผลบวกลวงจากของที่ถูก scroll ซ่อนไว้ (ผมพลาดจุดนี้รอบแรก). + workflow 5 โซนอ่านโค้ดคู่ขนาน แล้วให้เอเจนต์อีกชุดหักล้างทีละข้อ (ยืนยัน 17 / ตกไป 10). **แก้ 16 จุด:** (1) **`pkStage` เวทีโป๊กเกอร์ — ต้นตอของทั้งโหมดเกม**: แนวตั้งหมุน 90° `transform: translateX(100vw) rotate(90deg)` ⇒ จุด (x,y) ไปอยู่ที่ (100vw−y, x) **ขอบ "ซ้าย" ของ stage = ขอบ "บน" จริงของจอ** จึงย่อ stage ตาม inset จริงทั้ง 4 ด้าน + translateX = ความสูงใหม่ของตัวเอง; สาขาแนวนอน `inset:0` → ใส่ env ทั้ง 4 ด้าน. **แก้ตัวเดียวของข้างในขยับตามหมด** (HUD/ที่นั่ง/ปุ่ม/ชีตตั้งค่า) ไม่ต้องไล่แก้ทีละชิ้น. (2) การ์ดโมดัลเกมเต็มจอ padding `18px 14px 26px` → เผื่อ env บน-ล่าง (ครอบทุกเกม + หน้าเลือกเกม). (3) **ตัวครอบโมดัลเกม `alignItems: stretch` → `flex-start`** — stretch บังคับความสูงการ์ด = ความสูงจอ เนื้อหายาวกว่าจึงล้นทะลุ padding ล่าง (ปุ่ม "เริ่มเกม" จมใน home indicator) พบตอนวัดว่าลูกสุดท้ายสูง 743 แต่ content box มีแค่ 687. (4) ปุ่มปิด/ปุ่มเต็มจอของโมดัลเกม → `position: fixed` + เผื่อ env เมื่อ fs (absolute ไม่ขยับตาม padding และเลื่อนตามการ์ดเข้าไปใต้นาฬิกาได้). (5) แถบเตือน sync `sticky top:0` → `max(env(top), var(--paruay-banner-h,0px))` (เกาะใต้แถบออฟไลน์ด้วย). (6) หัวแผง AdminDashboard. (7) หัวแผง POS ของ Family (fpOverlay). (8) ปุ่มปิดพรีวิวใบแจ้งหนี้เงินกู้. (9) หน้าลูกค้า QR 3 จุด (ชีตประวัติ/หน้าส่งออเดอร์แล้ว/หน้าติดตามระยะไกล). (10) โมดัลรายละเอียดทริป. (11) **CSS `padding-bottom: 14px !important` ที่ผมเขียนเองตอน v3.7.370 ลบ env ของแถบตะกร้า POS ทิ้งบนจอ ≥1100px**. **verified:** ก่อนแก้หน้าเลือกเกมมีหัวข้อ+ปุ่มปิดอยู่ที่ 18/14px (ในเขต 47px) และ HUD โป๊กเกอร์อยู่ในเขตแถบสถานะ — หลังแก้ 0 จุดทุกหน้าที่ไล่ (หน้าหลัก/เลือกเกม/ตั้งค่าโป๊กเกอร์/โป๊กเกอร์กำลังเล่น/POS มือถือ+iPad) + ภาพยืนยัน. **regression:** จอ inset=0 ค่าคำนวณเท่าเดิมเป๊ะทุกจุด (18/26/14/0/16/0) วัดบน parme.me จริงหลัง deploy. env() รวมทั้งไฟล์ 32→43 top, 48→56 bottom, +4 left, +3 right. APK = versionCode 8)
v3.7.399 (2026-07-25 — **UI บนสุดทับแถบสถานะ (นาฬิกา/WiFi/แบตเตอรี่)** — เจ้าของรายงาน. **วัดก่อน:** บนแอป Android (emulator, Capacitor 8, Android 15) `env(safe-area-inset-top)` = **0px** และ WebView **ไม่ได้** วาดใต้แถบสถานะ (`innerH 839` vs `screenH 915`, เส้นทดสอบที่ `top:0` อยู่ใต้แถบสถานะ) ⇒ **ทำซ้ำบน Android ไม่ได้**. พื้นผิวที่วาดใต้แถบสถานะจริงคือ **PWA บน iPhone/iPad** เพราะ head ตั้ง `apple-mobile-web-app-status-bar-style=black-translucent` + `viewport-fit=cover` (ทั้งแอปชดเชยด้วย `env(safe-area-inset-top)` 29 จุด). แก้ **3 จุดที่ไม่ได้ชดเชย**: (1) **`.pv-pos-head`** — ของที่ผมเขียนเองตอน v3.7.373 (POS เต็มจอ: ตัวครอบ `padding:0` + หัวมี padding แค่ 16px) → `calc(16px + env(safe-area-inset-top))`; (2) **OfflineBanner** `position:fixed; top:0; zIndex:99999` ไม่มี safe-area เลย → `padding: calc(6px + env(...)) 12px 6px`; (3) **toast "sync ครบแล้ว"** `top: 8` → `calc(8px + env(...))`. + กันช่องว่างซ้อน: กล่องหน้าหลักเดิม `max(24px, env(safe-area-inset-top))` → `max(24px, calc(env(...) - var(--paruay-banner-h, 0px)))` (แถบกิน safe-area ไปแล้ว ไม่ต้องเผื่ออีก). **verified:** จอ inset=0 ค่าคำนวณเท่าเดิมเป๊ะทุกจุด (หัว POS 16px, แถบ 6px, หน้าหลัก 24px — ไม่มี regression) + จำลอง inset 47px ได้ 63px / 47px(ไม่มีแถบ) / 24px(มีแถบสูง 77px) + แอป Android จริงหน้าตาเหมือนเดิม. **ยังไม่ยืนยัน:** ไม่มีเครื่อง iPhone/iPad ทดสอบจริง — ตรวจได้แค่ระดับค่าที่คำนวณ. APK = versionCode 7)
v3.7.398 (2026-07-25 — **ไมค์ในผู้ช่วย AI ใช้ไม่ได้บนแอป Android + กันเด้งออกจากระบบตอนเน็ตออกไม่ได้จริง**. (1) **ไมค์ — พัง 2 ชั้นซ้อน วัดจากแอปจริงทั้งคู่:** ชั้นแรก Android WebView *มี* `webkitSpeechRecognition` แต่ `start()` คืน error `'not-allowed'` เสมอ (ไม่มี speech service ให้ WebView) → `rec.onerror` เดิมตีความว่า "ผู้ใช้ปิดไมค์" แล้วหยุด **ไม่ตกไปทางอัดเสียงส่ง Gemini** ⇒ กดไมค์แล้วขึ้นแค่ toast "ไมโครโฟนถูกปิด". แก้: `isWebView = / wv\)/.test(UA) || __isNativeApp()` แล้ว `const SR = !isWebView && (...)` ⇒ ลาวไป `startAiRecord()`, ภาษาอื่นไป `kbHint()` (+ hint บอกใช้ไมค์คีย์บอร์ดบน Android ด้วย ไม่ใช่แค่ iOS). ชั้นสอง **`getUserMedia` ถูกปฏิเสธแม้ RECORD_AUDIO granted=true** — อ่านซอร์ส `BridgeWebChromeClient.onPermissionRequest` พบว่า Capacitor ขอ **RECORD_AUDIO + MODIFY_AUDIO_SETTINGS พร้อมกัน** และ callback ตั้ง `granted=false` ถ้ามีตัวใดตัวหนึ่ง false; manifest เราไม่ได้ประกาศ MODIFY_AUDIO_SETTINGS → Android คืน false → `request.deny()`. แก้: เพิ่ม uses-permission ใน AndroidManifest (ฝั่ง `~/parme-app`). **verified e2e บนแอปจริง:** dialog "Allow Parme to record audio?" เด้ง (เดิมไม่เคยเด้ง) → `gUM OK` → `REC START` → กดซ้ำหยุด → `blob 76KB audio/webm;codecs=opus` → edge fn `transcribe` (map webm→ogg อยู่แล้ว) → Gemini คืนข้อความลาวเติมลงช่องพิมพ์จริง. **กล้องตรวจแล้วไม่พัง** (`CAMERA OK` — VIDEO_CAPTURE ขอแค่ CAMERA ที่ประกาศไว้แล้ว). (2) **auth:** `navigator.onLine` เป็น true ตอนต่อ wifi ที่ออกเน็ตไม่ได้ → เดิมเด้งผู้ใช้ออกจากระบบทั้งที่ไม่ได้กดออกเอง แล้วล็อกอินกลับก็ไม่ได้. แก้: ก่อนเด้งออก **เฉพาะกรณียังมี cached user** ให้ยิง `GET /auth/v1/health` (timeout 5s) — ตอบอะไรมาก็ได้ = ติดต่อได้ → ออกจริง; throw = คงสถานะ local ไว้. **verified fail→pass:** proxy ตัน + cold start เดิมเด้งไปหน้าล็อกอิน ตอนนี้อยู่ในระบบ ข้อมูลครบ + แถบเตือน cloud ขึ้นถูกต้อง; **regression:** กด "ອອກຈາກລະບົບ" จริงยังออกทันที (เพราะ `clearLocalUserCaches` ลบ cached_user ก่อน). APK ที่แจก = versionCode 6)
v3.7.397 (2026-07-25 — **ไม่โชว์แถบ "ບໍ່ສາມາດເຊື່ອມ cloud" ตอนออฟไลน์** (เจ้าของ: "ไม่จำเป็นต้องแสดงก็ได้"): แถบนี้ซ้ำกับแถบแดงบนสุดที่บอกออฟไลน์อยู่แล้ว และปุ่ม "ລອງໃໝ່" กดไปก็ไม่ได้ผลตอนไม่มีเน็ต. แก้ที่ catch ของ `loadCloudData` จุดเดียว (มี `setCloudError` แค่จุดเดียวในไฟล์ทั้งไฟล์). **เงื่อนไขแคบไว้ 2 ชั้นโดยตั้งใจ** เพื่อไม่ให้กลายเป็น silent fallback: กลบเฉพาะเมื่อ `navigator.onLine === false` **และ** ข้อความ error เข้าเกณฑ์เน็ต (`/fetch|network|timeout|timed.?out|abort|load failed/i` — regex ชุดเดียวกับที่ใช้ตัดสิน `netDown` ตอน flush คิว). ⇒ ออนไลน์แล้วพัง (JWT/RLS/สิทธิ์/timeout) ยังเตือนเหมือนเดิม, ออฟไลน์แต่เป็นบั๊กโค้ดจริงก็ยังเตือน. **verified บนแอปจริง (emulator, บิลด์ฝังไฟล์):** ก่อนแก้ (396) โหมดเครื่องบิน → แถบชมพู "TypeError: Failed to fetch" โผล่; หลังแก้ (397) → หายไป เหลือแถบแดงบนสุดอย่างเดียว ข้อมูลยังครบ; ออนไลน์ปกติ → ไม่มีแถบใดโผล่ โหลดข้อมูลได้. + node: classifier 9 เคส (Chrome/Safari/Firefox/abort/timeout = เน็ต; JWT/RLS/permission/TypeError = ไม่ใช่) + ตรรกะ guard บนเบราว์เซอร์จริงทั้ง 2 สถานะ. **ไม่ได้ยืนยัน e2e:** เคส "ออนไลน์แต่เซิร์ฟเวอร์พัง" — จำลองด้วย proxy ตันไม่ได้เพราะ auth พังไปด้วยแล้วเด้งไปหน้าล็อกอิน (คนละเส้นทาง). APK ที่แจก = versionCode 4)
v3.7.396 (2026-07-25 — **กันบิลด์เก่าพังจาก 395**: `__isNativeApp()` เช็ค `Capacitor.Plugins.App` เพิ่ม ไม่ใช่แค่ `isNativePlatform()`. เหตุ: APK ตัวโหลดสด v3.7.394 ที่ผู้ใช้อาจติดตั้งอยู่ **ดึง index.html ตัวใหม่จาก parme.me** → `isNativePlatform()` true → ส่ง `me.parme.pos://auth` ทั้งที่บิลด์นั้นไม่มี intent-filter → เบราว์เซอร์เปิดไม่ขึ้นเลย (แย่กว่าเดิม). `@capacitor/app` มีเฉพาะบิลด์ 395+ ซึ่งเป็นบิลด์เดียวกับที่มี intent-filter → ใช้เป็นตัวแยกได้ตรง. **verified บน emulator ทั้ง 2 ทาง:** บิลด์ฝังไฟล์ 396 กด Google → เข้าแอปได้ (ไม่มีแถบ URL); บิลด์เก่า 394 กด Google → กลับไปพฤติกรรมเดิม (Chrome/parme.me) ไม่เจอหน้า error. APK ที่แจก = versionCode 3)
v3.7.395 (2026-07-25 — **แก้ Google login พังในแอปเนทีฟ (กระทบ APK ทั้ง 2 แบบ)**: `redirectTo` เดิม = `location.origin+pathname` → ในแอปแบบฝังไฟล์ origin คือ `https://localhost` ซึ่งไม่อยู่ใน `GOTRUE_URI_ALLOW_LIST` → GoTrue fallback ไป `SITE_URL` → **session ไปตกที่ Chrome (parme.me) ส่วนแอปค้างหน้าล็อกอิน** (verified บน emulator ทั้งก่อนและหลังแก้). แก้ 4 ชั้น: (1) index.html เพิ่ม `__isNativeApp()` + `OAUTH_NATIVE_REDIRECT='me.parme.pos://auth'` + listener `appUrlOpen` อ่าน `#access_token/#refresh_token` → `auth.setSession` (supabase-js 2.45.4 = **implicit flow** ตรวจจากบันเดิลจริง); (2) `@capacitor/app` 8.1.1 + intent-filter BROWSABLE scheme `@string/custom_url_scheme` ใน AndroidManifest; (3) `CFBundleURLTypes` ใน iOS Info.plist; (4) VPS `.env` `ADDITIONAL_REDIRECT_URLS` += `me.parme.pos://**` แล้ว `docker compose up -d auth` (backup `.env.bak-oauth-*`). **เว็บไม่กระทบ** — verified บน parme.me จริง: `window.Capacitor` undefined → redirectTo ยังเป็น `https://parme.me/` เท่าเดิม)
v3.7.394 (2026-07-24 — ปุ่มดาวน์โหลด APK ใน AdminDashboard (`/parme.apk`) + i18n `admin_download_apk` ×4)
v3.7.393 (2026-07-24 — sw.js กวาด cache ชื่อเก่า `paruay-shell-*` ทิ้งตอน activate (rebrand))
v3.7.392 (2026-07-23 — **variant groups: ตั้งชื่อแบบเองได้ + แก้/ลบ/เพิ่มตอนแก้สินค้า** (เจ้าของเลือกแบบเต็มผ่าน 2 คำถาม) — ยกเครื่องส่วน ร้อน/เย็น/ปั่น จาก 3 แบบตายตัว→list ไดนามิก. **DB migration (VPS, ทำแล้ว+verify):** `ALTER TABLE pos_products ADD COLUMN IF NOT EXISTS var_group text, var_word text` + qr_menu RPC คืน 2 คอลัมน์ + NOTIFY pgrst (backup `/docker/backups/rollback-qr_menu-20260723.sql`; SQL `tools/migrations/`? — เก็บใน scratchpad vm_migration.sql). **สถาปัตยกรรม (คงของเดิม — variant ยังเป็น "สินค้าแยกหลายตัว" ผูก productId เดี่ยว, ไม่แตะ cart/สต็อก/ใบเสร็จ):** สินค้าในกลุ่มแชร์ `varGroup` (id) + มี `varWord` (ชื่อแบบ) ต่อตัว; ชื่อ = base+' '+varWord. **client 14 กลุ่มแก้:** state `pVar`→`pVars`(list)+`pVarGroup`; ฟอร์มไดนามิก (ชิป ร้อน/เย็น/ปั่น + "+เพิ่มแบบ" custom + ลบแถว) โผล่ทั้ง add & edit(group); `submitProduct` variant branch สร้าง**และ**อัปเดต sibling (upMap by id — ไม่ซ้ำ) + new varGroup; `startEdit` ตรวจ sibling (varGroup หรือ legacy varBase) prefill; `groupShown` จับกลุ่มด้วย varGroup ก่อน fallback varBase (ร้อน/เย็น/ปั่น เดิมไม่พัง); chooser/tile ใช้ `varWord||varBase().word`; ลบแถว pid→askConfirm+delProduct; posProductRow+3 load map+QR map เพิ่ม var_group/var_word. **verified แอปจริง (demo):** add custom→2 สินค้า+tile เดียว; edit→prefill 2 แถว; แก้ราคา 5000→6000 อัปเดตไม่ซ้ำ; เพิ่ม custom "แก้วใหญ่"→รวมกลุ่ม 3; ลบ+confirm→เหลือ 2. + node: groupShown 3 เคส (custom/legacy/lone), posProductRow+load round-trip. + DB: write/read var_group, qr_menu keys, REST select 200. **หมายเหตุ:** pos_var_title ยังเขียน "ร้อน/เย็น/ปั่น" (ชิป preset) แม้เพิ่ม custom ได้ — ปล่อยไว้กัน 4-lang edit; migration SQL ควร copy เข้า tools/migrations/)
v3.7.391 (2026-07-22 — **แถวเพิ่มหมวดสินค้าไม่เหลือที่พิมพ์บนจอแคบ** (เจ้าของภาพ Android ~360: อีโมจิ+ชื่อ+ดรอปดาวน์หน่วย 120px+ปุ่มเพิ่ม เบียดจนช่องชื่อเหลือ ~60px) — inline 2 จุด: กล่องแถว (`display: flex, gap: 8`) เพิ่ม `flexWrap: 'wrap'`; ช่องชื่อ `flex: 1` → **`flex: '1 1 150px'`** (basis 150 การันตีที่พิมพ์ พอไม่พอดรอปดาวน์+ปุ่มห่อลงบรรทัดล่าง). verified fb 360: ช่องชื่อ 60→**239px** ดรอปดาวน์+ปุ่มห่อลงบรรทัด2; 1280: บรรทัดเดียวเหมือนเดิม (ช่องชื่อ 321). **นี่เป็นแถวมือถือปกติ ไม่ใช่เฉพาะจอใหญ่ — flexWrap มีผลทุกจอ ตั้งใจ เพราะเจ้าของเจอบนมือถือ**)
v3.7.390 (2026-07-22 — **ป้าย "ຍອດລວມ" ชิดตัวเลขจน "เกือบชน"** (เจ้าของขอหลัง 389 — ช่องที่เหลือมาจาก line-box ของฟอนต์ลาว (Noto เผื่อสระบน-ล่างสูง) ไม่ใช่ gap ที่ตั้ง): rowGap 2→0 + label `lineHeight: 1.1` + ตัวเลข `lineHeight: 1.05`. verified 360×800 ยอด ₭800M: ระยะกล่องป้าย→กล่องเลข **0px** ชิดขวาตรงกัน ไม่ล้น; ยอดสั้น (₭40,000) ยังบรรทัดเดียว baseline ตรงกันปกติ)
v3.7.389 (2026-07-22 — **เก็บอาการยอดยาวบนมือถือจากภาพจริงเจ้าของ (Android ~360px, ₭800 ล้าน)**: (1) ปุ่ม "ล้าง" โดนบีบจนตัวแตกสองบรรทัด "ລ້າ/ງ" → เพิ่ม `whiteSpace: 'nowrap', flexShrink: 0`; (2) ป้าย "ຍອດລວມ" ที่ห่อขึ้นบนลอยห่างตัวเลข ~20px+ → กล่อง label+จำนวน เปลี่ยน `gap: 8` (ซึ่งกลายเป็น row-gap ตอนห่อด้วย!) เป็น `columnGap: 8, rowGap: 2` + `alignContent: 'flex-end'` (กัน flex กระจายบรรทัดตอนคอนเทนเนอร์สูง). verified ที่ 360×800 (กว้างเท่าเครื่องเจ้าของ — ที่ 390 ยอดเดิมพอดีเส้นไม่ห่อ จอแคบกว่าถึงห่อ): ล้าง 34px บรรทัดเดียว, ป้ายขึ้นบนห่างเลข 2px, ชิดขวาตรงกัน, ไม่ล้น. **บทเรียน: `gap` เดี่ยวใน flex-wrap คุมทั้งสองแกน — ตอนห่อบรรทัดจะกลายเป็นระยะห่างแนวตั้งด้วย ให้แยก columnGap/rowGap เสมอเมื่อตั้งใจคุมแค่แนวนอน**)
v3.7.388 (2026-07-22 — **ปุ่มส่วนลดไร้ไอคอน + ป้ายยอดรวมหนีขึ้นบนอัตโนมัติเมื่อเลขยาว** (เจ้าของขอ) — inline 2 จุด: (1) ตัด `'🏷️ ' +` จากปุ่มส่วนลด; (2) กล่อง label+จำนวน (`alignItems: 'baseline', gap: 8`) เพิ่ม `flexWrap: 'wrap', justifyContent: 'flex-end'` → เมื่อความกว้างไม่พอ ป้าย "ຍອດລວມ" ห่อขึ้นบรรทัดบน ชิดขวาทั้งคู่ ไม่มีตรรกะเงื่อนไข. **verified fail→pass ที่ 390**: ₭100,000,000 = พอดีบรรทัดเดียว (ไม่ห่อ ถูกต้อง), ₭1,000,000,000 = ป้ายขึ้นบน + ชิดขวา + ไม่ล้นจอ (376≤390); ไอคอนหายทั้งสถานะปกติและมีส่วนลด (เทสต์ตอน discAmt>0 เห็นข้อความ '−…' ไม่มี 🏷))
v3.7.387 (2026-07-22 — **ปุ่มส่วนลดชิดปุ่มล้าง** (เจ้าของขอ หลังปุ่มล้างแดงติดบนเครื่องจริงแล้ว) — แถว [ล้าง][ส่วนลด][ยอดรวม] ในแถบตะกร้า: inline เปลี่ยน `justifyContent: 'space-between'` → `gap: 8` + กล่องยอดรวมเพิ่ม `marginLeft: 'auto'` = ล้าง+ส่วนลดจับกลุ่มซ้าย ยอดรวมชิดขวาเหมือนเดิม. มีผลทุกจอ (inline — แนวเดียวกับปุ่มล้าง 386). verified DOM: 1280 และ 390 → ช่องว่างล้าง→ส่วนลด 8px, ยอดรวมห่างขอบขวา 0px, บรรทัดเดียวไม่ล้น (แถวสูง 35px))
v3.7.386 (2026-07-22 — **ปุ่ม "ล้าง" แดงทึบตัวขาว — จบที่ inline `backgroundColor`** หลังพลาด 3 รอบ. ลำดับเหตุ: (1) v381/383 ทำผ่าน CSS `[style*="background: transparent"]` — น่าจะถูกอยู่แล้วแต่เครื่องเจ้าของติด SW เก่าเลยเห็นตัวหนังสือเปล่าเดิม → เจ้าของทักซ้ำ; (2) v385 ย้ายมา inline `background: 'var(--pv-danger)'` + ลบกฎ CSS — **พังบนธีม fb ทันที**: กฎ global `html[data-theme="fb"] button[style*="background: var(--pv-danger)"]` แปลงปุ่ม danger เป็นพาสเทลชมพู (by-design ของธีม) ทับ inline; (3) v386 เปลี่ยนเป็น **`backgroundColor:`** → serialize เป็น `background-color:` ซึ่ง substring `background: var(--pv-danger)` ของกฎ fb ไม่แมตช์ = หลุดจากกฎ ยังใช้ token เดิม. **หมายเหตุ: ปุ่มนี้แดงบนมือถือด้วย** (inline ไม่ใช่ ≥700 — เจ้าของสั่งสามรอบ สเปกไม่จำกัดจอ). verified: fb 1280 = rgb(250,56,62)+ขาว, เขียว 390 = rgb(170,60,40)+ขาว. **บทเรียน 2 ข้อ**: (ก) ธีม fb มีกฎ `[style*=...]` ดัก background ปุ่ม — จะตั้งสีปุ่มด้วย inline ให้ระวังโดนดัก เลี่ยงได้ด้วย backgroundColor; (ข) `getComputedStyle` คืน **live object** — เก็บค่าลง primitive ก่อนสลับ data-theme ไม่งั้นอ่านย้อนหลังได้ค่าธีมใหม่ (ทำผมวินิจฉัยหลงทางหนึ่งรอบ: อ่าน "ธีมเขียว" ได้ค่าพาสเทลของ fb))
v3.7.384 (2026-07-22 — **การ์ดโต๊ะ: ⇄ สลับโต๊ะย้ายมาอยู่แถวเดียวกับปุ่มเพิ่ม/แก้ชื่อ** (เจ้าของรายงาน 2 จอ: มือถือ ⇄ absolute มุมล่างซ้าย**ลอยทับ**ปุ่มชื่อ = บั๊กเดิมของแอปไม่ใช่ผลจากงานจอใหญ่; จอใหญ่ "0 ລາຍການ" ชิดปุ่มชื่อเกิน) — **แก้ JSX จริง มีผลทุกขนาดจอตามเจ้าของสั่ง**: ถอด ⇄ จาก absolute → สร้างแถวล่าง `div (marginTop auto, flex row, gap 6)` ห่อ [⇄?, ปุ่มชื่อ?] เงื่อนไข `(qrEnabled || (occ && nTables>1))`; ปุ่มชื่อเปลี่ยน maxWidth 100% → minWidth 0 (ellipsis ในแถว); CSS ≥700 เพิ่ม `> div[margin-top: auto] { margin-top: 7px !important }` (การ์ด block จาก 382 ทำ auto→0 เลยชิด). verified DOM: 390 → ⇄ static/flex-row/ทับใครเป็น 0 (เช็ค intersect ทุก element ในการ์ด); 1280 → การ์ดยังสูงเดียว 129, ช่องว่างบรรทัดจำนวน→แถวปุ่ม 9px. หมายเหตุ: demo ปิด qr → ปุ่มชื่อไม่เรนเดอร์ ยืนยันแถวคู่ ⇄+ชื่อจากโครงโค้ด (ห่อ wrapper เดียวกัน) — เจ้าของดูของจริงร้านที่เปิด QR)
v3.7.383 (2026-07-22 — **ปุ่ม "ล้าง" เป็นปุ่มแดงทึบตัวอักษรขาว** (เจ้าของทัก 381 "ยังเป็นตัวอักษรเฉย ๆ" — อาจติด SW เก่า + ขอบบาง 381 มองไม่ออกว่าเป็นปุ่ม จึงสั่งสเปกชัด: แดง+ขาว) — กฎเดิม 381 เปลี่ยนค่าเป็น `background: var(--pv-danger); color: #fff; border: none; radius 10; padding 8×16`. **verified ทั้งสองธีมรอบนี้** (บทเรียนจาก 381 ที่วัดแค่ธีมเขียว): เขียว bg rgb(170,60,40), fb bg rgb(250,56,62) — แดงทึบตัวขาวทั้งคู่; มือถือ 390 ยัง transparent เดิม)
v3.7.382 (2026-07-22 — **การ์ดโต๊ะเท่ากันทุกใบ + เงิน/จำนวนบรรทัดเดียว** (เจ้าของ: เปิดโต๊ะแล้วช่องไม่เสมอกัน) — กฎ ≥700: กริดโต๊ะ (`repeat(3,1fr)+gap:10px` — โดนกริดสินค้าหน้าขายด้วย ตั้งใจ: tile สูงเท่ากันสวยขึ้น) → `grid-auto-rows: 1fr`; การ์ดโต๊ะ (`button[min-height: 96px]` — จุดเดียวในไฟล์) → `display: block` + text-align center (ปลด flex ให้ inline-block รวมบรรทัดได้; badge absolute ไม่โดน margin เพราะ `:not([position: absolute])`); ยอดเงิน (`.display[fs12]`) + จำนวน (`[fs9][opacity:0.7]`) → inline-block บรรทัดเดียว. verified DOM 1280 (demo มีโต๊ะเปิด 2): การ์ด 5 ใบสูงเดียว 99px, เงิน+จำนวน top ต่างกัน <6px; มือถือ 390: flex เดิม เงิน/จำนวนแยกบรรทัดเดิม)
v3.7.381 (2026-07-22 — **ปุ่ม "ล้าง" ในแถบตะกร้าเป็นปุ่มจริงบนจอใหญ่** (เจ้าของวง: เป็นตัวหนังสือแดงเปล่าข้างปุ่มมีกรอบ) — กฎ ≥700: `.pv-pos-cart button[background: transparent][var(--pv-danger)][font-size: 12px]` → พื้นขาว + ขอบ `1.5px solid rgba(var(--pv-danger-rgb),0.55)` + radius 10 + padding 7×14. verified DOM: 1280 = ขอบ solid พื้นขาว 48×37; 390 = transparent/ไม่มีขอบ เดิมเป๊ะ)
v3.7.380 (2026-07-22 — **ปุ่ม/ตัวเลือกข้างช่องกรอกสูง 36 เท่าช่อง** (เจ้าของวง 4 จุด "ยังไม่เสมอกัน": +เพิ่ม, กล้องสแกน×2, อีโมจิ+เลือกรูป — ค้าง 42–48 หลังช่องเหลือ 36 จาก 378) — กฎ ≥700: `div[display:flex]:has(> input) > button/label` → height 36 + padding แนวตั้ง 0 + inline-flex center; ช่องอีโมจิ (inline fs 20/22) → 36×36 fs16; ปุ่มไอคอน inline width 44/48 → 36. verified DOM 1280: อีโมจิ 36×36 ×2, +เพิ่ม 64×36 ×2, 📷 36×36, สแกนขาย 46×36, ร้อน/เย็น/ปั่น 88×36, ปุ่ม lots ✏️ 34–35×36 — สูง 36 ทั้งหมด; มือถือ 390: อีโมจิ 44×42 เดิมเป๊ะ. หมายเหตุ: ปุ่มสแกนกว้าง 46 (ไม่ใช่จัตุรัส 36) เพราะกว้างมาจาก padding ไม่ใช่ inline width — สูงตรงคือประเด็นหลัก ปล่อยไว้)
v3.7.379 (2026-07-22 — **ถอดกฎบังคับกว้างเท่ากันของ 378 ออก** (เจ้าของดูแล้ว: "ไม่ใช่แบบนี้ เอาแค่ขนาดกะทัดรัด ไม่ต้องเสมอกัน") — ลบกฎ `.pv-prod-side [display:flex] > input/select { flex: 1 1 0 }`; **คงส่วนกะทัดรัดไว้ทั้งหมด** (สูง 36px เท่ากัน, ฟอนต์ 13, padding 7×10, radius 8). verified DOM 1280: สูงยัง 36 เดียว, แถวหมวดกลับสัดส่วนเดิม 44|326|120 (อีโมจิ|ชื่อ|ดรอปดาวน์); มือถือไม่ต้องตรวจซ้ำ — การแก้คือลบกฎ ≥700 ล้วน ๆ ที่มือถือไม่เคยเข้าเงื่อนไข. **บันทึกไว้กันสับสนภายหลัง: "เสมอกัน" ในคำสั่งก่อนหน้าหมายถึงขนาดกะทัดรัดสม่ำเสมอ ไม่ใช่ความกว้างเท่ากันทุกช่อง**)
v3.7.378 (2026-07-22 — **ช่องกรอกใน POS ขนาดมาตรฐานเดียวกันบนจอใหญ่** (เจ้าของ: "ช่องใหญ่ ไม่เท่ากัน ดูไม่สวย") — วัดของจริงก่อนแก้: สูง 5 ค่า (38/41/42/44/48) ฟอนต์ 13–15 กว้างสะเปะสะปะ. กฎ ≥700 ใน `.pv-pos-body`: input/select ทุกช่อง → **สูง 36px !important** (จำเป็น — ช่องลำดับสินค้ามี inline `height: 34px`), padding 7×10, ฟอนต์ 13, radius 8; textarea → min-height 36; **ยกเว้นช่องอีโมจิ (inline fs 20/22px), checkbox, file** ที่จงใจเล็ก; ฟอร์มสินค้า (`.pv-prod-side [display:flex] > input/select`) → `flex: 1 1 0` = ช่องแถวเดียวกันกว้างเท่ากันเป๊ะ. verified DOM 1280: 21 ช่องแท็บสินค้า + แท็บตั้งค่า (กางแผงจำนวนโต๊ะ) = 36px เดียวทั้งหมด, แถวคู่กว้างเท่ากัน 6/6 แถว (223=223, 226=226×3, 285=285, 275=275); มือถือ 390 กลับค่าเดิม (42 ไม่ใช่ 36 = ไม่รั่ว))
v3.7.377 (2026-07-22 — **อุดรูโหว่ใต้ช่องค้นหาแท็บสินค้า** (เจ้าของส่งภาพ: กล่องว่าง ~170px ระหว่างช่องค้นหากับลิสต์) — **root cause = จุดอ่อนของ master-detail v372: grid สองคอลัมน์แชร์แถวร่วมกัน** แถว 1 สูงตามการ์ดหมวดฝั่งซ้าย (~230px) แต่ช่องค้นหาสูง 56 → เหลือรู (ตอน verify v372 ผมวัดตำแหน่ง search/list แยกกันแต่**ไม่ได้วัดช่องว่างระหว่างกัน** เลยหลุด). แก้: **ห่อลูกฝั่งซ้ายทั้งหมด (34,804 ตัวอักษร, 5 การ์ด) เป็น `.pv-prod-side` ก้อนเดียว** — python insert เปิดหลัง root + ปิดก่อน `})(), products.length === 0 ?` (จุดเดียวในแท็บ, assert แล้ว); CSS เปลี่ยนเป็นผัง 2×2: side col1 row 1/3, `:not(.pv-prod-side)` col2, search row1, list row2 `grid-template-rows: auto 1fr`. verified DOM: 1280 ช่องว่าง search→list = **10px** (เดิม ~170), side/list คนละคอลัมน์, top ตรงกัน; 390: block flow ลำดับ ฟอร์ม→ค้นหา→ลิสต์ เต็มกว้างเดิม. บทเรียน: **ตรวจ layout ต้องวัด "ระยะห่างระหว่างของ" ไม่ใช่แค่ตำแหน่งของแต่ละชิ้น**)
v3.7.376 (2026-07-22 — **การ์ดออเดอร์/VIP → แถวเดียวแบบตาราง** (เจ้าของดู 375 บนจอจริง: "ยังใหญ่อยู่" → ยกระดับจาก "การ์ดเตี้ย" เป็นแถวตาราง): การ์ด `min-height:120px` ≥700 → `flex row` align center gap 10, padding 7×12 = **สูง ~41px (VIP ครบช่อง) / ~34px (โต๊ะ)** จากเดิม ~250; ชื่อลูกค้า (`15px+line-height:1.2` — แยกจากวันนัด `15px+margin-top:4px`!) = คอลัมน์ยืด ellipsis, ที่เหลือ shrink 0, footer (`margin-top:auto`) → `margin-left:auto` ชิดขวา; เลขโต๊ะ 30→18px; `.pv-vip-list` เลิก grid tile ของ 375 → คงคอลัมน์เดิม gap 6 (แถวเต็มกว้างครึ่งจอ = อ่านเป็นตาราง). verified synthetic 2 แบบ (VIP+โต๊ะ) ที่ 1280: ทุก child top เดียวกัน/footer ชิดขวา/ellipsis; 390: กลับ column/120px/15px เดิม. เตือนเจ้าของ: iPad Safari อาจติด SW เก่า — reload 2 ครั้ง)
v3.7.375 (2026-07-22 — **การ์ดออเดอร์/VIP กระชับบนจอใหญ่** (เจ้าของส่งภาพ VIP บน iPad: การ์ด ~250px ต่อใบ "ใหญ่เกินไป ทำเป็นตารางเล็ก ๆ"): การ์ดจาก `orderCard` (ใช้ร่วมแท็บออเดอร์+VIP, signature `min-height: 120px`) ≥700 → block/minH 0/padding 8×12, "n รายการ"+ยอดรวม inline บรรทัดเดียว, เลขโต๊ะ 30→20px, ชื่อ/วันนัด 15→13px; ≥900 ลิสต์ VIP แต่ละฝั่ง (className ใหม่ `pv-vip-list` ใน `faak`) → `repeat(auto-fill, minmax(220px,1fr))` = ตารางการ์ดเล็ก. **verified แบบ synthetic node** (demo ไม่มีออเดอร์ — qr ปิด): ฉีดการ์ดจำลอง signature เดียวกับของจริงเข้า `.pv-pos-body` ที่ 1280 → 122px/บรรทัดเดียว/grid 3 cols; ที่ 390 → ค่าเดิมทุกตัว (flex/120px/block/flex). **ยังไม่เห็นกับการ์ดจริงบนจอ** — เจ้าของดูร้าน vmoto ยืนยันตา)
v3.7.374 (2026-07-22 — **เก็บสองข้อเสนอค้าง**: (ก) **แชตผู้ช่วย AI บนจอใหญ่ = คอลัมน์สนทนา 760 กลางจอ** — CSS อย่างเดียว ไม่แตะ React: overlay แชต (`fixed + rgb(243,238,225)` = #F3EEE1 เอกลักษณ์ของจอนี้) > พื้นที่ข้อความ (`overflow-y: auto`) กับแถบพิมพ์ (`div:last-child`) ใช้ `padding-inline: max(14px, calc((100% - 760px)/2))` — bubble/ช่องพิมพ์อยู่คอลัมน์กลาง scrollbar ชิดขอบจอแบบแอปแชตจริง; (ข) **iPad แนวตั้ง/จอกลาง (700–1099): รายการในบิลแคป 108px → 26vh** (~307px บน iPad 1180 สูง ≈ 6-7 แถว) — `.pv-pos-cart [style*="max-height: 108px"]` ในบล็อก ≥700; โหมดสองคอลัมน์ ≥1100 ยัง `none` (กฎเจาะจงกว่า+อยู่ทีหลัง). verified DOM: 1280 แชต padding 257px = (1280−760)/2 เป๊ะ; 820×1180 แคป 306.8 = 26vh; regression: 1280 สองคอลัมน์ none ✓, มือถือ 390 แคป 108px + แชต padding 14px เดิม ✓. บทเรียนซ้ำ: SW re-register ทุกโหลด → เทสต์เวอร์ชันใหม่ในพรีวิวต้อง unregister+ลบ cache ทุกครั้ง)
v3.7.373 (2026-07-22 — **POS เต็มจอบน iPad/คอมพิวเตอร์** (เจ้าของสั่งเลิกหน้าต่างลอย): `.pv-pos-shell` ≥700 → height/max-width 100%, radius 0 (แทนกฎหน้าต่างลอย 96vh/920/1320 เดิมทั้งสามจุด); overlay ของ POS ต้อง padding 0 — **บทเรียน specificity: กฎจัดกลางชีต v369 มี attribute selector 4 ตัว (0,4,0) ชนะ `:has(.class)` (0,2,0) แม้ !important เท่ากัน** → เขียนกฎ POS ให้ครบ 4 attribute + `:has(> .pv-pos-shell)` แล้ววางทีหลัง; เนื้อหาแท็บบนจอกว้างมากคุมด้วย `.pv-pos-body > div { max-width: 1280px; margin auto }` (≥900). verified DOM: 1280 → shell 0,0,1274×800 radius 0 (1274 = หัก scrollbar), settings root 1242 กลางจอ; มือถือ 390 → 0,0,390×844 เหมือนเดิม. ภาพหน้าจอแผงพรีวิวเพี้ยน (ปัญหา tooling เดิมจาก v370) — ยึด DOM)
v3.7.372 (2026-07-22 — **จัดผังจอใหญ่ให้กระชับ (คำสั่งเจ้าของ: "ไม่ใช่แค่ขยาย รวมบรรทัดเข้าด้วยกัน")** — (ก) **แท็บสินค้า = master-detail**: className ใหม่ `pv-tab-products` ที่ root → grid 2 คอลัมน์ ≥1100; ลูกทุกตัวบังคับคอลัมน์ซ้าย (การ์ดหมวด/หน่วย/ฟอร์มเพิ่มสินค้า) ยกเว้นช่องค้นหา (`*:has(+ .pv-prod-list)` → col2 row1) + ลิสต์ (`grid-row: 2/99` col2) = ฟอร์มซ้าย·รายการสดขวาเห็นพร้อมกัน; ยกเลิกกฎ 371 ที่ทำ pv-prod-list เป็น 2 คอลัมน์ (ครึ่งจอแล้วคอลัมน์เดียวถูก; search+list อยู่ใน Fragment → เป็นลูกตรงของ root ใน DOM จึง target ได้); (ข) **ตั้งค่า**: container ขาวใน accordion (`[EAEAEF radius14] > [white radius12]`) → grid 2 คอลัมน์ (13 แถว → ~7) แผงที่กาง (ลูก EAEAEF ใน white — แทรกต่อจากแถวของมันเสมอ ยืนยันจาก DOM) `grid-column:1/-1` + **`width:100%` (จำเป็น! margin:auto จากกฎ 700 ของ 371 ทำ grid item หดเหลือ 441px)**. verified DOM 1280: root grid/2, ฟอร์ม x193 ลิสต์ x643 ลิสต์ใต้ช่องค้นหา, white grid/2 แถวคู่บรรทัดเดียว, แผง 684=เต็ม; มือถือ 390: ทุกอย่างกลับ block/flex เดิม; ภาพยืนยันทั้งสองแท็บ)
v3.7.371 (2026-07-22 — **จอใหญ่ครบทุกแท็บ POS** (ต่อจาก 369/370 ที่ทำหน้าขาย): className ใหม่ 3 จุด `pv-prod-list` (ลิสต์สินค้า) + `pv-stock-list` ×2 (ก้อนใกล้หมด/ทั้งหมด). ≥1100: ลิสต์สินค้า+สต็อกเป็นตาราง 2 คอลัมน์ (หัวข้อก้อนสต็อก `grid-column:1/-1`), แท็บตั้งค่า accordion คุมกว้าง 700 กลางจอ (signature `rgb(234,234,239)` scoped `.pv-pos-body` — กล่องเดียวกันใน settings modal หลักอยู่นอก scope ไม่โดน). ≥1280: โต๊ะ/กริดแท็บไม่มีตะกร้า 3→5 คอลัมน์ (`:not(:has(> .pv-pos-cart))`), ออเดอร์ 2→3 คอลัมน์ (`repeat(2,1fr)+gap:10px` ใน POS มีจุดเดียว — ตรวจแล้ว 12560/22202 อยู่นอก POS). **verified วัด DOM ที่ 1280**: tables 5 tracks, prod-list grid 2, stock-list grid 2 + header span (ต้อง seed stock ใน demo localStorage ก่อน — demo ไม่มีของติดตามสต็อก), settings 700 กลาง (ช่องว่างข้างละ 91px); **มือถือ 390 กลับค่าเดิมครบ** (flex/block/3 tracks/maxW none). **ยังไม่ verified บนจอจริง: แท็บออเดอร์/VIP** (demo ปิด qrEnabled → DOM ไม่มีให้วัด) — กลไก selector เดียวกับที่ผ่าน ถ้าเพี้ยนแค่ค้าง 2 คอลัมน์เดิม; VIP ตั้งใจคงเดิม (การ์ดกว้างอ่านดีอยู่แล้ว) เช่นเดียวกับ report (920 กำลังดี ตัวเลขการเงินคอลัมน์เดียวอ่านง่ายกว่า))
v3.7.370 (2026-07-22 — **แผงบิลโหมดสองคอลัมน์ (≥1100): ยอด+ปุ่มบันทึกตรึงล่างสุด รายการบิลยืดเต็มกลาง** — เจ้าของขอจากภาพ iPad จริง. CSS ล้วนในบล็อก ≥1100 เดิม: `.pv-pos-cart` เป็น flex column, กล่องห่อรายการ+ยอด (ลูกตัวสุดท้ายเสมอ ทั้งกรณีว่าง/มีของ) flex:1, ตัวรายการ (`[style*="max-height: 108px"]` — แคปที่จำเป็นบนมือถือ) → `max-height:none + flex:1 + min-height:96px` เลื่อนในตัวเอง. verified วัด DOM ที่ 1280×800: itemsH 108→477px, ปุ่มบันทึกห่างขอบล่างแผง 14px; มือถือ 390 ยัง 108px + shell กลับ flex; ภาพหน้าจอยืนยัน. หมายเหตุ: iPad แนวตั้ง (<1100 ไม่มีแผงข้าง) คงเดิมโดยตั้งใจ; บทเรียน tooling — แผงพรีวิวย่อหลัง PostToolUse hook เปิดไฟล์ → ภาพหน้าจอเพี้ยน/พิกัดคลิกเพี้ยนทั้งที่ DOM ถูก ให้เชื่อ getComputedStyle+getBoundingClientRect แล้วเก็บภาพทีหลัง)
v3.7.369 (2026-07-22 — **ชั้น UI จอใหญ่ (iPad/คอมพิวเตอร์) ทั้งแอป** — CSS overlay ล้วนตามไอดิออมธีม fb + className โครงสร้าง POS 5 จุด (`pv-pos-shell/head/tabs/body/cart`), **มือถือไม่โดนแตะ** (ทุกกฎใต้ `min-width:700 + min-height:520` หรือ `hover+pointer:fine`; ยืนยันจาก DOM: กริดขายมือถือยัง 3 คอลัมน์): เวที backdrop ไล่สีจาก `--pv-primary` ผ่าน `color-mix` (ตามธีมอัตโนมัติ ทั้งเขียว+fb), ชีต/โมดัล 600→720 + ชีตแปะขอบล่าง→กล่องกลางจอมุมโค้ง 22 + เงา, POS เป็นหน้าต่างลอย (≥900: กว้าง 920 + กริดสินค้า/โต๊ะ 3→4 คอลัมน์ — เฉพาะ `repeat(3,1fr)+gap:10px` ใน shell, กริดเลข vmoto gap:6 กับแถวสถิติ `'1fr 1fr 1fr'` ไม่โดน), **≥1100: หน้าขายสองคอลัมน์ สินค้าซ้าย-ตะกร้าขวา 400px ด้วย `:has(>.pv-pos-cart)`** (เบราว์เซอร์ไม่มี :has → คอลัมน์เดียวกว้าง งามเหมือนกัน; แท็บอื่น shell กลับคอลัมน์เดียวอัตโนมัติ), hover ปุ่ม/การ์ดเฉพาะเมาส์. verified ในพรีวิว ?demo=1: 1280/768×1024/390×844 + ธีม fb + เทียบ prod 368 ที่ 390px. หมายเหตุ: จอแชต AI เต็มจอกว้างบนเดสก์ท็อป — ใช้ได้แต่ยังไม่ได้จัดเป็นคอลัมน์ ไว้ค่อยเก็บ)
v3.7.306 (2026-07-17 — **เมนูลูกค้า QR รวม ร้อน/เย็น/ปั่น เป็น tile เดียว + แผงเลือก** เหมือนหน้าขาย; ย้าย varBase/groupShown ขึ้น module scope แชร์ 2 คอมโพเนนต์. verified e2e กับข้อมูลจริงร้าน vmotolaos)
v3.7.305 (2026-07-17 — **หน้าขาย POS รวมสินค้า ร้อน/เย็น/ปั่น (ชื่อฐานเดียวกัน) เป็น tile เดียว** → แตะแล้ว bottom sheet เลือกแบบ (คำแบบดึงจาก I18N runtime ไม่ hardcode); การแสดงผลล้วน เส้นทางเงินไม่แตะ)
v3.7.304 (2026-07-17 — ช่องต้นทุนต่อแบบ ร้อน/เย็น/ปั่น ในฟอร์มเพิ่มสินค้า (ว่าง=ใช้ต้นทุนหลัก))
v3.7.303 (2026-07-17 — **ฟอร์มเพิ่มสินค้า: ติ๊ก ร้อน/เย็น/ปั่น + ราคาต่อแบบ → บันทึกครั้งเดียวสร้างสินค้าแยกต่อแบบ** (จงใจเป็นสินค้าปกติหลายตัว ไม่ทำกลไก variant — cart/สต็อก/ใบเสร็จ/QR ผูก productId เดี่ยวทั้งระบบ); i18n pos_var_* ×4; **บั๊กที่เจอ: เกต canSaveProduct บังคับราคาหลัก → แก้กติกาเดียวกับ submitProduct**)
v3.7.302 (2026-07-16 — พิลล์ยี่ห้อใน vmoto editor: กดสลับต้องยืนยัน "แน่ใจหรือยัง..." (askConfirm); กดพิลล์เดิมไม่ถาม)
v3.7.301 (2026-07-16 — **รูป 360° vmoto อัปเป็น WebP** (โปร่งใสรอด + เล็กกว่า JPEG); เบราว์เซอร์ทำ webp ไม่ได้ → เติมพื้นขาว+JPEG. root cause พื้นดำ: JPEG ไม่มี alpha. รูปเก่าต้องอัปใหม่)
v3.7.300 (2026-07-16 — ปุ่มลบทั้ง 5 ใน vmoto editor (ยี่ห้อ/รุ่น/สี/รูป/โลโก้) ต้อง askConfirm ก่อน danger:true)
v3.7.299 (2026-07-16 — **ปิด silent fallback ใน vmLoad หลัง incident sales_cfg โดนทับ** (กู้จาก backup แล้ว): โหลด error → โชว์ error+ลองใหม่ ไม่ render ปุ่มบันทึก)
v3.7.298 (2026-07-16 — ช่องชื่อสีใน vmoto editor แปลอัตโนมัติ 4 ภาษา (dictionary 13 สี ในตัว; คำลาว verified 3 ทาง); คำนอกลิสต์ copy เหมือนกันทุกภาษา)
v3.7.297 (2026-07-16 — ช่องชื่อ (ยี่ห้อ/รุ่น/สี) ใน vmoto editor พิมพ์แล้วทับทุกภาษา (proper noun); desc ยังแยกภาษา)
v3.7.296 (2026-07-16 — **vmoto sales page หลายยี่ห้อ**: editor มีแถบเลือกยี่ห้อ+เพิ่ม/ลบ, sales_cfg → {brands:[...]} (backward-compat flat เดิม); ฝั่งหน้าเว็บ vmoto-citi แถบสลับยี่ห้อบนสุด)
v3.7.289-295 (2026-07-14/15 — vmoto sales-page CMS ครบชุดใน POS settings ร้าน vmoto: รุ่น/สเปก/ราคา (เพิ่ม-ลบ-ซ่อนรุ่น-ซ่อนสเปกรายช่อง) + มัดจำ/บัญชี + โปรโม + สี/รูป 360° อัป Supabase Storage (bucket vmoto, RLS ต่อร้าน) + โลโก้ hero (PNG); ตาราง sales_cfg + RPC vmoto_public_cfg + storage migration ใน tools/migrations/)
v3.7.288 (2026-07-13 — **แก้ sw.js activate ลบ cache ของแอปอื่นทั้ง origin**: เดิม `k !== CACHE ? caches.delete(k)` กวาดทุก cache key บน origin — CacheStorage เป็นของทั้ง origin (parme.me) ไม่ใช่ของ SW scope → ถ้ามี PWA อื่น deploy ใต้ parme.me (เช่น borisat/vmoto) จะโดนลบ offline shell ทุกครั้งที่ parme activate. แก้: เพิ่ม `CACHE_PREFIX='parme-shell-'` + ลบเฉพาะ key ที่ขึ้นต้น prefix (sw.js header v2→v3). **verified fail→pass ในพรีวิว**: ก่อนแก้ activate ลบ `borisat-shell-1` จริง; หลังแก้ `borisat-shell-1` รอด + `parme-shell-vOLD`/เวอร์ชันเก่าถูกลบถูกต้อง + ทดสอบ offline จริง (หยุด server แล้ว reload — แอปเปิดจาก cache ได้เต็มหน้า). **ยังไม่ deploy** — รอเจ้าของยืนยัน; ⚠️ deploy ครั้งนี้ต้อง rsync **sw.js ด้วย** ไม่ใช่แค่ index.html. หมายเหตุ: ไฟล์งานตอนเริ่มอยู่ที่ v3.7.287 แล้ว (287 และก่อนหน้าเป็นงานค้าง session อื่น ยังไม่ถูกจด))
v3.7.285 (2026-07-11 — **ปุ่มดาวน์โหลดคีย์บอร์ดลาว (APK) ใน AdminDashboard**: ปุ่ม `<a href='/laokeyboard.apk' download>` ใน toolbar ข้างปุ่ม Export CSV (แสดงทุก tab, สไตล์เดียวกันแต่พื้นเขียว #1B4332) + i18n key ใหม่ `admin_download_laokb` ครบ 4 ภาษา. ไฟล์ APK อัปโหลดที่ VPS `/docker/parme-prod/html/laokeyboard.apk` (861KB, debug-signed จากโปรเจกต์ `~/laokeyboard` — คีย์บอร์ดลาว IME + voice typing แยกต่างหาก). **ปุ่มเห็นเฉพาะ admin แต่ตัวไฟล์เป็น static URL สาธารณะ** (ใครมีลิงก์โหลดได้ — ไม่ sensitive). nginx เสิร์ฟเป็น application/octet-stream (ไม่มี apk ใน mime.types — Android ติดตั้งได้ปกติ ไม่ได้แก้ nginx). verified หลัง deploy: live = v3.7.285, APK 200/861564 bytes, key อยู่ใน HTML live ครบ 5 จุด. **หมายเหตุ: บันทึก v3.7.271-284 ยังไม่ถูกจดโดย session ที่ทำ (ไม่ใช่งานรอบนี้) + repo ยังไม่ commit ตั้งแต่ v3.7.267 — รอบนี้จงใจไม่ commit เพื่อไม่พัดงานค้างของ session อื่นเข้า commit**)
v3.7.270 (2026-07-09 — เจ้าของเลือก routing เสียง: **ลาว→Gemini, ไทย/อังกฤษ/จีน→เอนจินในเครื่องฟรีเท่านั้น (ไม่วิ่ง Gemini)**. verified ทดสอบจริงบน VPS: Whisper self-host ลาวแย่ (ออกอักษรไทย/เวียดนาม คำเพี้ยน ช้า 8-15s) ส่วน Gemini ลาวออกอักษรลาวถูก → ลาวคง Gemini. แก้ startAiVoice: (1) ลาว + (iOS หรือไม่มี SR) → Gemini เลย (Apple ไม่มีลาว); ลาว + Android/desktop-Chrome → ลอง Web Speech ก่อน (Google ทำลาวได้ฟรี) แล้ว onerror→Gemini. (2) ไทย/en/zh: Web Speech ฟรี; ที่เครื่องทำไม่ได้ (iOS เพิ่มหน้าโฮม บล็อก / no SR) → toast บอกใช้ **ไมค์บนคีย์บอร์ด** (ฟรี) ไม่ fallback Gemini. **ผลข้างเคียงที่ต้องรู้: ไทยบน iPhone-เพิ่มหน้าโฮม ปุ่ม 🎤 ในแอปกดแล้วขึ้นฮินต์ให้ใช้ไมค์คีย์บอร์ดแทน (กดตรงๆไม่ถอดให้)** — ถ้าอยากให้กดแล้วทำงานเลยต้องยอมให้ fallback Gemini (~เศษสตางค์). ยังไม่ verify บน iOS/Android จริง (ไม่มีเครื่อง))
v3.7.269 (2026-07-09 — เจ้าของถาม "ทำ iPhone ให้ฟรีเหมือน Android (in-browser) ได้ไหม". **verified via websearch: iOS บล็อก webkitSpeechRecognition ใน standalone PWA (เพิ่มหน้าโฮม) — WebKit โยน error ทันทีไม่ขอไมค์; ทำงานได้เฉพาะเปิดใน Safari แท็บปกติ** (Apple limit แก้ด้วยโค้ดไม่ได้). แก้ startAiVoice: routing ใหม่ = ใช้ Web Speech (ฟรี) บน Android/desktop/**iOS-Safari-แท็บ**; เฉพาะ **iOS standalone PWA → server (Gemini)**. detect ด้วย navigator.standalone \|\| matchMedia('(display-mode:standalone)'). + onerror('language-not-supported'/'service-not-allowed'/'network')→ตกไป server (เช่นลาวบน iOS ที่เครื่องถอดไม่ได้). **ยังไม่ verify บน iOS จริง (ไม่มีเครื่อง)**. หมายเหตุกรอบใหญ่: Gemini เป็น free tier อยู่แล้ว (ไม่เสียเงิน) — โควตาหมดเมื่อวานเพราะผมเทสต์เอง ไม่ใช่ค่าใช้จ่ายปกติ)
v3.7.268 (2026-07-09 — เจ้าของรายงาน "ถอดเสียงมั่ว" หลังสลับหลักเป็น 3-flash-preview: ตรวจแล้วพบ **โควตา free tier ของวันหมด** (429 ทั้งสองโมเดล — จากการทดสอบ perf หลายสิบ call วันเดียว) ซ้อนกับ preview ที่ยังไม่พิสูจน์กับเสียงจริง. แก้: (1) **สลับหลักกลับ `gemini-2.5-flash`** (ตัวที่เจ้าของยืนยันกับเสียงจริงแล้ว) 3-flash เหลือเป็น fallback, (2) client: 429/all_models_failed → toast "โควตาเสียง AI วันนี้เต็ม" แทน "ฟังไม่ชัด" (เดิมหลอกให้ลองซ้ำ). **โควตารีเซ็ต ~14:00 ICT (เที่ยงคืน Pacific)**; ใช้จริงจัง scale ต้องเปิด billing GCP. ยังไม่ verify: คุณภาพ 3-flash กับเสียงจริง (เทียบไม่ได้จนโควตากลับ))
v3.7.267 (2026-07-09 — **ผู้ช่วย AI: iPhone ใช้เอนจิน Google ถอดเสียง**. iPhone เลือกเอนจิน Web Speech ไม่ได้ (WebKit บังคับ Apple/Siri — พังใน PWA + ไม่มีภาษาลาว) → เพิ่มเส้นทางอัดเสียง: iOS (หรือเครื่องไม่มี SR) กด 🎤 = MediaRecorder อัด (กดซ้ำ ⏹ หยุด, auto-stop 30s) → base64 → **edge fn ใหม่ `transcribe`** (`supabase/functions/transcribe/`) → Gemini `gemini-2.5-flash` (env GEMINI_MODEL override ได้) ถอดตรงตัว → เติมช่องพิมพ์. Android/desktop ใช้ Web Speech เดิม (Google engine ฟรีอยู่แล้ว). state aiVoice: false|'listen'|'rec'|'stt'. mime normalize mp4→aac ฝั่ง fn. **GEMINI_API_KEY ใส่แล้ว LIVE (2026-07-09)** — key จาก AI Studio บัญชี mahudone (Free tier, project gen-lang-client-0998319342; หมายเหตุ: key format ใหม่ของ Google ขึ้นต้น `AQ.` ไม่ใช่ `AIza`; key โผล่ในแชตแล้ว rotate ได้ที่ aistudio → ใส่ .env → restart functions). **e2e verified:** เสียงพูดไทยจริง (AAC/m4a แบบ iPhone) → `{ok:true,text:"บันทึกค่ากาแฟ 25,000"}` ถูกเป๊ะ; anon → 401. **perf fix (เจ้าของทักช้ามาก):** Gemini 2.5 เปิด thinking เป็น default → วัดจริง ~48s/ครั้ง; เพิ่ม `thinkingConfig:{thinkingBudget:0}` → ~2.5-3.5s. **รอบสอง (เจ้าของถามเร็วกว่านี้ได้ไหม):** สลับหลักเป็น `gemini-3-flash-preview` (วัด ~2-2.5s + ถอดครบกว่า — ได้คำ "กีบ" ที่ 2.5 ตัด) + **per-model timeout [12s,25s] + auto-fallback → 2.5-flash** (เจอจริง: preview ค้าง 58s → Kong ตัดเป็น 500 1/3 รอบ; หลังแก้ = แย่สุด ~15s ไม่มี 500; พิสูจน์ fallback ด้วย bogus model แล้ว). อย่าใช้ flash-lite (ตัดคำหาย); 2.0-flash โดน 429 บน free tier (ไม่มีโควตา). **ข้อจำกัดที่เหลือ = free tier rate limit**: ยิงถี่ (ทดสอบ 8 รอบติด) ชน 429 ทั้งสอง model → client เห็น "ฟังไม่ชัด"; ใช้จริงเว้นช่วงปกติไม่ชน แต่ถ้าผู้ใช้เยอะ/บ่อยต้องเปิด billing GCP (ถูกมาก ~$0.001/ครั้ง). เหลือทดสอบ iPhone จริงโดยเจ้าของ)
v3.7.266 (2026-07-09 — แก้ settings footer โชว์ "vv3.7.265": L24576 ต่อสตริง `"… · v", APP_VERSION` แต่ APP_VERSION มี 'v' อยู่แล้ว → ลบ 'v' ในสตริง. ตรวจ class ทั้งไฟล์: จุดต่อ APP_VERSION อื่น (8888/20024/22267) ถูกอยู่แล้ว; `sw.js?v=`+APP_VERSION (36706) เป็น cache-bust query param ไม่ใช่จอแสดงผล — ไม่แตะ. verified ใน demo: เปิด settings modal → footer = "Parme · ພາme · v3.7.266")
v3.7.265 (2026-07-09 — **window.confirm() → styled ConfirmDialog ครบ 12 จุด** (ปิดงานค้างจาก v3.7.264): component กลางใหม่ `ConfirmDialog` (~L8260, ก่อน AdminDashboard) หน้าตา/พฤติกรรมเดียวกับ askConfirm modal ของ POS (ครีม #FBF6E9, ปุ่ม danger #AA3C28 / ปกติ #1B4332, modalFade, zIndex 10080 เหนือ settings modal 9999) + รองรับ `\n` ใน message (pre-line, ใช้กับ admin ban โชว์ email). caller ถือ state เอง 3 component: **Parme** ×9 (เพื่อนลบ/ทริปปิด-ออก-settle×2-ยกเลิกคำขอ/reset settings/ลบทั้งหมด/ปิดบัญชีเงินกู้, mount ท้าย main return, lang ใน scope นี้คือ `settings.lang` ไม่ใช่ `lang`!), **FamilyScreen** ×2 (ออกกลุ่ม/ลบ tx, mount เฉพาะ detail return @~12234 — list return @~11458 ไม่มีจุดเรียก), **AdminDashboard** ×1 (ban/unban, confirmLabel=t.admin_ban/unban). ทุกจุด refactor sync→callback ทีละจุด (caller ทั้งหมดเป็น onClick ไม่มีใคร await). ปุ่มยืนยันใช้ i18n key เดิมทั้งหมด (pos_leave/pos_remove/reset_defaults_cta/delete_all_cta) ไม่เพิ่ม key ใหม่. **verified ใน demo:** reset settings (cancel+confirm→toast), delete-all (dialog+cancel), loan ปิดบัญชี (confirm→status paid). **ยังไม่ verified บนเครื่องจริง:** admin ban, family ออก/ลบ tx, ลบเพื่อน, ทริป 5 จุด (demo ไม่มีข้อมูล cloud — transform เหมือนกันทุกจุด + ตรวจ depth ของ mount ด้วย paren-counter แล้ว). พบ bug เดิมนอกขอบเขต: settings footer โชว์ "vv3.7.265" (L24576 ต่อ 'v'+APP_VERSION ซ้อน) — ยังไม่แก้)
v3.7.264 (2026-07-09 — **UI/pop-up sweep**: หน้าลูกค้า QR เลิกใช้ native alert() ทั้ง 3 จุด (สลิปใหญ่เกิน / ส่งออเดอร์ไม่สำเร็จ / เรียกพนักงานไม่สำเร็จ) → toast pill เข้าธีม (state qMsg+qToast, style เดียวกับ pill หลักแอป v3.7.259). ตรวจ pop-up in-app แล้ว (modal รายรับ-จ่าย/POS/QR modal/toast) สวยเข้าธีมอยู่แล้ว. **ค้าง: window.confirm() 12 จุด** (destructive actions ทั้งหมด — ลบบัญชี/ลบทั้งหมด/ออกกลุ่ม/ปิดทริป/ลบ tx/mark loan) ยังเป็นกล่องระบบดิบ → งานแยก: refactor sync→async ทีละจุด + verify ต่อ flow ก่อน deploy (ห้าม bulk))
v3.7.263 (2026-07-09 — **POS QR session-token**: เช็คบิลสำเร็จ → หมุน token โต๊ะนั้นทันที (QR เก่าตาย กันลูกค้าเก่าสั่งปนใหม่); staff โชว์ QR ใหม่จากในแอป (ไม่ใช้สติกเกอร์). server: `qr_order_allowed` +arg token (RLS เช็ค dinein token=table_tokens[โต๊ะ] ปัจจุบัน; ALTER POLICY atomic), RPC `set_table_token` (per-key jsonb merge, staff-only via is_pos_staff — กัน 2 เครื่องเขียนทับกันทั้งก้อน). client: rotate ตอน completeSale + move/merge(src) + ปุ่ม "สร้าง QR ใหม่" + mount-sync per-key union + genTok crypto8 + จอ "QR หมดอายุ" ตอน insert โดน RLS reject. SQL: tools/migrations/2026-07-09-qr-session-token.sql. **P0 verified:** anon อ่าน table_tokens ไม่ได้ (pos_shops_sel=owner/member). ผ่าน adversarial review 3-lens ก่อนลง + e2e (token เก่า→block, ใหม่→ผ่าน, merge ไม่ทับ, remote ไม่กระทบ). **client interactive UI (modal/rotate/expired) ยังต้อง test บนเครื่อง login จริง**)
v3.7.262 (2026-07-09 — เปลี่ยนป้ายปุ่ม AI จาก "ถามผู้ช่วย AI" → "ผู้ช่วย AI" (เอา verb ออกทั้ง 4 ภาษา: ຜູ້ຊ່ວຍ AI / ผู้ช่วย AI / AI assistant / AI助手))
v3.7.261 (2026-07-09 — เอาปุ่ม "🎤 เว้าบันทึก" (voice-add) ออกจากหน้าหลัก เพราะปุ่ม 🤖 AI ครอบหน้าที่นี้แล้ว (มีไมค์+สั่งเพิ่มด้วยภาษาธรรมชาติ). ลบเฉพาะ element ปุ่ม; โค้ด voice ที่เหลือ (startVoiceAdd/parseVoice/overlay/undo/state) กลายเป็น dead code ยังไม่ลบ (`voiceLangCode` ยังใช้อยู่โดย AI mic — อย่าลบ). ถ้าจะ purge dead voice code ค่อยทำแยก)
v3.7.260 (2026-07-08 — **AI CHAT AGENT**: ห้องแชท "🤖 ถามผู้ช่วย AI" หน้าหลัก ต่อยอดจาก voice. edge fn ใหม่ `assistant` (`supabase/functions/assistant/`, sonnet-5, **tool-use loop รันฝั่ง server**) — ตอบเรื่องเงิน (get_balance/summarize/search คำนวณจาก snapshot ที่ client ส่ง, edge fn ไม่แตะ DB) + เพิ่มรายรับ-รายจ่าย (add_transaction คืน adds[], **client persist เองใต้ RLS** + undo). auth gate role==='authenticated'. deploy: scp → `/docker/supabase/docker/volumes/functions/assistant/` + `docker compose up -d --wait functions`. เลข AI ตรงกับแอป (get_balance ใช้ stats.bal). cost สูงกว่า parse-expense (หลาย call/คำถาม))
v3.7.258 (2026-07-04 — แก้ login error message: อ่าน error จาก fErr.context (supabase-js invoke โยนเมื่อ non-2xx) — เดิม branch เป็น dead code โชว์ generic)

## Workflow การแก้โค้ด (สำคัญมาก)

> 🛠️ **มี helper scripts ใน `tools/` แล้ว** (ใช้แทนขั้นตอนมือด้านล่าง — ปลอดภัยกว่า):
> - `python3 tools/bump.py 3.7.xx` — bump ครบ 4 จุด อัตโนมัติ (assert จุดละ 1 ครั้งก่อนเขียน, ไม่แตะ comment ประวัติ; มี `--dry` ดูก่อนได้)
> - `python3 tools/check.py` — แยก block หลัก (ที่มี `APP_VERSION`) แล้วรัน `node --check` ให้เลย
> - `bash tools/release.sh 3.7.xx "สรุปสั้น"` — bump → check → พิมพ์ขั้นตอน deploy ทั้ง 5 ขั้น (เว็บ/commit/APK/app-version.json/ตรวจ hash) ไม่ deploy ให้เอง
> ขั้นตอนมือด้านล่างเก็บไว้เป็นเอกสารอ้างอิง/fallback ถ้า script มีปัญหา

### 1. Version bump — แก้ครบ **4 จุด** ทุกครั้งที่ release
แนะนำ: `python3 tools/bump.py 3.7.xx` (จัดการให้ครบ + ปลอดภัย). ถ้าทำมือ:
ก่อนแก้ให้ `grep -n` หาเลขบรรทัดสดเสมอ (เลขบรรทัดขยับได้):
```
grep -n "v3\.7\.35" index.html
```
จุดที่ต้องแก้ (ตำแหน่งโดยประมาณ):
1. `<title>ພາລວຍ · Parme v3.7.xx</title>` (~บรรทัด 6)
2. `<meta name="app-version" content="v3.7.xx" />` (~บรรทัด 8)
3. `<div class="version-tag">v3.7.xx</div>` (~บรรทัด 872)
4. `const APP_VERSION = 'v3.7.xx';` (~บรรทัด 3753)

⚠️ ระวัง comment ประวัติ เช่น `// v3.7.34 — ...` กระจายอยู่ในโค้ด — **ห้ามนับรวม** ตอน bump ให้แก้เฉพาะ 4 จุด canonical ข้างบน

### 2. ตรวจ syntax หลังแก้ทุกครั้ง
แนะนำ: `python3 tools/check.py`. (กลไกข้างใต้: `index.html` มี `<script>` ~8 blocks; block หลักของแอป
ที่มี logic ทั้งหมดคือ block ที่มี `APP_VERSION` ขนาด ~1.6MB — script แยก block นั้นออกมารัน `node --check` ให้)
หลังแก้โค้ดต้องผ่าน `node --check` ก่อน commit เสมอ

ตัวอย่างสคริปต์แยก + เช็ก (Python):
```python
import io
src = io.open('index.html', encoding='utf-8').read()
idx = 0
while True:
    s = src.find('<script', idx)
    if s == -1: break
    gt = src.find('>', s); e = src.find('</script>', gt)
    c = src[gt+1:e]
    if 'APP_VERSION' in c:   # block หลักของแอป
        io.open('/tmp/app.js', 'w', encoding='utf-8').write(c)
    idx = e + 9
```
แล้ว `node --check /tmp/app.js`

### 3. การแก้ string — assert ก่อนเขียนเสมอ
ก่อน replace ให้นับจำนวน occurrence ให้ตรงตามคาด ป้องกันแก้ผิดจุด (ไฟล์ใหญ่ string ซ้ำได้ง่าย) ใช้ Edit tool ของ Claude Code โดยใส่ context รอบ ๆ ให้ unique หรือถ้าแก้หลายจุดพร้อมกันใช้ Python heredoc ที่ assert count ก่อน

### 4. Deploy (v3.7.249+ — prod อยู่บน VPS แล้ว)
prod ตัวจริงคือ nginx บน VPS — **push GitHub อย่างเดียวไม่ deploy แล้ว** ต้อง rsync ด้วย:
```
# 1) deploy จริง (VPS)
rsync -e "ssh -i ~/.ssh/hostinger_vps_ed25519" -av ~/parme/index.html root@187.127.209.83:/docker/parme-prod/html/index.html
# 2) verify
curl -s https://parme.me/ | grep -o 'app-version" content="[^"]*'
# 3) เก็บ repo ให้ตรง prod (+GitHub Pages fallback)
git add -A && git commit -m "v3.7.xx — <สรุปสั้น>" && git push
```
**ถ้าปล่อย APK ใหม่ด้วย (v3.7.411+) ต้องทำอีก 2 ขั้น** ไม่งั้นแอปเนทีฟไม่รู้ว่ามีของใหม่ —
แบนเนอร์ในแอปเทียบ `versionCode` ของตัวเองกับไฟล์ `app-version.json` บนเว็บ:
```
# 4) APK (หลัง bump versionCode ใน ~/parme-app/android/app/build.gradle แล้ว build)
scp -i ~/.ssh/hostinger_vps_ed25519 ~/parme-app/android/app/build/outputs/apk/release/app-release.apk root@187.127.209.83:/docker/parme-prod/html/parme.apk
# 5) ประกาศเวอร์ชันให้แอปเนทีฟเห็น (versionCode ต้องตรงกับ APK ที่เพิ่งอัป)
printf '%s' '{"versionCode":19,"versionName":"3.7.411","url":"https://parme.me/parme.apk"}' > /tmp/av.json
scp -i ~/.ssh/hostinger_vps_ed25519 /tmp/av.json root@187.127.209.83:/docker/parme-prod/html/app-version.json
```
⚠️ `app-version.json` **ต้องมี CORS** (`Access-Control-Allow-Origin: *` ใน `/docker/parme-prod/default.conf`)
เพราะแอปฝังไฟล์มี origin `https://localhost` → อ่านข้ามโดเมน. ถ้ากฎนี้หาย แบนเนอร์จะเงียบด้วย `TypeError: Failed to fetch`
✅ `tools/release.sh` (เขียนใหม่ v3.7.412) พิมพ์ทั้ง 5 ขั้นให้พร้อมค่าที่กรอกไว้แล้ว รวมทั้งดึง `versionCode` จาก APK ที่ build จริง
และขั้นตรวจ hash เทียบกับไฟล์บนเซิร์ฟเวอร์ — แต่มันยัง **ไม่ deploy ให้เอง** (โดยเจตนา) ต้องรันคำสั่งที่มันพิมพ์ออกมา
nginx ส่ง `Cache-Control: no-cache` ให้ HTML/sw.js แล้ว (ตั้งใน `/docker/parme-prod/default.conf` 2026-07-04) → ผู้ใช้ได้เวอร์ชันใหม่ตอนเปิดแอปรอบถัดไป. ถ้าแก้ schema DB → รัน SQL บน VPS (`docker exec supabase-db psql -U postgres`) + เก็บไฟล์ใน `tools/migrations/` + **อย่าลืม `NOTIFY pgrst, 'reload schema';`** ไม่งั้น PostgREST ไม่รู้จักคอลัมน์ใหม่

## โครงสร้าง Supabase (ตารางหลัก)
ฝั่งการเงิน: `user_settings` (มี `fx_manual` jsonb สำหรับ FX override sync), `families`, `family_transactions`, `family_members`, `family_messages`, `family_ious`
ฝั่ง POS: `pos_products` (มี `sort`, `station`), `pos_categories` (มี `station`), `pos_sales` (มี `table_no`, `sub_total`, `discount`, `svc`, `svc_pct`), `pos_shops` (มี `table_tokens`, `seeded`), `pos_qr_orders`, `pos_open_bills`

⚠️ การเพิ่ม column ต้องรัน SQL ใน Supabase เอง (M รันเอง) ใช้ `ADD COLUMN IF NOT EXISTS` เสมอเพื่อความปลอดภัย

## ระบบ FX (สรุปจากงานล่าสุด v3.7.35)
- auto-rate เป็น **kip-based** (กีบต่อ 1 หน่วย) ดึงจาก jsDelivr/er-api
- `fxKipPerUnit(st, sym, codeHint)` → กีบต่อหน่วย
- `fxToPrimary(st, sym, codeHint, primarySym, primaryCodeHint)` → **สกุลหลักต่อหน่วย** (ใช้กีบเป็นสะพาน) ถ้าสกุลหลัก = กีบ จะได้ผลเท่า fxKipPerUnit เป๊ะ
- กราฟใช้คอลัมน์ `fxk_<sym>` = ค่าที่แปลงเป็นสกุลหลักแล้ว, tooltip แสดง `≈ CUR fmt(...)`

## Demo mode (v3.7.88+) — เปิด UI หลัง login ได้โดยไม่ต้องล็อกอินจริง
เพิ่ม `?demo=1` ใน URL → bypass Supabase auth + seed localStorage (7 transactions + 2 IOUs) → เปิดหน้าหลัก/รายงาน/Family/POS ในพรีวิวได้เลย ไม่กระทบ Supabase production. กลไก: `__fakeSupabase` stub (chainable .from() คืน empty array) + `__DEMO__` ตรวจ URL + early-return ใน auth useEffect + loadCloudData. ใช้สำหรับ Claude ตรวจ UI/UX bug ในพรีวิวก่อน deploy.

## ⚠️ ต้องรัน SQL (v3.7.98–99) — POS ลูกค้าเชื่อ + วิธีจ่าย
1. `tools/migrations/2026-06-17-pos-customer-credit.sql` — เพิ่มคอลัมน์ `paid`/`customer`/`settled_at` ใน `pos_sales`. **สำคัญ:** client v3.7.98+ ส่ง 3 คอลัมน์นี้ในทุก upsert ของการขาย → ถ้ายังไม่รัน SQL การ sync ขายขึ้น cloud จะ fail เงียบ ๆ (try/catch — ข้อมูลไม่หาย เซฟ local ปกติ แต่ไม่ขึ้นเครื่องอื่นจนกว่าจะรัน SQL). ฟีเจอร์ "ลงเชื่อ" verified ใน demo แล้ว (create→panel→settle ครบ).
2. `tools/migrations/2026-06-17-pos-payment-method.sql` — เพิ่มคอลัมน์ `payment` (text = cash/transfer/null) ใน `pos_sales`. client v3.7.99+ ส่ง `payment` ทุก upsert → เหตุผลเดียวกับข้อ 1 (ต้องรันคู่กัน). ฟีเจอร์ "ระบุวิธีจ่าย" เปิด/ปิดได้ในตั้งค่า POS (toggle เก็บ localStorage `parme_pos_pay_mode`); ปิดอยู่ → `payment` = null. verified ใน demo (เปิด toggle → ขาย→chooser→cash/transfer → report แยกชิป 💵 เງິນສົດ / 📱 ໂอน).

## POS Tier 1 (เสริมร้าน POS — จาก audit)
- ✅ **#1 ลูกค้าเชื่อ** (v3.7.98) — ดู section "ต้องรัน SQL" ด้านบน
- ✅ **#2 ระบุวิธีจ่าย เงินสด/โอน + toggle** (v3.7.99) — ดู section "ต้องรัน SQL" ด้านบน
- ✅ **#3 Export CSV** (v3.7.100) — **client ล้วน ไม่แตะ DB ไม่ต้องรัน SQL**. ปุ่ม 📤 ในแถบ range ของ PosReport → ดึงบิลตามช่วงที่เลือก (วัน/เดือน/ปี/ทั้งหมด) เป็น CSV. 1 แถว/บิล, 15 คอลัมน์ (วันที่/เวลา/รหัสบิล/โต๊ะ/รายการ/ยอดย่อย/ส่วนลด/ค่าบริการ/รวม/ต้นทุน/กำไร/การจ่าย/สถานะ/ลูกค้า/ผู้ขาย). มี BOM (Excel อ่าน Lao/Thai ได้), CSV-injection guard (=+-@ → prefix '), RFC-4180 escaping, CRLF, ตัวเลขดิบ (ไม่ใช้ fmt). iOS: ลอง `navigator.share({files})` ก่อน → fallback `<a download>`. ใช้ inline `T()` ล้วน (PosReport ไม่มี `t` ใน scope). helper `exportCsv`/`filtRows`/`csvCols` อยู่ใน PosReport (~L30075). verified ใน demo (จับ blob จริง: header ลาวถูกครบ + escaping ผ่าน adversarial name `=Boon, "VIP"`).
- ✅ **#1b ลูกค้าเชื่อ — กดดูรายบิล + แก้ไข** (v3.7.101) — **client ล้วน ไม่ต้องรัน SQL** (reuse คอลัมน์ paid/settled_at เดิม). แตะแถวลูกค้าในการ์ด `ລູກຄ້າເຊື່ອ` (มี chevron ›) → bottom-sheet (zIndex 500) โชว์บิลค้างรายใบ (วันเวลา + รายการ + ยอด). ต่อบิล: **✓ ຮັບເງິນ** = settle เฉพาะบิลนั้น (จ่ายบางส่วน — fn ใหม่ `posSettleCreditBill(saleId)` mirror `posSettleCredit` **ไม่เรียก applyTotalsDelta** เพราะ rev นับ unpaid อยู่แล้ว → settle ไม่ขยับรายได้ แค่ Owed ลด), **✏️ ແກ້ໄຂ** = ดูข้อ #1c, **🗑️ ລຶບ** = two-tap confirm (ไม่มี askConfirm/showToast ใน PosReport) → `posDeleteSale` (ลบคืน stock + reverse totals → รายได้ลด). settle/delete บิลสุดท้าย → useEffect auto-close sheet. edit/delete gate ด้วย `amOwner`; settle ไม่ gate. verified ใน demo ครบ.
- ✅ **#1c แก้บิลเชื่อในหน้าขาย** (v3.7.102) — **client ล้วน ไม่ต้องรัน SQL**. กด ✏️ ในแผ่นรายละเอียด → ปิดแผ่น แล้ว `onEditBill(saleId)` (ใน POSModal) สร้าง **บิลชั่วคราว** `{name:'__edit__', table:null}` โหลด items→cart map (กรอง productId ที่มีจริง, นับ `editDropped` ที่หาย=สินค้าถูกลบ) → `setActiveBillId` + `setTab('sell')`. หน้าขายโหมดแก้: bill-chips strip ถูกแทนด้วย **banner "ກຳລັງແກ້ໄຂບິນ"** (+ ⚠️ ถ้า editDropped>0) + ปุ่มยกเลิก; แถว checkout เปลี่ยนเป็นปุ่มเดียว **💾 ບັນທຶກການແກ້ໄຂ** (ซ่อนปุ่ม credit + complete → ไม่มีทางสร้างบิลใหม่). `saveEdit` → `onEditSale(saleId,{items, seller:null})` (posEditSale spread `...old` → paid:false/customer/payment/table/createdAt/id คงไว้; seller:null → คง old.seller; ถ้า editDropped>0 askConfirm ก่อน). `exitEdit` ลบบิล `__edit__` + คืน `prevActiveBillId` + `setTab('report')`. bills loader กรอง `__edit__` ออกกัน remount ค้าง. **ข้อจำกัด:** posEditSale คิด total = Σ price×qty (ไม่ re-apply discount/svc เดิม — quirk เดียวกับ modal เก่า) จึงไม่เปิดให้แก้ส่วนลด/โต๊ะในโหมดนี้. verified ใน demo: load→cart, เพิ่มสินค้า→save = 1 บิล 2 รายการ ยังเชื่อ rev ถูก · cancel ไม่กระทบบิล.
- ✅ **#1d รวม edit เป็น `startBillEdit` เดียว** (v3.7.106) — extract logic โหลด-เข้า-ตะกร้าเป็น `startBillEdit(saleId)` ใน POSModal. ทั้ง **ปุ่ม ✏️ ในรายการประวัติบิล** (เดิมเปิด edit modal เล็ก qty-only ที่ `setEditSale`, ~L33086) **และ** onEditBill ของแผ่นลูกค้าเชื่อ เรียก `startBillEdit` ร่วมกัน → แก้บิลไหนก็เด้งหน้าขาย เพิ่ม/แก้สินค้าได้เหมือนกันหมด. edit modal เก่า (editSale ~L33270) กลายเป็น dead code (ไม่มี opener แล้ว — ปล่อยไว้ ไม่ลบ กันเสี่ยง). verified ใน demo: ปุ่มประวัติบิล→หน้าขาย, เพิ่ม Pepsi→save = บิลเดิมอัปเดต Beerlao×2+Pepsi ₭24,000 paid คงไว้ 1 บิล.

## perf — CLS 0.48→0.001 (v3.7.105)
loading screens (`authState==='loading'` ~L17664 + `cloudLoading` ~L18239) เคยใช้ `minHeight:100vh`+`justify-content:center` (โลโก้กลางจอ, อยู่ใน flow). พอ render เปลี่ยนเป็น dashboard React **reuse DOM node** ย้ายโลโก้กลางจอ→บนสุด = เนื้อหากระโดด ~259px = **CLS 0.48 (แดง)**. แก้: ทำ splash เป็น `position:fixed; inset:0` (ออกจาก flow) + `key:'authloading'` (บังคับ unmount สะอาด ไม่ reuse node → dashboard = เนื้อหาใหม่ ไม่ใช่ "ย้าย"). วัดด้วย buffered `layout-shift` PerformanceObserver: 0.4804→0.0013. **LCP ~2.7s บนเครื่องจริงเป็นข้อจำกัดของ single-file 4.2MB (parse+mount) — ไม่แก้ตอนนี้.**

## bugfix สำคัญ — realtime mapSale ตัด paid/customer (v3.7.104)
`mapSale` (~L17166) = mapper สำหรับ **realtime delta-merge** (`mergeRow`) ของ `pos_sales`. เขียนไว้ก่อนมีฟีเจอร์ลูกค้าเชื่อ/วิธีจ่าย → **ไม่มี paid/customer/payment/settled_at**. อาการ: แก้/settle/สร้างบิลเชื่อ → Supabase realtime echo กลับ → `mergeRow`→`mapSale` ตัด paid/customer ทิ้ง → in-memory บิลกลายเป็น paid:undefined → `creditBy` (เช็ก `paid===false`) ไม่นับ → **บิลเชื่อหายจากการ์ดทันทีหลังแก้** จนกว่าจะ refresh (loadShopData mapping เต็ม อ่าน cloud กลับมา). **ไม่เกิดใน demo** เพราะ fake supabase channel เป็น no-op (mergeRow ไม่ทำงาน) — เป็นเหตุผลที่เทสต์ demo ผ่านตลอด. แก้: เติม `paid/customer/payment/settledAt` ใน mapSale ให้ตรงกับ loadShopData (16901). **บทเรียน: เพิ่ม column sale ใหม่ ต้องอัปเดต 3 ที่ — upsert mapping (16846), loadShopData mapping (16901), mapSale realtime mapper (17166).**

## bugfix สำคัญ — empty-cloud overwrite (v3.7.103)
`loadShopData` (~L16877) เคยใช้ `if (prods) setPosProducts(...)` — แต่ `[]` เป็น **truthy** → ถ้า cloud คืน array ว่าง (sync ยังไม่เสร็จ/ล้มเหลว/seed ตอน posShopId ยัง null) จะ**เขียนทับสินค้า local เป็นว่าง** = สินค้า/บิลหายหลัง refresh (เฉพาะตอนล็อกอิน+มีร้าน; เส้นทางไม่มีร้านปลอดภัยเพราะ loadShopData ไม่รัน). แก้: `if (prods && prods.length)` กับ **products + categories + sales** (cloud ว่าง = ไม่ลบ local). ⚠️ ทดสอบ demo ไม่ได้ (ไม่มีร้าน) — verify เครื่องจริง. **เหลือ:** ถ้า cloud ไม่ว่างแต่ขาดบิลที่ sync ไม่ขึ้น (เช่นยังไม่รัน SQL credit/payment → upsert fail) ก็ยังโดนทับได้ → ทางแก้คือรัน SQL ให้ครบ.

## v3.7.249 (2026-07-04) — audit ใหญ่ 4 agents + ซ่อมยกชุด
**ซ่อมแล้ว (deploy บน VPS แล้ว):**
- **dead writes 14 จุด** — supabase-js builder ไม่ถูก `await`/`.then` = ไม่ยิง HTTP เลย: sync `pos_ingredients` (ตายสนิททั้ง feature), reset POS wipe cloud, ตั้งค่าร้าน 7 จุด (fifo/stock/table_count/units), ลบ ghost bill `__edit__`. แก้: `.then(r=>{if(r&&r.error)...})` + enqueue สำหรับ ingredients. **บทเรียน: `supabase.from(...)` ต้องมี await/.then เสมอ**
- **offline tx queue** — (a) แก้ไขรายการเก่าตอน offline โดนทับหาย: เพิ่ม pending-edit protection (local ชนะ cloud) ใน poll merge 3 path + loadCloudData, (b) เลิก `txPendingClear()` เหมา, (c) จำกัด re-push local-only เฉพาะ pending∪สร้างใหม่<72ชม. (กัน tx ที่ลบจากเครื่องอื่นคืนชีพ), (d) 'online' event flush คิว POS ด้วย + timer 30s (`window.__parmeFlushTimer` + event `parme-auto-flush`)
- **timezone** — `todayStr()` เดิม UTC → ก่อน 7 โมงเช้า รายการ/ยอดขายลงวันเมื่อวาน. แก้เป็น local (+รับ Date param) + จุด inline อีก 6 (famDate/dk/ds/todayS/dob max). RPC ฝั่ง DB ใช้ Asia/Bangkok อยู่แล้ว
- **auth watchdog** — session มาช้ากว่า 6s watchdog → token ถูกทิ้ง → realtime ค้าง anon (SUBSCRIBED แต่เงียบ). แก้: early-return branch ยัง setAuth ให้ realtime
- **stale-response guards** — `posShopIdRef` + guard ใน loadShopData/loadPosTotals (สลับร้านเร็ว = ข้อมูลข้ามร้าน), double-tap guard `completeRemoteSale`, recipe-type ไม่คืน product stock ตอน void/edit, `computeShares` โยนเศษปัดให้แชร์ใหญ่สุด (Σ = ยอดจริง)
- **linkedTxId round-trip** — คอลัมน์ใหม่ `pos_sales.linked_tx_id` (SQL: `tools/migrations/2026-07-04-pos-sales-linked-tx.sql`, รันบน VPS แล้ว + NOTIFY pgrst) + ใส่ครบ 4 mappers → ลบ/แก้บิลที่ผูกรายรับ ตามไปจัดการ tx ถูกแล้ว (เดิมค่าอยู่แค่ local โดน sync ทับหาย = ยอดค้างเกิน)
- **dead code ~293 บรรทัด** — modal แก้บิลเก่า (editSale), goal UI เก่า (bar/goalCard/short/ring/goalRing), orphan states 12 ตัว. `CATALOG_BASE` → `./catalog/` (เดิมชี้ github.io). เหลือ `rotateToken` (อาจเป็นฟีเจอร์อนาคต — ถาม M ก่อนลบ)
- **nginx VPS** — เพิ่ม `Cache-Control: no-cache` ให้ HTML/sw.js (เดิมไม่มี header → heuristic cache → ติดเวอร์ชันเก่า), catalog 7 วัน

## v3.7.250 (2026-07-04) — เก็บ follow-up รอบสอง
- **POS เปิดตอน offline ได้แล้ว** — resolve ร้านล้ม (เน็ตล่ม) → กู้จาก `parme_pos_shops_cache` (เก็บ list+myId+user ทุกครั้งที่ resolve สำเร็จ ได้สิทธิ์ owner ถูกต้อง) + state `posShopDegraded` + auto-retry ตอน 'online' event
- **self-heal สินค้า/หมวด** — push คืน cloud เฉพาะ id ที่ค้างคิว `pendingUpsertIds()` (เดิม push ทุกตัวที่ cloud ไม่มี = ของที่ลบจากเครื่องอื่นคืนชีพ) — local merge ยังคงเดิม
- **แก้บิลเก่าไม่ reprice แล้ว** — `editPriceOv` ใน cartItems: สินค้าที่อยู่ในบิลเดิมใช้ราคา/ต้นทุน "ตามบิล" (จอ+ยอด+เซฟตรงกันหมด) ของเพิ่มใหม่ใช้ราคาปัจจุบัน
- **QR order status** — helper `qrOrderUpdate(id, patch)` retry×3 backoff แทน `.then(()=>{})` ทั้ง 8 จุด (accepted/rejected/done/paid/expired/patch)
- **localStorage เต็ม** — `saveTxStore` fallback เซฟแบบตัดรูปแนบ (รูปอยู่บน cloud row แล้ว) แทนเงียบทั้งก้อน
- **stale guards** — `loadReqRef` (loadDetail: เปิดครอบครัวซ้อน/ปิดแล้วไม่เด้งคืน), `openRef` guard ใน loadPOS, `currentUserIdRef` guard ใน loadCloudData (สลับบัญชีเร็ว)

## v3.7.251 (2026-07-04) — ซ่อม regression จาก self-review (workflow 6-มิติ + adversarial verify)
adversarial self-review ของ diff v3.7.248→250 (demo smoke ไม่ครอบ sync/offline เพราะ fake supabase) เจอ 6 regression **ทั้งหมด medium/low** (qrOrderUpdate scope + dead-code deletion ผ่านสะอาด; editPriceOv โดน refute = ถูกอยู่แล้ว). ซ่อมที่ราก:
- **todayStr แปลง local ไม่ครบ (4 จุด):** v3.7.249 แปลง write-side เป็น local แต่ read-side ยัง UTC → ปนกัน. แก้: POS `date` mappers 3 จุด (17969/18049/18279) → `todayStr(new Date(r.created_at))`; family month-key `mk` (11420) → `todayStr().slice(0,7)`. **บทเรียน: แปลง timezone ต้องแก้ทั้ง write + read + ทุก bucket key พร้อมกัน** (sale.date ของ pos_sales ไม่ persist — derive จาก created_at ทุกครั้งที่โหลด)
- **tx queue race (ราก):** `pushPendingTx` เดิม `txPendingWrite([])` ล้างทั้งก้อนหลัง await → id ที่ persist() append ระหว่าง upsert หลุด = tx ค้างถาวร. แก้: re-read คิวแล้วลบเฉพาะ id ที่ส่งเสร็จ (done=dead∪success) → ปิด race ที่ทำให้ 72h gate ทำ row หาย
- **computeShares orphan:** residual (v3.7.249) โยนเศษให้คนใหญ่สุด แต่ถ้ามี member ถูกถอด (id ยังค้างใน item.memberIds จาก addIouItem ที่ seed allMemIds) จะโยนทั้งก้อน = overcharge debtor. แก้: filter id นอกวงก่อนหาร → per ถูก residual เป็นเศษจริง. **พิสูจน์เชิงตัวเลข node แล้ว 5 เคส** (orphan 90k/2 = 45k/45k ไม่ใช่ 60k)
- **POS degraded iOS:** เพิ่ม visibilitychange fallback (iOS มักไม่ยิง 'online' → กลับมา foreground ก็ re-resolve ร้าน)

## v3.7.252 (2026-07-04) — completeness sweep (critic catch)
หลังซ่อม v3.7.251 ทำ completeness sweep timezone (grep ทุก `slice(0,10)`/`slice(0,7)`/`created_at` ที่เป็น date bucket) → เจอ straggler ที่ review 6-มิติ**พลาด**: **day-chart bucket key** (report กราฟ "มื้/วัน", FamilyScreen ~11651 + Parme ~21551) ยังสร้าง key ด้วย `d.toISOString().slice(0,10)` = UTC ขณะที่ tx match ด้วย `t2.date` (local หลัง v3.7.249) → tx วันนี้หายจากกราฟช่วง 00:00–07:00 (UTC+7). แก้: `ensure(todayStr(d), ...)`. **week/month/year ปลอดภัยอยู่แล้ว** (key กับ matcher ใช้ transform เดียวกัน — week: `mon().toISOString()` ทั้งคู่; month/year: getFullYear/getMonth local ทั้งคู่). **บทเรียนย้ำ: หลังแปลง timezone ต้อง grep completeness ทุก bucket-key builder เทียบ basis กับตัวที่มัน match — dimension review อาจโฟกัสจุดใหญ่แล้วพลาด bucket ย่อย**

## v3.7.253 (2026-07-04) — Security + money-math audit (workflow 5-มิติ + skeptic verify + live-DB exploit test)
audit มิติที่ยังไม่เคยตรวจ: RLS / SECURITY DEFINER / XSS / family+FX+loan math / POS money. **RLS เปิดครบทุกตาราง, mapper-drift + XSS สะอาด.** เจอ 15 issue (2 critical, 6 high) — **reproduce exploit บน prod จริงก่อนแก้** (test harness node+supabase-js mint JWT):

**✅ อุดแล้ว (DB — apply บน VPS, verify exploit blocked + legit flow works):**
- 🔴 **CRITICAL family_members self-join** — fm_ins policy เช็คแค่ user_id ไม่เช็ค family_id → ใครก็ INSERT เข้าครอบครัวไหนก็ได้ (ไม่ต้องมี code) → เห็น/แก้เงินครอบครัวคนอื่นทั้งหมด. แก้: DROP fm_ins (join ผ่าน create_family/join_family RPC SECURITY DEFINER เท่านั้น). **บทเรียน: exploit ตัวแรก 403 เพราะ `return=representation` (red herring) — insert ปกติ 201 = จริง! ต้องเทสต์หลายรูปแบบ**
- 🟠 **ft_upd** ไม่มี WITH_CHECK → ย้าย tx เข้าครอบครัวคนอื่น. แก้: + WITH CHECK is_family_full
- 🟡 set_family_currency/goals(4,5-arg) is_family_member→is_family_full (POS-only seller เปลี่ยนสกุล/เป้าไม่ได้แล้ว)
- 🟡 pos_sales_totals today_rev UTC→Asia/Bangkok (ตรง client local)
- SQL record: `tools/migrations/2026-07-04-security-rls-secdef.sql`; body ใหม่บน VPS `/docker/sec-func-fixes.sql`; rollback `/docker/backups/rollback-security-20260704.sql`

**✅ อุดแล้ว (client v3.7.253):**
- 🟠 family `inToday` (daily goal) ไม่ filter สกุลเงิน → +isPrimaryTx
- 🟠 posEditSale ลบส่วนลด/svc ตอนแก้บิล → ยอด+รายได้พองเท่าส่วนลด. แก้: คง old.discount/svc/svcPct, total = subTotal−disc+svc (verify: บิล 100k ลด 20k แก้ไม่เปลี่ยน = คง 80k)
- loan-IOU header (low) สรุปยอดไม่รวมดอก ต่างจากแถว → iouOwed() interest-aware

**⚠️ ยังไม่แก้ — ต้องเจ้าของตัดสินใจ/งานใหญ่ (เรียงตามคุ้ม):**
1. ✅ **~~get_email_by_username (high, PII)~~ ปิดสมบูรณ์ 2 phase (v3.7.255→256):**
   - Edge Function `login-username` (`supabase/functions/login-username/`, deploy บน VPS): resolve อีเมลฝั่ง server (service role) → GoTrue `/token` ส่ง captcha ต่อ → คืน session ไม่คืนอีเมล
   - client username login ใช้ edge fn (setSession), ไม่เรียก get_email แล้ว (v3.7.256 ถอด fallback)
   - **REVOKE EXECUTE get_email_by_username FROM anon,authenticated,public** (เหลือ postgres+service_role). **verify prod: anon เรียก = 403 permission denied; edge fn (service role) ยัง resolve+ถึง GoTrue = username login ทำงาน.** เจ้าของยืนยัน browser login ผ่าน
   - **หมายเหตุ:** captcha ทำให้ Claude เทสต์ full login เองไม่ได้ (mint JWT bypass ได้แค่ RLS ไม่ใช่ signin) → ใช้ 2-phase + fallback ปลอดภัย. **เหลือ:** username_taken/search_user_by_username ยัง leak การมีตัวตน (username → มี/ไม่มี) — พิจารณาแยก (severity ต่ำกว่า ไม่ leak อีเมลแล้ว)
2. ✅ **~~sync_loan_payment (high)~~ แก้แล้ว v3.7.257:** overload 5-arg รับ `p_paid` (client ส่ง `computeLoanState.remaining<=0` = นับดอก) แทนเทียบเงินต้นเปล่า; เก็บ 4-arg เดิม (client เก่าไม่พัง). client 2 call sites ส่ง p_paid. **e2e-test live DB ผ่าน** (p_paid=false+total=principal→mirror ไม่ flip; p_paid=true→flip)
3. ✅ **~~pos_products anon cost/recipe leak~~ แก้แล้ว v3.7.254** — RPC `qr_menu(p_shop)` คืนเฉพาะคอลัมน์ปลอดภัย + client QR menu (L31682) ใช้ RPC + DROP qr_anon_read_products/categories. เจ้าของอ่านร้านตัวเองผ่าน pos_products_all/pos_categories_all (scoped) ไม่กระทบ. verify: anon อ่าน cost ตรง=[], RPC ได้เมนูไม่มี cost, ปิด authenticated cross-shop ด้วย. **⚠️ QR customer menu ควรให้เจ้าของสแกนเทสต์จริง 1 ครั้ง** (demo ไม่ครอบ path ลูกค้า)
4. ✅ **~~FIFO COGS (medium)~~ แก้แล้ว v3.7.257:** costTotal fifo mode = `fifoConsume().cogs` (unit-test node ผ่าน). average mode ไม่แตะ. 0 ร้าน fifo = latent
5. ✅ **~~FIFO void-restore~~ แก้แล้ว v3.7.257:** เก็บ used layers/item → posDeleteSale prepend คืน. **⚠️ posEditSale ยังไม่คืน layer** (edit qty fifo = residual แคบ)
6. 🟢 **credit payment split (low):** settle บิลเชื่อไม่บันทึกวิธีจ่าย → chip เงินสด+โอน ไม่ตรง revenue (CSV ยัง reconcile)

**follow-up ที่เหลือ (design change — ต้องเทสต์ร้านจริง/ตัดสินใจก่อนทำ):**
1. **soft-delete tombstones** (`deleted_at` ทุกตาราง sync) — ลบข้ามเครื่องยัง propagate ผ่าน realtime เท่านั้น (cursor poll มองไม่เห็น delete; local-only ghost ค้างบนเครื่อง stale จนกว่า realtime จะจับ). ต้องแก้ DB schema + ทุก delete path + filter ทุก query
2. **atomic stock decrement RPC** — สองเครื่องขายพร้อมกันยัง lose update (upsert absolute). ควรมี `decrement_stock(product_id, qty)` + เปลี่ยน checkout path; กระทบคิว offline (เก็บ delta แทน absolute)
3. **`game_sessions` realtime ไม่มี filter** — fan-out ทุก event × ทุก client (filter ด้วย array ไม่ได้ — ต้องเปลี่ยน schema เป็น notify-row ต่อ user หรือยอมรับจนกว่าผู้ใช้เกมจะเยอะ)
4. duplicate report block ×2 (~130 บรรทัด family vs personal) — refactor เมื่อสะดวก
5. `rotateToken` (QR token rotation) เขียนไว้ไม่มี UI เรียก — ถาม M: ฟีเจอร์อนาคตหรือลบ

## งานค้าง (ณ v3.7.103)
- Tier 1 #4+ (ถ้าจะทำต่อ): ดูจาก POS audit — เช่น พิมพ์ใบเสร็จ/แชร์, สต็อกสินค้า, กะ/รอบขาย ฯลฯ
- ขยาย product catalog (รับรูป → ลบพื้นหลังดำ → webp 256px q80 → อัปเดต `catalog/catalog.json` + zip ไม่ต้อง bump แอป) — *ต้องมีรูปจริงจาก M*
- ✅ **2.3 cursor polling** (transactions/ious) — **เสร็จแล้ว**: client ใช้ `.gt('updated_at', txCursorRef)` (full/delta/first mode + `cursorDisabledRef` fallback, pollForUpdates ~L12848) + SQL `tools/migrations/2026-06-16-cursor-polling-updated-at.sql` (top-level = run แล้ว)
- ✅ **2.4 admin dashboard RPC** — **เสร็จแล้ว** (2026-06-17): client wire ไว้ตั้งแต่ v3.7.50 + SQL `tools/migrations/2026-06-17-admin-dashboard-summary.sql` รันที่ Supabase แล้ว (verified: hourly=24/weekday=7, dau/mau คืนค่าถูก). แก้บั๊ก sparse hourly/weekday จาก draft v2.
- **Phase 2.1b / 2.2b** ตัด `loadDetail()/loadShopData()` ออกจาก local writes (ต้องการ optimistic update เต็ม) — ⚠️ เสี่ยง: แตะ core money-sync, ทำเมื่อมั่นใจ delta-merge เสถียร + ต้องเทสต์ multi-device จริง (เทสต์ใน demo mode ไม่ได้ เพราะ supabase stub)

## sprint ที่ทำแล้ว (2026-06-15/16)
ดู `docs/2026-06-15-research-action-plan.md` สำหรับรายละเอียดงานวิจัย (52-agent workflow + adversarial verify)
- v3.7.37 — security probe fix + preconnect + decision records
- v3.7.37 — SQL: 25 indexes (RLS, .or() patterns, POS, admin) — `tools/migrations/2026-06-15-research-indexes.sql`
- v3.7.38 — family realtime delta-merge (7 RTT/event → 0)
- v3.7.39 — POS realtime delta-merge (4-query refetch → 0)
- v3.7.40 — POS sales totals RPC (correctness fix: ยอด > 500 บิลผิดเงียบๆ) — `tools/migrations/2026-06-16-pos-sales-totals-rpc.sql`

## สิ่งที่ไม่ควรทำ
- ห้ามแตก index.html เป็นหลายไฟล์
- ห้าม `cat`/อ่านทั้งไฟล์ 3.9MB โดยไม่จำเป็น — ใช้ `grep -n` + `sed -n` ช่วงแคบ ๆ (ระวังบรรทัด I18N dictionary ที่ยาวเป็นหมื่นตัวอักษร — grep แบบจำกัด width)
- ห้าม bump version ไม่ครบ 4 จุด
- ห้าม commit โดยยังไม่ผ่าน `node --check`

## ตัดสินใจแล้ว — ไม่ทำ (ป้องกัน session ถัดไปเสนอซ้ำ)

ผ่าน research workflow 2026-06-15 (52 agents, audit + adversarial review):

**ไม่ migrate PowerSync / cr-sqlite (local-first)** — ขัด iron rule "single file"; anon QR ไม่มี JWT (sync layer ไม่รองรับ); ผู้ใช้ส่วนใหญ่ single-writer ไม่ต้อง CRDT; delta-merge ใน-memory ทำเองได้ ~90% ของ benefit (ดู v3.7.37+ realtime delta-merge work).

**ไม่ migrate Vite / SvelteKit** — claim "ลด bundle 60%" ลอย เพราะ Parme ไม่มี Babel runtime อยู่แล้ว (React.createElement ตรงๆ); ขัด no-build rule.

**ไม่ย้าย GitHub Pages → Cloudflare Pages** — origin change → PWA ที่ install ไว้แล้ว orphan; GitHub Pages มี Fastly Bangkok PoP อยู่แล้ว.

**ไม่เพิ่ม Service Worker shell cache** — ขัด iron rule "ห้ามแตกไฟล์"; SW จาก Blob URL ไม่มี scope; version skew กับ Supabase schema = correctness risk.

**Trigger reconsider:** ถ้ามี shop จริง > 10 + multi-cashier conflict report จริง + ยอมแตก single-file constraint → คุยเรื่อง local-first ใหม่.
